# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

AI parental-control platform (ESPRIT PFE / internship project). Two independent apps in one repo:

- **`backend/`** — Node.js + Express + PostgreSQL API (TypeScript, ESM). Stores usage/screen metadata, computes daily scores, generates gamified missions.
- **`MobileApp/`** — React Native 0.74 Android app (TypeScript) with custom Java/Kotlin native modules. Does all sensitive processing (OCR, NSFW, keyword filtering) **on-device**; only extracted text + risk metadata leave the phone.

The privacy model is central: **no screenshot ever reaches the backend.** The device captures a screen, runs ML Kit OCR + `nsfwjs`/TFLite + a multilingual keyword filter, deletes the image, and POSTs only text metadata to `POST /api/screen-events`: a ≤500-char text preview, bounded numeric risk scores, an enum category, the app package/label, and a schema-bounded `imageClassificationDetails` object — a fixed set of named fields (classifier scores + model-label vocabulary), every string length-capped, unknown keys stripped and logged. The schema bounds the shape and size of what the device can send there (~2 KB of model output), not the semantics of each field; every field is server-validated in `backend/src/validators/screenEvents.validator.ts` (the ≤500-char cap enforced there **and** by the `VARCHAR(500)` column). **Mobile→backend coupling (handoff #15):** `source` and `captureQualityHint` are `.valid()` enums with no truncate escape — adding a `ClassificationSource` or a third `captureQualityHint` value on the device 400s **every** capture until the validator gains the value in the same commit; string fields elsewhere in the object `.truncate()` instead, and `mockHint` is accepted-but-stripped. Preserve this invariant when touching the capture pipeline.

## Working rule

**After finishing any task, update this CLAUDE.md file.** Fold in whatever changed that a future session would need — new/renamed commands, added routes or modules, architectural shifts, new conventions, or gotchas discovered. Keep it accurate and concise; remove anything the change made stale. Skip only trivial edits that alter nothing documented here.

This is enforced by a **Stop hook** in `.claude/settings.json`: if you changed source (`MobileApp/`, `backend/`, `scripts/`, `docs/`, or `demo_dashboard.html`) without touching CLAUDE.md, it blocks the turn from ending until you update CLAUDE.md (or state in one line that nothing needs documenting).

## Commands

The two apps have separate toolchains — always `cd` into the right one. The shell is PowerShell on Windows.

### backend/
```powershell
npm run dev            # tsx watch src/index.ts (hot reload)
npm run build          # tsc -> dist/
npm start              # node dist/index.js
npm run db:up          # docker compose up -d (Postgres on host :5433 → container 5432)
npm run db:migrate     # tsx src/db/migrate.ts (ledger-tracked; safe to re-run any number of times)
npm run db:setup       # db:up + db:migrate
npm test               # jest (ts-jest, tests live in backend/tests/**.test.ts)
npm run test:watch
npx jest tests/scoringEngine.test.ts   # run one test file
```
`docker-compose.yml` maps host port **5433**→5432 specifically to avoid the local PostgreSQL 18 service on 5432; `.env.example` already points `DATABASE_URL` at 5433, so `npm run db:setup` works without stopping that service. `db/migrate.ts` keeps a **`schema_migrations` ledger** (created by the runner itself): each file runs once, inside its own transaction together with its ledger insert (fail → rollback → no row, no partial schema), and re-running is a safe no-op. Decision logic is the pure, DB-free `src/db/migrationPlan.ts` (tested by `tests/migrationPlan.test.ts`). Editing an already-applied file is detected by **checksum** and **refused** (exit 1), not silently re-run. A pre-ledger DB already at head (e.g. Helmi's demo DB) is auto-detected via landmark probes and **baselined** — every file stamped as applied, nothing executed. A *partially* migrated pre-ledger DB aborts with an actionable message; the operator picks up with `MIGRATE_BASELINE_UPTO=<file> npm run db:migrate` (stamp ≤ that file, run the rest). `docker compose down -v` is **no longer** a routine recovery path — only for deliberately discarding a database. See `backend/DATABASE.md` for more.

### MobileApp/
```powershell
npm start              # react-native start (Metro)
npm run android        # react-native run-android
npm run lint           # eslint .
npm test               # jest (preset: react-native, tests in MobileApp/__tests__/)
npx jest __tests__/keywordFilter.test.ts   # run one test file
npm run download-nsfw-model   # fetch the on-device NSFW TFLite model (needed before NSFW works)
```
Native Android changes (Java/Kotlin under `android/app/src/main/java/com/mobileapp/`) require a full `run-android` rebuild — Metro fast-refresh does not pick them up. **JS-only** changes need no rebuild: `adb shell am force-stop com.mobileapp` then relaunch re-pulls the bundle from Metro.

**Building on this Windows + OneDrive machine — two blockers** (`react-native run-android` fails here):
1. `'gradlew.bat' is not recognized` — the RN CLI spawns it without a `.\` prefix. Build directly from `MobileApp/android` in PowerShell: `.\gradlew.bat app:installDebug -PreactNativeDevServerPort=8081`.
2. `Could not move temporary workspace … to immutable location` — Gradle × OneDrive file-lock (project lives under `C:\Users\…\OneDrive\`). Redirect the project cache off OneDrive: append `--project-cache-dir C:\gradlecache\mobileapp`. First clear the corrupt cache (`.\gradlew.bat --stop`, delete `android/.gradle`).

Full working install: `.\gradlew.bat app:installDebug --project-cache-dir C:\gradlecache\mobileapp -PreactNativeDevServerPort=8081`, then launch via `adb`. `adb reverse tcp:3000 tcp:3000` + `tcp:8081 tcp:8081` tunnels a USB device to the backend + Metro. Permanent fix: move the repo out of OneDrive.

**Defence-day pre-flight (do this before you're in front of the jury, not after):** (1) confirm the accessibility service is actually delivering events — Settings → Accessibility → SafeGuard shows ON, then switch apps once and check the Monitor tab's **Accessibility service** card reads **Active** (sage), not *Enabled* (neutral, = listed but never yet proven) and not *Not responding* (amber, = a poll-observed switch the a11y feed missed). `Active` proves the service is bound and pushing window events; it does **not** prove the window filter accepted a switch and a capture fired — an `a11y.window.accepted` line in the log on a real app switch remains the stronger end-to-end check, and `Active` can also light from in-app navigation alone, so "switch apps once" is the better instruction but not strictly what flips it. adb cannot verify or fix binding reliably on this ROM, and raw `isEnabled()`/`connected` can read true with zero events flowing — the card's evidence-based state is what catches that. (2) toggle monitoring ON from a cold app launch and confirm a clean `startCapture SUCCESS — loop scheduled` with no red LogBox before relying on it live. (3) **Re-check the card after any screen sleep, not just once at setup.** ALL_IS_FIXED #1b device-tested a ≥2 min screen sleep twice: the first time the accessibility service came back fully unbound (`dumpsys accessibility` showed empty Bound/Enabled services) with zero in-app signal until the card was read; an immediate repeat of the identical sleep did **not** reproduce it — service stayed bound throughout. The drop is real but not deterministic from sleep alone (total backgrounded duration may matter; unconfirmed) — treat "checked once before going live" as insufficient if the device sleeps at any point during the defence, and re-glance the card after any unlock. **Rate caveat:** the #1b session saw the service drop 3 times total, but that session also ran 8+ `adb shell am force-stop` + relaunch cycles in under an hour — a testing artifact, not organic usage — and at least 2 of the 3 drops landed immediately after one of those cycles rather than during idle/sleep time. `Crashed services` read empty in `dumpsys` at drop time (the service was wiped from `enabled_accessibility_services` entirely, not left listed-but-crashed) — the exact OS trigger is unconfirmed. Don't read "3 drops/session" as an organic per-hour rate; a defence run does no force-stops, so the real rate there is unmeasured and plausibly much lower.

## Configuration you'll hit immediately

- **Backend env** (`backend/src/config/env.ts`): `DATABASE_URL` and `JWT_SECRET` are **required** (app throws on boot if missing). Others optional: `PORT` (3000), `NODE_ENV`, `JWT_ISSUER`, `APP_TIMEZONE` (default `Africa/Tunis`, validated at boot via `Intl.DateTimeFormat` — throws on an unrecognized IANA zone name; see the Cron/Scoring bullets below, ALL_IS_FIXED #10).
- **Mobile → backend URL** (`MobileApp/src/config/apiConfig.ts`): `DEV_LAN_HOST` is currently `127.0.0.1` for a **USB device via `adb reverse`** (see build note above). For a phone on the same Wi-Fi instead, set it to your PC's Wi-Fi IPv4. Android emulator auto-uses `10.0.2.2`. `__DEV__` builds hit `http://<host>:3000`.
- **Auth in dev**: the app mints a child JWT via `/api/dev/*` routes (see `useDevChildToken`). Those `/dev` and `/debug` routes are registered **only when `NODE_ENV !== production`** (`routes/index.ts`).

## Backend architecture

Entry: `src/index.ts` → `createApp()` (`src/app.ts`) → `src/routes/index.ts`.

- **Auth boundary**: `routes/index.ts` mounts `/health` and (non-prod) `/dev`, `/debug` publicly, then applies `verifyToken` middleware — **every route below that line requires `Authorization: Bearer <JWT>`**. JWT payload = `{ sub, role: 'parent'|'child', childId? }`.
- **Ownership authorization (ALL_IS_FIXED #7)** — `verifyToken` proves identity, not that the caller owns the `childId` they name. The **single** predicate is `userCanAccessChild(user, childId)` in `src/middleware/childAccess.ts` (child branch: `user.childId === childId`; parent branch: `parentOwnsChild(user.sub, childId)` = `SELECT 1 FROM children WHERE id=$1 AND parent_id=$2`, a PK probe — **not cached**, no invalidation story to own). Every earlier copy is gone (the `assertChildAccess` rubber-stamp whose *parent* branch was `return true` — 3 copies / 7 call sites — and the private `assertParentOwnsChild` in `child.routes.ts`). Child id arrives in **4 shapes**: `param` / `query` / `body` → the `requireChildAccess('<loc>:childId')` middleware factory (its returned fn is a **named** expression `requireChildAccessMw` so the enumeration test can see it); `derived` (from a loaded `missions` row) → an in-handler `userCanAccessChild` call in `approve`/`reject` after the row is fetched. **`tests/routeAuthorization.test.ts`** walks the mounted router and checks the **complete** table against a committed `ROUTE_INVENTORY` (symmetric — any route added/renamed/removed reds the suite until a human classifies it; that churn is the point) + per-class assertions + a `:childId`-path-can't-be-non-child belt-and-suspenders rule; **residual:** it cannot introspect a handler body, so the two `derived` routes are held by a source-text assertion (`userCanAccessChild(` present) + acceptance, not by chain inspection. **Failure = `403 {"error":"Access denied for this child"}`** uniformly (matches the pre-existing contract; adversary is another registered parent so a child UUID's existence is not a secret — 404 would churn the dashboard and collide with the genuine `404 "Mission not found"`). **`POST /missions/generate`**: a named `devOnlyRoute` runs **first** (flat `404` in production for everyone, *before* any role/body/ownership branch) — putting the ownership guard ahead of the old in-handler `isProduction` 404 would have made prod answer `403` vs `404` by ownership on an invisible route (the #8-SF4 failure class); then `requireParentRole` + `requireChildAccess('body:childId')`. **Behaviour delta:** the ~9 newly-guarded `param`/`query` routes now do a DB lookup — if it *throws*, the response is `500 {"error":"Authorization check failed"}` where a rubber stamp would have returned `200`; `child.routes.ts`'s 500-on-lookup-error body changed from `"Failed to fetch/update child …"` to the same. Happy paths and 403 paths unchanged. **This is authorization given a valid token — there is no authentication / registration / token-issuance route; the only JWT source is the unauthenticated `/api/dev/*-token`. Do not describe the API as "secured".**
- **Routes** (`src/routes/*.routes.ts`): `screen-events`, `usage`, `scores`, `missions`, `rewards`, `badges`, `bonus`, `custom-missions`, `child`.
- **Scoring** (`src/scoring/`): pure functions. `scoringEngine.ts` computes an **addiction risk score** (higher = worse; weighted intensity/compulsivity/night-usage/escalation/real-imbalance) and a **well-being score** (higher = better). `aggregateUsage.ts` turns raw `usage_sessions` rows into the daily stats those functions consume; `wellbeingProxies.ts` fetches DB-backed inputs (physical activity, bedtime variance, family interaction). Keep scoring functions pure and covered by `tests/scoringEngine.test.ts` / `wellbeingProxies.test.ts`. **Timezone (ALL_IS_FIXED #10):** `nightMinutesInSession`/`toScoreDateString` (`aggregateUsage.ts`) take an explicit `timeZone` parameter (defaulted to `Africa/Tunis`, still pure/dependency-free — production call sites thread `env.appTimezone` through). Every `TIMESTAMPTZ`-vs-`DATE` comparison in `dailyScoreJob.ts`/`wellbeingProxies.ts`/`usage.routes.ts`/`scores.routes.ts` binds the zone as a SQL parameter (`AT TIME ZONE $n`) rather than relying on the implicit `::date` cast promotion (which silently used the Postgres session `TimeZone` GUC, confirmed UTC on this DB). **`fetchBedtimeVarianceMinutes` deliberately stays UTC** (documented inline) — its `STDDEV` is a wrap-sensitive metric over seconds-since-midnight; converting it to local time would move the wrap discontinuity onto the most common real bedtime and clamp a regular sleeper's `sleepConsistency` to 0. Its `toScoreDateString` call passes `'UTC'` explicitly since that helper's own default changed to `Africa/Tunis` for every other caller.
- **Cron** (`src/jobs/dailyScoreJob.ts`): scheduled on server start (`node-cron`), now pinned to `{ timezone: env.appTimezone }` so the 01:00 fire time no longer depends on the host's local zone (ALL_IS_FIXED #10). Per child per day, aggregates sessions → computes both scores → writes `daily_scores` → may generate a mission from high addiction / low well-being. `GET /api/scores/:childId/trend`'s 7-day window (`scores.routes.ts`) also moved off a UTC-evaluated `CURRENT_DATE` onto the configured zone's "today" (S10). `formatScoreDate` (`scores.routes.ts`) now builds its string from a returned `Date`'s local components — `pg` parses `DATE` columns into a `Date` at local midnight, so `.toISOString()` was rendering every `score_date` one day early on this UTC+1 host.
- **Missions** (`src/services/`): `missionGenerator` (rule-based generation), `missionCompletion`, `gamificationService` (points/badges), `quizService`, `customMissionService`. Mission "risk cooldown" is now "a pending risky mission exists" — see `hasRecentRiskyMission`, not the legacy env var.
- **`POST /api/missions/dev/force` (ALL_IS_FIXED #6)** — dev-only route to force a specific mission template for a demo. Chain: `devOnlyRoute` **first** (flat `404` in production, before any role/body/ownership branch — same `#8-SF4` reason as `/generate`) → `requireParentRole` → `validateBody(forceMissionDevSchema)` → `requireChildAccess('body:childId')`. Inventoried in `ROUTE_INVENTORY` as `child:body`. Body: `{ childId, templateKey (a `.valid()` enum over `Object.keys(MISSION_TEMPLATES)` — unknown → 400), triggerReason? (default `risky_content`), category?, score? (default 75) }`. It calls `generateMissionForChild` directly with a new optional `MissionGenerationContext.forceTemplateKey` (`undefined` on every production path) which **bypasses the cooldown, the adaptive risk threshold, the pending limit, the random `pickMissionTemplate` draw and the age-based rewrite** (`forceTemplate` → `cloneTemplate`, skipping `buildTemplateFromPick`); quiz enrichment, escalation, the metadata object and the INSERT are unchanged. `expires_at` still comes from the column default (`007_missions_gamification.sql:13`, `NOW()+24h`), so the forced row is visible to `hasRecentRiskyMission` / `getActivePendingMission` and re-surfaces via `POST /api/screen-events`. **Verified backend-only at the time:** a forced pending Tic-Tac-Toe mission is returned to a risky capture as a re-surfaced `newMission` (id + `metadata.game` + `reSurfaced:true`). **Device presentation is no longer unexercised — see ALL_IS_FIXED #51's device arm below**, which drove `presentMissionFromCapture` directly (a temporary, fully-reverted `__DEV__` scaffold, since staging a genuine risky OCR capture safely was out of that session's budget) and played the resulting overlay/in-app mission end-to-end.
- **ALL_IS_FIXED #51 — Tic-Tac-Toe playable natively in the mission overlay, plus a completion-trust fix and a pool edit (2026-09-13, full closeout `briefs/ALL_IS_FIXED-51-CLOSEOUT.md`).** Three parts, one native game:
  - **Part A (mobile):** `OverlayTicTacToeHelper.java` (new) renders a real 3×3 board inside the overlay window, mirroring `OverlayQuizHelper.java`'s shape exactly (stateless, static `showGame`, all state in local closures — resets on any hide/reshow, deliberately, same as the quiz). `OverlayWindowHelper.java` dispatches to it when `missionType === "minigame"` **and** `metadata.game === "tictactoe"` (parsed defensively; anything else — including Sudoku, still `minigame` too — falls through to the existing hand-off, never a half-identified render). AI is **exactly one tier** — the `medium` logic from `gameLogic.ts`'s `tttAiMove` (win-check, block-check, center, random fallback) — `easy` and `hard` are cut because the generator never sets `metadata.aiDifficulty`, making them unreachable dead code by construction. A **turn-lock bug found only on device** (the initial `renderBoard()` enabled every empty cell regardless of whose turn it was, letting a child place two X's during the AI's ~450ms "thinking" delay) was caught and fixed before the first real play — a `humanTurn` boolean now gates both the tap handler and per-cell `setEnabled`.
  - **Part A, also (the blocker a plan-review round caught before any code was written):** the in-app Tic-Tac-Toe path (`MissionScreen.tsx` → `TicTacToeGame.tsx`, reached whenever "Display over other apps" isn't granted) calls `completeMission` directly with the game's own payload, bypassing `buildCompletionPayload` entirely — so Part B's backend enforcement, as first scoped, would have refused **every in-app completion**, not just added overlay enforcement. Fixed by extending `tttTurn.ts`'s state machine to record a `moveSequence` and `TicTacToeGame.tsx`'s `onComplete` to emit `finalBoard`/`moveSequence` alongside `won`/`completed` — the same evidence shape the overlay produces, now on both surfaces. `buildCompletionPayload`'s `minigame` case (`missionCompletion.ts`) forwards that evidence when present and returns `{}` (never a fabricated `won:true`) when it's missing for a tictactoe mission; Sudoku's unconditional `{won:true}` is unchanged.
  - **Part B (backend):** `evaluateMissionCompletion`'s `minigame` branch, for `metadata.game === 'tictactoe'`, replays a claimed `moveSequence` onto an empty board (`X` first, alternating, `''` for empty — chosen over `null`/`JSONObject.NULL` specifically because `Joi.string().valid(...)` would 400 every board with an empty cell, i.e. most wins, so the schema uses bare `Joi.valid('X','O','')`) and checks it against the claimed `finalBoard`; `payload.won` is never read. A win, loss, **or draw** all succeed as long as the board is a legitimately played terminal position — this is existing behavior made explicit (the in-app game already sent `completed:true` on every terminal board and the old branch already trusted it), not new scoring policy. Fabrication (missing evidence, illegal moves, moves continuing past a decided position, a board that doesn't match the replay, non-terminal) refuses with **0 points** — a distinct path from the generic branch's 25%-partial-credit fallback, which means "didn't finish," not "faked." The server-derived `outcome` (`'X'|'O'|'draw'`) is stored in `metadata.completionData`, unchanged write path. Sudoku and every other minigame keep the old bare `payload.won===true||payload.completed===true` check.
  - **Part C:** one line — `'nback'` removed from `RISK_CATEGORY_TEMPLATES.adult` and `.violent` (`missionGenerator.ts`) — verified template-by-template that every remaining key in both pools is `quiz`, `real_world`, or `minigame:tictactoe` (all overlay-native-playable). `nback` is untouched in the separate, inline `high_addiction`-trigger candidate list inside `pickMissionTemplate` — that list is not a pool map and was never in scope.
  - **Device-verified, all five outcome shapes, both surfaces, one session:** overlay-arm AI win (`outcome:"O"`, a real medium-tier win-over-block), in-app-arm child win via a corner fork (`outcome:"X"`), a landscape-arm draw (state survived rotation intact; the overflowing card's `ScrollView` correctly revealed clipped rows and a cell tapped mid-scroll registered on the right cell), and a screen-off/wake/unlock-arm AI win with the board **intact** (not reset — the better of the two outcomes §A2b treats as acceptable). Negative arm (fabricated `{won:true}`, empty board, non-replaying `moveSequence`) run at the API level: all three refused, 0 points, mission stayed `pending`; a legitimate loss submitted immediately after was accepted in full — confirming the negative set is fabrication, not "any non-win."
  - **#50 is now live, not dormant:** a mission type has moved off the overlay hand-off path onto a native in-window one, and nothing enforces that the Java dispatch list and `gameRegistry.ts`'s JS dispatch list stay in agreement. Not fixed here.
  - Mobile: **417 tests / 33 suites** (was 405/32 — new suite `missionCompletion.test.ts`, +8 cases; `tttTurn.test.ts` +4 for `moveSequence`). Backend: **212 / 21** (was 196/21 — Part B +16 cases in two new `describe` blocks, Part C +5; no new suites). `tsc` unchanged (8 pre-existing, 0 new) on both.
- **ALL_IS_FIXED #46 — mission overlay full-bleed restyle, real progress, per-answer quiz feedback (2026-09-13, mobile-only, enhancement not a defect fix — closeout `briefs/ALL_IS_FIXED-46-CLOSEOUT.md`).** All three native overlay surfaces (`OverlayWindowHelper`'s two-button prompt card, `OverlayQuizHelper`, `OverlayTicTacToeHelper`) now share one chrome: a new `OverlayChrome.java` plus `values/colors.xml`/`values/dimens.xml` (new) supply the palette/type-scale both the programmatic surfaces and `overlay_mission.xml` read from — one source of style truth, zero deleted files, zero change to `OverlayWindowHelper`'s `completeBtn`/`laterBtn` dispatch logic. **Full-bleed + insets:** the window root (`overlay_root`, the same View both `OverlayQuizHelper`/`OverlayTicTacToeHelper` `removeAllViews()` and rebuild into) gets an opaque `@color/overlay_bg` background and `ViewCompat.setOnApplyWindowInsetsListener` system-bar padding installed **exactly once**, in `OverlayWindowHelper.attach()` — padding doesn't shrink where a View's own background paints, so the background stays edge-to-edge under the status/gesture bars while content stays clear of them; re-fires automatically on rotation, no `onConfigurationChanged` needed. **Content sizing:** `ScrollView(fillViewport)` → `gravity=center` column → content, centering short content for free and scrolling (not clipping) tall content — this is also the landscape-overflow fix, a side effect of the sizing change rather than this brief's purpose. `OverlayChrome.applyMaxWidth` clamps the content column to `@dimen/overlay_content_max_width` (480dp) and **re-clamps via an `addOnLayoutChangeListener` on rotation** — a width computed once from `DisplayMetrics` does NOT get recomputed by Android's ordinary remeasure-on-rotation, an error the plan draft made and corrected before implementation; device-confirmed the fix is real (landscape column visibly wider than portrait, not stuck narrow, not edge-to-edge). **Progress dots** (quiz only, `OverlayChrome.buildProgressDots`) replace the plain "Question N/M" text, placed above the question; Tic-Tac-Toe deliberately gets no equivalent (the board is its own progress). Points caption dropped the raw `missionType` string (`"35 points · quiz"` → `"35 points"`). **Per-answer feedback (quiz only):** tapping an option commits it to `submittedAnswers` unconditionally (payload unchanged), then recolors green/red and — if wrong — also greens the true correct option, using `metadata.correctAnswers`, which R1 traced as **already present** in the overlay's metadata today (unfiltered, end-to-end from `missionGenerator.ts` through the Intent extra) — this brief newly *reads* that field, it does not newly expose it (see #52). A single `inputLocked` boolean gates the entire options row (not per-button) for the ~1000ms hold — an own-chosen value, not a spec citation — device-verified against the correction-13 failure mode specifically: four rapid taps chained in one `adb shell` call (correct, then three wrong) left only the first tap's coloring in effect, no early advance. **Teardown safety:** `OverlayQuizHelper.showQuiz` returns a canceller `Runnable`; `OverlayService` stores it in a new `pendingQuizCancel` field and invokes it from both existing removal paths (`removeOverlayNow`/`removeOverlay`) before detaching — otherwise a pending "advance to next question" callback could fire against a detached view or double-complete the mission. A pre-existing nested-`ScrollView` bug in `OverlayTicTacToeHelper` (its old per-helper `ScrollView`, added via `height=0dp+weight=1`, would have collapsed to zero height once the card became `wrap_content` under the new outer scroll) was caught rereading the diff before the first build, not on device — fixed by dropping the inner `ScrollView`, the grid now adds straight to the shared card. **Not fixed here, filed:** **#52** — `correctAnswers` is client-readable in the overlay's metadata (scoring unaffected, server independently re-grades); **#53** — `OverlayTicTacToeHelper`'s own AI-move `postDelayed` has the identical teardown gap #46 just closed for the quiz. Back-press on a started quiz remains a confirmed no-op (unchanged, not a live hole). Device-verified end to end on `2508CRN2BE`: quiz + Tic-Tac-Toe, portrait + landscape, a full Tic-Tac-Toe game played to a real AI win (backend replay-validated), real mission completions via the existing `/api/missions/dev/force` dev route (#6) scoring correctly (`missions` 30→34, `child_points` 530→590 across the session). Mobile: **417/33 unchanged** (Java/resource-only diff, JS scaffold used for the device arm fully reverted before commit), `tsc` 8/8 unchanged. No backend change (R1=yes selected the no-backend-change branch).
- **ALL_IS_FIXED #54 — the overlay adopts the app's design system (2026-09-13, mobile-only, corrects #46's own palette — full closeout `briefs/ALL_IS_FIXED-54-CLOSEOUT.md`).** #46 centralised the overlay's palette into `values/colors.xml`/`values/dimens.xml` but invented the values without checking whether the app already had a design system — it does: `MobileApp/src/theme/index.ts`'s `focus` object, documented in its own header as "the dark, immersive surface used for active missions, games and quizzes," genuinely consumed by `MissionScreen.tsx`/`QuizScreen.tsx` (the in-app quiz fallback). #54 repoints every overlay color/dimen at `focus`'s real values; **zero layout, insets, feedback-timing, or dispatch changes** — #46's structural work stands untouched. **Colors:** every `colors.xml` entry now equals a `focus.*` hex (background/card panel → `focus.bg`, option buttons+Tic-Tac-Toe cells → `focus.surface`, text → `focus.text`, caption → `focus.textMuted`, eyebrow → `focus.amber`, correct-feedback → `focus.accentStrong`, incorrect-feedback+warning-text → `focus.coral`, progress-dot-active → `focus.accent`, dot-inactive+new `overlay_border` → `focus.border`); one exception is `overlay_btn_text` (new), a literal `#FFFFFF` because `MissionScreen.primaryBtnText` really does use raw white, not `focus.text`'s warm white — found by reading the real screen, not assumed. **The full-bleed background and the card panel stay the same color on purpose** (`overlay_mission_card.xml` untouched) — the app never layers a screen-sized `focus.surface` panel on a `focus.bg` ground, `focus.surface` is for small raised elements *inside* a `focus.bg` screen (this was caught in plan review before implementation: an earlier draft had mapped both to `focus.surface`, which would have made the option rows invisible against their own container). **Type sizes are pinned to `MissionScreen`/`QuizScreen`'s real rendered values, not to `theme/index.ts`'s `type` scale** — neither screen actually consumes `type.*` (both hardcode fontSize/fontWeight; `type.heading` has exactly one consumer app-wide, a light-mode primitive), so matching `type.*` would match a specification the app itself doesn't follow. The overlay's title dimen is split in two: the XML prompt card (the overlay's one-screen analogue of `MissionScreen`, no co-occurring question) takes `MissionScreen.title`'s real 26sp; the quiz/Tic-Tac-Toe surface's title (`OverlayChrome.titleView()`, sitting above a question on one combined screen unlike the app's two-step flow) uses a constructed 18sp with no direct app precedent, sized to read as secondary under the 21sp question (`QuizScreen.question`'s real size). `letterSpacing` is narrower than a first pass assumed: of every real style checked, only `MissionScreen.badgeText` carries tracking (`1.2` at `11sp` → `0.109em` on Android, RN dp vs. Android em) — applied to the badge only, not imported from `type.eyebrow`/`type.title`'s unused values. **Rounded option/cell buttons:** `Button.setBackgroundColor` (used for quiz options and Tic-Tac-Toe cells) replaces the platform drawable outright, rendering flat rectangles against `QuizScreen.option`'s real `borderRadius:14`/`borderWidth:1` rows — fixed with a new `overlay_option_shape.xml` rounded-rect+stroke drawable set once via `setBackgroundResource`, recolored via `setBackgroundTintList` instead of `setBackgroundColor` (which would flatten the shape back on every recolor) — a grep-confirmed audit found exactly these two `setBackgroundColor` call sites in the whole `overlay` package, nothing else needed converting (every other dim/disable effect already used `setAlpha`, which doesn't flatten drawables). **New parity test** (`__tests__/overlayThemeParity.test.ts`, 23 cases) reads `colors.xml`/`dimens.xml` from disk and asserts every color and every spacing/radius dimen against `theme/index.ts`'s `focus`/`spacing`/`radius`, with a fail-loud guard (an unmapped name breaks the test) — **deliberately does not assert type sizes**, since there's no theme-side source of truth to check them against and a passing assertion against the unused `type` scale would lock in a divergence from what the app actually renders, not prevent one. **Filed, not fixed: #55** — the app defines a type scale and doesn't consistently use it (report only; fixing it means touching every app screen, out of scope for a brief that must not touch the app). Device-verified on `2508CRN2BE`: option rows visibly distinct from the card background (the exact bug the plan-review catch above prevented from reaching a device), rounded corners survive the tint-based answer-feedback recolor, the double-tap guard still holds under the new background mechanism, a real quiz completion with proportional partial credit (2/3 correct → +20/30, backend-graded) and a real Tic-Tac-Toe draw (`{"outcome":"draw",...}`, server-replay-validated per #51) both scored correctly, and — closing a gap #46 shipped with — the first screenshots of `MissionScreen`/`MissionListScreen` anywhere in `briefs/device-session-logs/`, letting a side-by-side judgment happen for the first time: the palette now matches convincingly, but the two surfaces are **not pixel-identical** — `MissionScreen`'s pill-shaped badge/points/type chips have no overlay equivalent (plain text there), a real, disclosed, out-of-scope-for-this-brief difference (component/layout, not color). **Landscape was not re-verified this session** — multiple `adb` rotation methods (`settings put user_rotation`, `wm user-rotation lock`, `cmd window`) failed to produce a landscape render on this device/ROM; #46's own orientation-sensitive code (`applyMaxWidth`, insets) is untouched by #54 and was already device-verified for landscape in #46's closeout, but the combination with the new palette/rounded corners specifically was not independently observed in landscape here — disclosed as a real gap, not silently skipped. Mobile: **440/34** (was 417/33, +23 new suite), `tsc` 8/8 unchanged, 0 new lint. No backend change.
- **ALL_IS_FIXED #53 — the Tic-Tac-Toe overlay's AI-move delay had no teardown canceller; the missing one was a real, device-confirmed phantom mission completion, not just a stale-view write (2026-09-14, mobile-only, Java-only diff).** #46 shipped a canceller for the quiz's answer-feedback delay but explicitly left `OverlayTicTacToeHelper`'s AI-move `postDelayed` (`AI_MOVE_DELAY_MS` 450ms) unguarded (filed as #53 in that closeout). Tracing the full path before touching code (the review round that sent the first arm design back) found there is **no guard anywhere** between the delayed callback's eventual `emitMissionAction("complete", …)` dispatch and the backend accepting it — `useMissionOverlayListener`'s `handleOverlayAction` has no active-mission/id-match/dismissal check on the `complete` path, `buildCompletionPayload` forwards whatever `finalBoard`/`moveSequence` the stale closure produced, and `replayTicTacToe` (#51) only checks that the moves are a legitimate terminal position on a still-`pending` row — it has no notion of whether the app still shows the mission. Since neither teardown mechanism (`OverlayMissionModule.hideOverlay()`, the `ACTION_HIDE` service intent) touches server-side mission state, the mission stays `pending` and the stale AI win is accepted: **a torn-down overlay can award a child mission points for a game they already left.** Fix mirrors the quiz's shape exactly: `OverlayTicTacToeHelper.showGame` now returns a canceller `Runnable` (the single `pendingAiTurn` instance — created once, not re-created per tap, so a second post before the first fires can never leave an uncancellable orphan); `OverlayService` stores it in a new `volatile Runnable pendingTttCancel` field (the existing `pendingQuizCancel` was made `volatile` too, as defensive hardening — tracing every call site showed the actual read today always lands back on the main thread via `Looper.myLooper()`/`mainHandler.post()`, so this isn't a fix for a demonstrated race, just insurance against a future off-thread caller) and invokes it at all four removal-path call sites (both branches of `removeOverlayNow()` and `removeOverlay()`), same position as the quiz's, before `detach()`. **Device-verified with the DB as the primary signal, not a log line:** 3/3 staged red runs (fix wired but its cancel calls commented out) produced a real `missions.status='completed'` row + a `child_points.total_points` +20 delta for a board abandoned before the AI's delayed winning move ever displayed (one phantom row's `completionData`: `{"game":"tictactoe","outcome":"O","finalBoard":["X","X","","O","O","O","","","X"]}`, `moveSequence:[0,4,8,5,1,3]`); 3/3 green runs (fix restored) left the mission `pending` with a measured cancel-minus-post delta of 394–401ms (under the 450ms window) every time, proving the teardown genuinely landed inside the window rather than the pass being inherited from the red runs. A same-technique rehearsal against the quiz's own `pendingAdvance` callback on its last question — which reaches the identical `onQuizFinished`→`emitMissionAction("complete",…)` path and had never been device-verified since #46 shipped it — found it correctly cancels (measured delta 696ms, under the quiz's 1000ms hold): **this is a #46 finding, not a #53 one, and it is a green-only result, not a proof** — #46's canceller was exercised on a device for the first time and cancelled inside its window; the phantom quiz completion it prevents is established from the code path (the identical `onQuizFinished`→`emitMissionAction` shape TTT's red runs demonstrated), not from an observed failure — the counterfactual (canceller disabled, phantom quiz completion actually occurring) was never run. Say "cancelled inside its window on first device exercise," not "proved" or "genuinely works." Teardown mechanics, established while designing the arm: `OverlayService` is `android:exported="false"` with no debug-variant override, so `adb shell am start-service -a ACTION_HIDE` is refused (`Requires permission not exported`, confirmed live) — the only reachable rung is JS's `hideMissionOverlay()`. Tracing thread ownership further: `removeOverlayNow()`'s three callers (`onStartCommand`'s `ACTION_HIDE` branch, `showOverlayFromIntent`'s `attachTask`, `onDestroy()`) are all Android `Service` lifecycle callbacks and therefore always already on the main thread, and `removeOverlay()`'s only caller (`OverlayMissionModule.hideOverlay()`, a legacy-bridge `@ReactMethod`) always runs off it — so of the four removal branches, two (`removeOverlayNow()`'s posted branch, `removeOverlay()`'s direct branch) are reached by no call site that exists today, not exercised by any rung, code-review-only by construction rather than by test omission. **Filed, not fixed: Tier-4 #56** — `OverlayService`'s other cross-thread-touched fields (`overlayView`, `windowManager`) have the same unsynchronized-access shape as the pre-`volatile` canceller fields; out of scope here. Mobile: **440/34 unchanged** (Java-only diff; one `npm test` run mid-session showed a single `tttTurn.test.ts` failure that did not reproduce standalone or on immediate re-run — an unrelated flake in a file this brief never touched, not a regression). `tsc` 8/8 unchanged, lint 0 delta (no JS/TS file differs from HEAD in the final commit). No backend change. Device scaffold (`ScreenMonitor.tsx` debug buttons to present the overlay and arm a delayed `hideMissionOverlay()` call, plus temporary `Log.w` timing probes in both Java files) fully reverted before commit, confirmed by `git diff --stat` showing only the two intended files.
- **ALL_IS_FIXED — docs debt: the `{cancelable: false}` correction, the #52 decision, two riders (2026-09-14, docs-only, no code).**
  - **`{cancelable: false}` in #41/#45 made an existing default explicit, it did not close a hole.** Re-verified at the installed pin (`react-native@0.74.5`, `node_modules/react-native/Libraries/Alert/Alert.js`): on **Android**, `Alert.alert`'s `config.cancelable` is hard-set `false` at construction (`:70`) and only overridden if the caller passes a truthy `options.cancelable` (`:73-75`) — so `Alert.alert(title, msg, buttons, {cancelable: false})` was always a no-op against the default. Confirmed from the actual commits, not the briefs' prose: `5d5e1fb` (#41) and `858360a` (#45) both pass `{cancelable: false}` as the alert's 4th argument. On **iOS**, `Alert.alert` dispatches to `Alert.prompt`, whose iOS branch (`:125-170`) never reads `options.cancelable` at all — `RCTAlertManager.alertWithArgs`'s payload has no `cancelable` key, so the option is silently ignored there, not defaulted differently. This app is Android-only in practice, so the practical claim is: **#41 and #45's `{cancelable: false}` additions changed no default-vs-override behavior on the platform this app ships on** — the real fix in both was the `.finally()`-independent release/re-entrancy work around them, not the `cancelable` line itself. Correct any future reference to those diffs accordingly; do not describe `{cancelable: false}` there as "closing" anything.
  - **#46's own closeout under-described its quiz canceller.** It frames `pendingQuizCancel` as guarding against "a pending 'advance to next question' callback fir[ing] against a detached view or double-complet[ing] the mission" — true, but buried as a side clause. What it actually closes, stated at the severity #53 established for the identical shape: on the quiz's *last* question, that same delayed callback reaches `onQuizFinished`→`emitMissionAction("complete",…)`, so an uncancelled one is a real, server-acceptable phantom mission completion, not just a stale-view write. #46's canceller closed that before #53 was ever found; it was simply never named at that severity until #53's investigation traced the identical path on the Tic-Tac-Toe side.
  - **#52 (the quiz answer key is client-readable) — decided, not deferred: keep it, disclosed.** The overlay's per-answer feedback (#46) colors the correct option green the moment the child taps — the feature's whole purpose is to show the child which answer was right, so hiding the key in the Intent extra a moment earlier has no coherent secret left to protect; a determined reader gets the same information from the UI a beat later. #46's own retry path already re-shows the identical questions after a failed attempt, so the answer key's reachability is a consequence of a content decision (immediate feedback, repeatable questions), not a transport mistake. Nothing about scoring depends on it — the backend re-grades `submittedAnswers` against its own copy regardless of what the client saw — so the exposure's entire consequence is "points for a mission the child didn't have to learn from," which the retry flow already grants by design; there is no privilege escalation and no other child's data at risk. **Rejected:** narrowing the payload to one question at a time — the current question's answer still reaches the device before it's answered, so it doesn't close the actual exploit, only moves it, at the cost of a mobile brief plus a device session. **Not scheduled, filed:** moving per-question feedback server-side (the deferred-review design already specced in #46's own brief as §5b) is the only real fix, but it removes the immediacy that is the feature's point and is its own backend+mobile brief with its own device arm — reopen it only if the calendar allows. **The defence answer, ready to give unprompted:** the quiz's correct answers reach the device because the overlay shows the child which answer was right the instant they tap; a child with developer tooling could read them ahead of time; scoring doesn't depend on it since the server re-grades every submission independently, so the exposure buys the child points for a quiz they didn't learn from — which the retry flow already permits on purpose; if the quiz ever becomes an assessment rather than a teaching surface, the answers move server-side, and that review path is already specced. **Do not claim #52 is closed, unexploitable, or free of cost** — it is a disclosed exposure with a stated, defensible reason.
  - **Riders, filed for the next device session, neither large enough for its own item:** (1) **Landscape re-check still owed since #54** — #54 raised several type sizes (quiz question 17→21sp, prompt title 20→26sp) that #46's landscape measurement predates, and `adb` rotation tooling failed on this ROM in both #54's and #53's sessions; the scroll container should absorb the taller content, but that is reasoning, not a measurement — ride it on the next session with working rotation, and if rotation fails again, say so again rather than dropping it quietly. (2) **`tttTurn.test.ts`'s "accumulates in play order across a human/AI exchange" case is a real, filed Tier-4 flake** — it failed once in a full-suite run and passed standalone/on immediate rerun in two separate sessions on files neither session touched (#46's device-verification pass, and #53, 2026-09-14) — two sightings makes it a pre-existing test-order dependency worth a line so a third session doesn't re-diagnose it from scratch.
- **ALL_IS_FIXED #20 — the "8 pre-existing tsc errors" footnote is now a named baseline of 2, not a number (2026-09-15, mobile-only, type-only, zero runtime change). Baseline: 8 → 3 (Part A) → 2 (949).** The 8 had never been enumerated before this session (`npx tsc --noEmit` had never been run to completion and read start-to-finish).
  - **Part A (5 fixed) — pure type noise.** `interface X {...}` does **not** get an implicit index signature in TypeScript; an object **type alias** `type X = {...}` does — identical shape, identical emitted JS (there is none — types erase), but only the type alias satisfies a `Record<string, unknown>`/`Record<string, number>` parameter. `OverlayMissionActionEvent` (`native/OverlayMission.ts`), `ScreenCaptureDebugState` (`native/ScreenCapture.ts`) and `NsfwProbabilities` (`services/nsfwClassifier.ts`) were each declared once, `extends`-ed by nothing, never declaration-merged (checked by grep before touching any of them) — converted `interface` → `type` on all three, nothing else. The two consumers (`scLog('Overlay mission action', event)`, `scLog('Native state @ …', state)`) are confirmed log calls, not wire payloads. `imageClassifier.ts`'s two `NsfwProbabilities`-into-`Record<string, number>` sites feed `ImageClassificationDetails.nsfwProbabilities` (`types/imageClassification.ts:40`), untouched on purpose — deliberately free-form on the wire (the actual hole #8 closed; server-validated, length-capped). `NBackGame.tsx`'s `Pressable` carried `delayPressIn={0}` — `Pressable` has no such prop (`TouchableOpacity`'s prop; `Pressable`'s equivalent is `unstable_pressDelay`), silently ignored since it was written — deleted rather than swapping in the real prop, which would be an actual timing change to a screen with no render harness (Tier-4 #23). `package.json` has no `tsc` script and there is no CI (Tier-4 #21) — nothing branches on `tsc`'s exit code 2.
  - **949 (fixed, same session as the R1 decision below) — `postProcessedCategory` narrowed `string` → `VisionRiskCategory`.** Turned out to be the opposite of the risk-path validation gap it looked like on first read: the variable (`useScreenshotCapture.ts:902`) has exactly two write sites, `= 'neutral'` and `= postProcessed.finalCategory` (itself typed `VisionRiskCategory`), both always members of the 7-value union — the overly-wide `: string` annotation was the only reason the error fired, not an unvalidated value reaching `enforceCategoryConsistency`. One-line fix (`let postProcessedCategory: VisionRiskCategory;` + a `VisionRiskCategory` type import), zero runtime change.
  - **`useScreenshotCapture.ts:1041` / `:1046` — 2 remaining, deferred on purpose, not by default.** Two bare `return;` inside `processCapturedFrame` (the `shouldSkipScreenEventReporting` skip and the `!isMonitoringRef.current` skip), the only two exits in the function that don't return a `CaptureCycleResult` object. **Traced to their sole consumer:** `processCapturedFrame(event).then(onCycleComplete)` (`useScreenshotCapture.ts:1688`) → `onCycleComplete`'s only implementation (`ScreenMonitor.tsx:56`, `setLastResult(result)`) → 2 optional-chained render reads (`:318`, `:353`). Today `undefined` fails both chains, so the Monitor card is **sticky** — it keeps showing the last real result through these skips rather than crashing. **Deferred, deliberately, not because it's unclear how to fix it:** returning real `{success:false, skippedReason:'...'}` values would make "Cycle skipped: …" start appearing on that card — and rehearsal-log evidence (below) shows the `shouldSkipScreenEventReporting` path alone fires ~once/minute of normal use (8 times in a 9-minute session), so this is a visible, frequent change to a demo-facing card, not a quiet one. The two new `skippedReason` variants and the fix land **post-defence**. `switch`-exhaustiveness on `skippedReason` should be checked before adding them (new `tsc` errors at any exhaustive-switch site are the type system doing its job, not a regression — handle them, don't widen the union away).
  - **R1 — "could the app score its own screens?" Investigated against the 2026-09-14 rehearsal logs + `keywordFilter.ts`'s term lists. Decision: no escalation; #57 filed as a partial fix, not a closed hole.**
    - **Rehearsal logs (`briefs/device-session-logs/rehearsal-2026-09-14-{backend,logcat}.txt`):** 0 of 36 reported `Screen event stored` rows carry `appPackage: com.mobileapp`. Separately, `Screen event skipped — mission / game UI OCR` (the `shouldSkipScreenEventReporting` gate) fired **8 times** in ~9 minutes; the monitoring-stopped-mid-OCR skip fired **0 times**. All 8 OCR previews behind the skips are unambiguously SafeGuard's own screens (home/monitor "SafeGuard is watching…", Missions/Rewards/Badges list, Profile/points, paused-monitoring screen) — the heuristic held on every instance actually exercised. **Caveat that changes what the "0" is worth:** the very first skip (18:41:56) was attributed by UsageStats to `com.android.settings` while the OCR unmistakably showed SafeGuard's own paused screen — package attribution is unreliable enough that a package-based check alone would have missed this one; the **text** gate is what actually caught it. Say "the text gate held on all 8 instances, and package attribution wasn't the reason," not a bare "0 events."
    - **Keyword scan:** mission list-card copy (titles/descriptions in `MissionListScreen.tsx`) is clean. The `media_violence` quiz bank (`MobileApp/src/missions/games/quizBank.ts`, mirrored in `backend/src/constants/quizStaticBank.ts`) is not — *"Why is it harmful to share violent or **gore** images?"* — `'gore'` is an exact `\bgore\b` hit against `VIOLENT_TEXT_KEYWORDS` (`utils/keywordFilter.ts`), which alone would score `riskFlag:true, category:'violent'` and, via `applyExplicitOcrBoost`/`applyPostProcessingOverride`, floor the combined score from text alone. **Reachability traced, not assumed:** that text only ever renders inside `MissionScreen.tsx` (source `'screen'`) or the native overlay (source `'overlay'`/`'fallback'`), and all three paths route through `presentMissionFromCapture.ts`, which calls `beginMissionCaptureSession()` **before** presenting any of them — checked at the very top of `processCapturedFrame` (`isMissionCapturePaused()`), independent of and earlier than the Part-B skip gate. No screenshot is requested while that lease is held, so the string is real but was found unreachable through this path in the trace performed.
    - **One path the trace didn't close, checked at the code level (no device):** `checkMissionCaptureSessionBackstop()`/`forceEnd()` (`missionCaptureSession.ts:107-140`) are unconditional — nothing checks whether `MissionScreen`/the overlay is still mounted before releasing the lease, and nothing re-`begin()`s it afterward; `MissionScreen.tsx` has no listener for an externally-revoked lease. The 10-minute backstop has exactly two call sites, both confirmed by reading the code, not assumed from prior docs: `handleA11yWindowChanged` (`useScreenshotCapture.ts:437`, calls `checkMissionCaptureSessionBackstop()` as its **first line**, before the `OWN_PACKAGE`/`SAME_PACKAGE` filter or any other guard — so it does **not** require a real app switch, any raw `TYPE_WINDOW_STATE_CHANGED`/`TYPE_WINDOWS_CHANGED` accessibility event is sufficient) and `useMissionOverlayListener.ts:223` (`AppState → 'active'`, foreground-only, irrelevant to a foregrounded-the-whole-time session). There is no third, timer-driven site — the backstop is purely event-driven, so if literally zero a11y window events and zero background/foreground transitions occur in a 10+ minute window, `checkBackstop()` is never even called and the lease survives. **What's confirmed vs. what isn't:** confirmed — if the check ever fires while the mission is still displayed, the release is unconditional and nothing re-establishes the lease. Not confirmed from code alone — how likely a real 10-minute foregrounded quiz dwell is to produce zero qualifying a11y events; system dialogs, notification heads-up windows and IME appearance are plausible sources on a live device but this wasn't exercised on one. **If the backstop does fire mid-quiz, the consequence is bounded, not a runaway:** `hasRecentRiskyMission` blocks a new mission while one is already pending (the quiz itself), so no mission-about-a-mission loop — the actual cost would be one stored screen event scored `violent` off the app's own text, polluting the child's risk history / 7-day average / `weeklyRiskyCount`. Filed as a rider on #57, not implemented. **Superseded: fixed as ALL_IS_FIXED #58** (see that bullet) — `checkBackstop()` is no longer a pure age-since-`begin()` check; see #58 for what changed and what remains device-unverified.
  - **#57 (own-app capture policy gap) — filed, not implemented, and filed as partial by construction.** `getAppCategory()` has no entry for `com.mobileapp`; unlike `game`/`system` (interval 0) or `education` (floored 120s), the app's own package falls through to `'default'` and keeps the unrestricted periodic interval — periodic ticks keep running (and discarding their result at the `shouldSkipScreenEventReporting` gate) while the child sits on Missions/Badges/Monitor. A `getAppCategory()` entry would cut that wasted OCR+vision work. **It would not close the text-gate's blind spot** — per the 18:41:56 misattribution above, the policy entry keys off the same foreground-package signal that was wrong in that sample; a tick misattributed to another package while showing SafeGuard's own screen would not be suppressed by a package-based rule at all. Write this into #57's own file before implementing it, so a future session doesn't ship the interval fix and conclude the hole is shut. Deliberately **not implemented before the defence** — it changes capture behavior on the exact path the demo runs, for a benefit (fewer wasted cycles) that's pure efficiency; right the week after, not this week.
  - **Verify (949 landing, this commit):** `npx tsc --noEmit` → exactly the 2 above, quoted. `npm test` → **440/34 unchanged.** Lint: blob-level delta on `useScreenshotCapture.ts` (`git show HEAD:<path>` swapped in place, linted, swapped back) → **0/0 delta** (1 pre-existing error / 17 pre-existing warnings, unchanged); file had 0 CR bytes, no CRLF drift this time. Backend untouched. Gradle **not run** — JS-only.
- **DB**: plain SQL migrations in `src/db/migrations/NNN_*.sql`, applied in filename order by `db/migrate.ts` and tracked in the `schema_migrations` ledger. `db/pool.ts` exports the `pg` pool + `query`. No ORM. Add schema changes as a **new numbered file** (zero-padded 3 digits) — never edit an applied one; the ledger now **enforces** this by checksum (a changed applied file aborts the run). Each file runs inside a transaction, so `CREATE INDEX CONCURRENTLY` / `ALTER TYPE … ADD VALUE` won't work in a migration.

## Mobile architecture

Entry: `App.tsx` → dev JWT bootstrap (`useDevChildToken`) → `NavigationContainer` → `AppNavigator` (`src/navigation/`). `RealUsageTracker` runs alongside the UI to report usage.

- **Native modules** (`src/native/*.ts` bridge to `android/.../com/mobileapp/`): `ScreenCapture` (MediaProjection foreground service), `ForegroundApp` (UsageStats — which app is in front), `NsfwTflite` (on-device NSFW), `OverlayMission` (draws a mission overlay over other apps), `overlayPermission`, `SafeGuardAccessibility` (AccessibilityService window/IME/scroll events — see below).
- **On-device analysis pipeline** (`src/services/`): OCR via `mixedScriptOcr.ts` / `mobileArabicOcr.ts` (ML Kit + UI-noise filter + Arabizi/Derja normalization), `nsfwClassifier.ts` + `imageClassifier.ts` (image risk), `keywordFilter.ts` (EN/FR/AR/Tunisian-Derja multilingual). Results combine into a `combinedRiskScore` that drives capture cadence and mission triggering.
- **Adaptive capture** (documented in `README.md`): capture frequency is risk-based, not fixed — app-switch triggers + a follow-up + a periodic fallback. App-switch triggers are **accessibility-window-driven** when the SafeGuard accessibility service is connected (near-instant), falling back to the 1s UsageStats poll otherwise (see the accessibility bullet below). **Periodic fallback (A3c-2): one native-driven clock.** The native capture loop no longer captures on its own timer — it emits `onNativePeriodicTick` (bare `{timestamp}`) every `NATIVE_TICK_INTERVAL_MS` (**5s as of A3c-3**, decoupled from `RISK_INTERVAL_HIGH_MS`) from the same `Handler.postDelayed` loop on `ScreenCaptureThread`, Doze-resistant because the `mediaProjection`-type foreground service keeps the process unfrozen for the whole session (no `AlarmManager`/`WorkManager`). **A3c-3 tick-rate fix:** A3c-2 set the tick to 10s (`= RISK_INTERVAL_HIGH_MS`) and claimed subsampling would realize every effective interval — false, `15000 % 10000 ≠ 0`, so a 15s browser_social target quantized up to the 20s tick (device-confirmed 33% slower browser scanning, a regression). 5s divides 10/15/20/120 exactly; native floor is a strict `intervalMs < 5_000` so 5000 passes with **no native change**. Expect a minor uptick in one-tick `DEBOUNCED`/`interval_floor` logs (5s tick landing on the 5s coordinator/native floor — self-correcting, non-cumulative, A3c-1-observable). **Device-confirmed 2026-09-02 ("Smoke B", first measured this session):** ~3 min of continuous a11y-driven Instagram scrolling produced 9 consecutive `periodic_fallback` captures at **exactly 15s** (`browser_social` clamp), zero quantization to 20s — the regression this fix targeted does not reproduce. `useScreenshotCapture` subsamples each tick via `shouldEmitPeriodicCapture(now, lastPeriodicPassAtRef, dynamicIntervalMsRef)` — pass only if `≥ dynamicIntervalMsRef` elapsed since the last pass and the target `> 0` — then routes survivors through `tryCaptureNow('PERIODIC_FALLBACK')`, the same coordinator door as every other reason (so periodic capture now gets keyboard-suppression, mission-pause, the 5s debounce and A3c-1 rejection handling, which the old direct-capture loop bypassed). `dynamicIntervalMsRef` is the effective adaptive interval from `computeEffectiveAdaptiveInterval` (`adaptiveCapture.ts`, unchanged): risk mean `>70→10s / >30→15s / else 20s`, then per-category clamp via `appCapturePolicy.ts` (`browser_social→≤15s`, `game`/`system→0` = periodic disabled, `education→≥120s`). The old JS `setTimeout` adaptive loop (`resetPeriodicTimer`/`scheduleTick`, `CaptureReason.PERIODIC_ADAPTIVE`) is **retired**; `PERIODIC_ADAPTIVE` removed from the enum. Invariant: **higher risk must scan at least as often as lower risk** (HIGH ≤ MEDIUM ≤ LOW interval). **Liveness backstop (A3c-2c):** the tick handler runs `decideTickAction(...)` (pure, `adaptiveCapture.ts`) *before* the subsample gate — if the JS OCR lock (`isProcessingRef`) has been held past `OCR_LOCK_LIVENESS_MS` (60s, **12 ticks** post-A3c-3, comfortably above the ~40s worst-case legit `processCapturedFrame` = 2.5s foreground + 25s vision + 12s POST) it calls the extracted `forceReleaseProcessingLock('tick-liveness')` and returns; otherwise it subsamples as before. **A3c-3 scroll-settle** rides the same `noop` ticks: on a tick that neither force-releases nor emits periodic, the handler runs pure `decideScrollSettle(...)` and may `tryCaptureNow('SCROLL_SETTLED')` (see accessibility bullet). This is the **only** stale-lock recovery that survives backgrounding — the in-frame `setTimeout`/`setInterval` watchdogs are RN-frozen there. **Scope correction (D3 device smoke, 2026-09-03):** "survives backgrounding" means *app-backgrounded, screen-on* only — device-measured firing at `elapsed≈60000` there, but at `elapsed:195233` (i.e. only on screen-on thaw) when the screen was slept. MIUI freezes the RN JS thread on screen-off despite the FGS, so a frame that wedges while the phone is locked is unrecovered until the screen wakes. See D1 follow-up debt item 5. See frame-skip-gate Debt (4).
- **Capture coordinator** (`src/capture/captureCoordinator.ts`): every capture trigger in `useScreenshotCapture.ts` (`tryCaptureNow`, `triggerAppSwitchCapture`, follow-up, periodic tick, deferred re-issue) **must call `coordinatorRef.current.requestCapture(reason: CaptureReason)` before invoking native `captureNow`**, and route coalesced retries via `takePendingReason()`. The coordinator is a pure factory (`createCaptureCoordinator(deps)`, no React/native imports, injected clock) owning: request debounce (`CAPTURE_DEBOUNCE_MS` 5s, `FOLLOW_UP_MIN_GAP_MS` 2s for `APP_SWITCH_FOLLOW_UP` — both now live only here), mission-pause gating **for requests**, keyboard-suppression gating **for requests** (`setKeyboardVisible(bool)`; while true, `requestCapture` drops **only** the three `isKeyboardSuppressibleReason` reasons — `PERIODIC_FALLBACK`, `CONTENT_CHANGE`, `SCROLL_SETTLED` — returning `CaptureSkipReason.KEYBOARD_SUPPRESSED`; every app-switch reason, `BROWSER_NAVIGATION`, `RISK_FOLLOW_UP` and `APP_SWITCH_FOLLOW_UP` still pass so safety monitoring never stops while typing), and priority-based coalescing of one pending reason while OCR is busy (`requestCapture` decision order: `NOT_MONITORING` → `MISSION_PAUSED` → `KEYBOARD_SUPPRESSED` → `BUSY_DEFERRED`/`SUPERSEDED` → `DEBOUNCED` → allowed). `isKeyboardSuppressibleReason` is the **only** place that three-reason list is written (unit-tested over all 11 members); `reset()` also clears `keyboardVisible`. `CaptureReason` has 11 members (A3c-2 removed `PERIODIC_ADAPTIVE`; `PERIODIC_FALLBACK` wired to the native tick; `SCROLL_SETTLED` wired in A3c-3, tier-2, keyboard-suppressible, non-force); 4 (`MISSION_RESUME`, `RISK_FOLLOW_UP`, `CONTENT_CHANGE`, `BROWSER_NAVIGATION`) are declared for later wiring, and `CAPTURE_REASON_PRIORITY` leaves value 4 unused where `PERIODIC_ADAPTIVE` was. **Native-rejection handling** (A3c-1): the hook subscribes to the `onScreenCaptureRejected` bridge event (`{reason, elapsedMs?, timestamp}`, `reason` = `NativeRejectionReason` `'interval_floor' | 'busy'`) and calls `coordinator.onNativeRejected(reason)`. That method **never advances `lastAcceptedAtMs`** (no frame was accepted) and never touches `pendingReason`; for `'interval_floor'` it clears the coordinator's **force-arm mirror** (`forceArmedReason`, exposed via `isForceArmed()`), which is set whenever `requestCapture` allows an `isForceCaptureReason` reason and also cleared by `onFrameAccepted` and `reset()`. `'busy'` is log-only (an in-flight native frame consumes the real flag). Bridge events are not timer-dependent, so this still lands after RN unfreezes JS timers on foreground. **Inbound-frame guards stay in `processCapturedFrame`** — the OCR lock (`isProcessingRef`), generation token / `isActive()`, `OCR_LOCK_TAKEOVER_MS`, both watchdogs, the A3c-2c tick-liveness backstop (which force-releases the lock from the native tick when backgrounded — see the adaptive-capture bullet), and the inbound `isMissionCapturePaused()` check are NOT the coordinator's job (frames can still overlap/arrive stale even though every `captureNow` is now JS-initiated post-A3c-2). **Owed-force debt (A3d — never drop a tier-0 intent):** device evidence showed a genuine app switch (e.g. launcher → search → Instagram, both switches inside 5s) has its *second* switch silently `DEBOUNCED` — `requestCapture` returned before `tryCaptureNow` ever reached `forceNextCapture()`, so the new app's hash-gate baseline was never established and every capture for the rest of that session (55s observed) hit `frame.skipped.unchanged` against the *previous* app's frame. Fix: `requestCapture` now tracks a private `forceCaptureOwed` boolean (no TTL — `HASH_STALENESS_MS` 90s is already a backstop) — a `DEBOUNCED` decision for an `isForceCaptureReason` reason sets it; the *next* allowed decision of **any** reason consumes it and returns `force: true` (new field on the `CaptureDecision` allowed variant, replacing the hook's old direct `isForceCaptureReason(decision.reason)` check — `tryCaptureNow` now reads `decision.force`). `onNativeRejected('interval_floor')` **re-owes** the debt whenever `forceArmedReason` was set at rejection time (covers both a paid-down debt and a direct tier-0 reason that native's own floor rejected) — this deliberately reverses the pre-A3d assumption that a floor-rejected tier-0 attempt is simply gone; see the rewritten test in `captureCoordinator.test.ts`. `resumeCapture` does **not** clear the debt (a switch missed before a mission pause is still missed after it); `reset()` does. No new `CaptureReason`, no native change — `forceNextCapture()` already was a one-shot hash-gate bypass; the fix is *when* it gets armed. A parallel "blank/stale capture" hypothesis (raised by the same device evidence: hamming=1 five times against a non-blank reference frame is suspicious on its own) was **ANSWERED, closed 2026-09-02, negative.** The MediaProjection stale-token native pass added the raw dHash value to the `frame.skipped.unchanged` log; the next rapid-switch-then-scroll repro (this time with the accessibility service genuinely bound, confirmed via `a11y.window.accepted`) reran the exact scenario — ~3 min of active Instagram scrolling, 9 consecutive periodic captures, **zero `frame.skipped.unchanged` in the window, every capture a fresh distinct frame.** The dead/stale-capture-source hypothesis does not hold; do not re-open absent new device evidence. Pure logic covered by `__tests__/captureCoordinator.test.ts`.
- **Mission capture session lease (ALL_IS_FIXED #2)** (`src/utils/missionCaptureSession.ts` + `useScreenshotCapture.ts` + `useMissionOverlayListener.ts` + `presentMissionFromCapture.ts` + `MissionScreen.tsx`): while a mission blocks the screen (overlay or `MissionScreen`), capture must stay paused — `isMissionCapturePaused()` gates `captureCoordinator`'s `MISSION_PAUSED` check above, plus several early-returns in `useScreenshotCapture.ts`. Was a bare `sessionDepth` counter with no identity and only one balanced-decrement call site (`endMissionCaptureSession`, confirmed dead — zero callers besides its own definition — removed); every real release went through unconditional `forceEndMissionCaptureSession()`. **Now a single nullable lease** `{missionId, source: 'overlay'|'screen'|'fallback', startedAtMs}`, still a pure module-level singleton — bare exports delegate to one `createMissionCaptureSessionTracker(deps)` instance, the same factory-with-injected-clock shape as `a11yHealth.ts`, so the test file injects a fake clock without touching any caller. `begin(missionId, source)` is idempotent for the same id+source (no-op); a source change (the overlay→`MissionScreen` handoff) **re-stamps** `startedAtMs` to now rather than preserving the original time — a source change is evidence of a live owner, and preserving the original stamp risked the backstop firing mid-mission. `forceEnd()` stays unconditional and id-agnostic **by design** — every real release site means "this mission flow is over, whatever it was"; an id-scoped release would fail toward a wedge, unconditional release fails toward more monitoring, the correct direction. `missionId` exists for idempotence and diagnostics, not scoped release.
  **Backstop (age model superseded by ALL_IS_FIXED #58 — see that bullet for the current `lastTouchedAtMs`/hard-ceiling/overlay-query design; this paragraph describes the original, since-revised mechanism):** a lease older than `MISSION_SESSION_MAX_MS` (10 min) force-releases via `checkMissionCaptureSessionBackstop()`, called from the top of `handleA11yWindowChanged` (above its guards, so it runs even while mission-paused) and from `useMissionOverlayListener`'s `AppState → 'active'` listener. 10 minutes was chosen against `presentMissionFromCapture`'s own re-presentation timing (90s debounce, 60s re-surface window), not mission length — a tighter ceiling risked the backstop colliding with re-presentation and causing a double-present loop mid-demo, worse than a slow session. **Guarantee (corrected from an earlier draft of this fix):** the a11y-window signal is `DeviceEventEmitter`-driven and survives backgrounding on screen-on — it is not foreground-gated (the 1s UsageStats poll is the thing that's foreground-gated, not this feed); the `AppState → 'active'` signal is foreground-only. The backstop does **not** fire while the a11y service is down and the app never foregrounds, and not at all while the screen is off (JS thread frozen — same MIUI limitation as the A3c-2c tick-liveness backstop). Interacts with ALL_IS_FIXED #1: an a11y drop coinciding with a wedged session means only foregrounding recovers it. This is a lease expiry on a known, timestamped fact, not an inference from silence — it does not contradict `a11yHealth`'s "no time-based decay" rule, which is about inferring service *health* from the *absence* of a signal; a bounded mission session is a different problem.
  **Owed-resume debt:** `forceEnd()`/backstop expiry attempt to resume native capture via the registered handler; if none is registered at that moment, the tracker sets an internal `resumeOwed` flag instead of silently dropping the resume (the old `resumeCaptureFn?.()` no-op). `payOwedMissionCaptureResume()` pays it the next time a handler is available — called from the registration effect (mount/remount) **and** both backstop-check sites, so a remount-incurred debt isn't stuck waiting for a mount that may never come. Same shape as `captureCoordinator.ts`'s A3d `forceCaptureOwed` debt. **Any** successful resume — ordinary or debt payment — clears the flag, so a stale debt from an old wedge cannot cause a later, unrelated resume to double-fire (double-resume would re-stamp `missionEndedAtRef`, spuriously suppressing mission presentation for a grace window, and drop a live `scrollSettleStateRef` arm). **How often this actually triggers:** `pauseCapture`/`resumeCapture` (registered via this effect) both became fully stable callbacks in this fix (see below), so the unregistered window this debt exists for now only opens across a full RN root remount or dev Fast Refresh — not routine monitoring toggles. Mark this **lightly-exercised**, not proven — same treatment as `onPollTick`/`POLL_FREEZE_GAP_MS` in #1.
  **Reset-site gap closed:** `resetMissionCaptureSession()` previously ran only from `stopMonitoring()`; it now also runs from `startMonitoring()`'s and `handleMonitoringRevoked()`'s reset blocks, matching the established three-site pattern already used for `windowEventFilterRef`/`a11yHealthRef` (start/stop/revoke). Before this fix, a lease left open by a MediaProjection revoke survived the revoke and then survived the user's re-enable — `startMonitoring` succeeding while `isMissionCapturePaused()` was still true immediately re-paused native capture via the existing re-pause effect — a wedge that survived the one recovery action (toggle off/on) the whole design relies on.
  **Real behavior change (disclosed, not an inert refactor):** `pauseCapture` now guards on `isMonitoringRef.current` instead of the `isMonitoring` state value, matching `resumeCapture`'s existing pattern — a pause call that previously could silently no-op through a stale closure now actually pauses native capture.
  **Also fixed:** `useMissionOverlayListener.ts`'s generic overlay-failure catch branch called `scWarn` without importing it (`ReferenceError`, one of the 9 pre-existing `tsc` errors, now 8) — every unrecognized `executeMissionAction` failure (e.g. a network error) hit this and skipped its own `forceEndMissionCaptureSession()` call; fixed by adding the import.
  **`OverlayEventBridge` has no teardown-driven release signal** (verified reading `OverlayService.java`): `onDestroy()`/`removeOverlayNow()` never emit or buffer an `onOverlayMissionAction` event — only explicit user taps inside the overlay UI do. An overlay killed by the OS mid-mission (memory pressure, task kill) has no deterministic release; it's backstop-covered only, not closed.
  **Filed, not fixed here:** (1) Tier-3 — `useMissionOverlayListener.ts`'s `handlingRef` is set `true` and cleared only in a `.finally()`; if `handleOverlayAction` never settles (ties to the `withTimeout`/non-settling-promise family below), the flag stays `true` forever and every subsequent overlay action is dropped — backstop-covered only, same as the overlay-teardown case above. (2) ALL_IS_FIXED #2b — surface an open mission-capture lease + its age on the Monitor card, one-glance parity with the a11y health card; this brief bounds the wedge but doesn't make it visible while it lasts.
  **Device smoke (2508CRN2BE, closed same-day once a device became available — all required arms passed):** 363 mobile tests incl. 14 new pure-module cases in `__tests__/missionCaptureSession.test.ts`, `tsc` 8/8 errors, no new rule categories. A temporary `DEBUG_WEDGE_MISSION_SESSION`-equivalent scaffold (three buttons on the Monitor card: stage a wedge via `beginMissionCaptureSession('debug-wedge','screen')`, trigger `handleMonitoringRevoked()`, and read `isMissionCapturePaused()` directly) was added for this session only, exercised, then fully reverted (`git checkout` on the two touched files) before the follow-up commit — confirmed gone by a clean post-revert relaunch.
  **Lint delta root-cause (2026-09-05), corrected once more after an initial overclaim:** the originally-reported +377 `prettier/prettier` / -1 `no-void` traced to two Write-tool-authored files (`missionCaptureSession.test.ts` +222, `missionCaptureSession.ts` +144) sitting as CRLF **on local disk** while every sibling file (`a11yHealth.ts`/`.test.ts`) and this project's Prettier config (no `endOfLine` override, defaults to `lf`) expect LF — confirmed by direct byte inspection (`grep -c $'\r'`), and independently reproduced (measured +377/-1 exactly) via a clean before/after that swaps only the touched files to their pre-fix content in place rather than `git worktree add`-ing a second checkout (the latter re-materializes *every* file, touched or not, through the `autocrlf=true` smudge filter and produces a false repo-wide delta — observed firsthand, a `-6549 prettier/prettier` phantom across ~100 untouched files; don't use that method for this class of check). **Correction: this was never a git-history/committed-content defect.** `git cat-file -p 660940d:<path>` on both files shows the *stored blob* was already LF, byte-identical to `a11yHealth.ts` — `core.autocrlf=true`'s clean filter had silently normalized the CRLF working-tree content at the original `git add`/`commit` time, same as it does for every text file in this `.gitattributes`-less repo. The CRLF was purely a stale local working-tree artifact (the two files sat untouched on disk after that commit; `git commit` never rewrites your working tree to match what it stored) — real, and correctly flagged by a lint run against that disk state, but not something that would show in `git diff`, not something baked into repo history, and no worse than the ambient, already-documented, out-of-scope CRLF condition affecting the rest of this repo (the "~6961 pre-existing prettier/CRLF problems, no `.gitattributes`" note this file already carries). Ran `dos2unix` on both files to fix the local disk artifact — confirmed via `git diff`/`git add` that this changed **zero** stored bytes for `missionCaptureSession.test.ts` (blob was already exactly this content) and contributed nothing to `missionCaptureSession.ts`'s diff either; the only real, permanent git-level change was a separate `eslint --fix` pass correcting a handful of pre-existing bracket-spacing/line-wrap nits already present in that file's LF-stored blob, unrelated to line endings (18 lines, commit `9127cb9`). Lesson: when `grep -c $'\r'` disagrees with a sibling file, check `git cat-file -p <rev>:<path>` before concluding the *repository* has a problem — `autocrlf=true` can make a working tree lie about what's actually stored, in both directions.

- **Arm 4a (toggle OFF/ON mid-wedge) PASSED.** Staged a wedge, confirmed capture activity went fully silent (zero `capture.*`/`frame.phase` logs for a full periodic cycle — direct device confirmation of the Q1 finding that the native tick stops rescheduling itself while paused, not just a code-reading inference), toggled monitoring off then on, confirmed `capture.allowed` resumed immediately with no `MISSION_PAUSED` skip.
- **Arm 4b (revoke→restart) — fix target PASSED, full native-restart arm inconclusive by design of the scaffold.** Triggering `handleMonitoringRevoked()` while wedged, then reading `isMissionCapturePaused()` directly, confirmed `false` immediately after — `resetMissionCaptureSession()` **does** run in the revoke path now and **does** clear the lease, exactly the §3b gap this brief fixed. Native capture did not visibly resume afterward in this same test, but that's **not attributed to the fix**: calling `handleMonitoringRevoked()` directly only flips JS-side state without the native-side teardown a real `MediaProjection.Callback.onStop` performs first, so `startCapture()` was re-called against native state that still believed a session was already running — a scaffold-fidelity gap (would need the native debug-method pattern used to originally verify A3c-2b, a native change, out of this JS-only brief's scope), not a regression. The lease-clearing behavior itself — this brief's actual target — is device-confirmed.
- **Arm 1a (backstop via a11y window events, backgrounded + screen-on, SafeGuard never foregrounded) PASSED cleanly.** Pre-flight: `a11y.window.accepted` observed on a real app switch before staging. Staged a wedge, backgrounded SafeGuard, switched between other apps (home/calculator) every ~60s for ~12 min with SafeGuard never brought forward. Backstop fired at `ageMs: 646230` (~10.8 min, past the strict 600000ms threshold) via `a11y.window.accepted { from: 'com.miui.home' }` — not an `AppState` transition. Capture resumed on the very same event (`app_switch_launcher_return`, `force: true`) and continued firing normally (`periodic_fallback` every ~20s) for minutes afterward, confirming the native tick chain genuinely restarted, not just a log line. This is the actual defence-day scenario (a wedge recovering while the child keeps using the phone, SafeGuard never touched) and it works.
- **Arm 1b (backstop via `AppState → 'active'`, backgrounded + screen-on, zero app switches) PASSED.** Staged a fresh wedge, backgrounded with no switches at all for ~11 min (confirmed zero capture/backstop log activity throughout — the negative case: the backstop does not fire spontaneously without one of its two trigger signals), then foregrounded SafeGuard once. Backstop fired immediately at `ageMs: 745045` (~12.4 min) via the `AppState` path (no `a11y.window.accepted` beforehand), capture resumed immediately after.
- **Arms 2/3 (real mission trigger) deferred — as anticipated.** No dev route exists yet to force a mission type (ALL_IS_FIXED #6, not done) and mission generation is cooldown-gated; the Missions tab showed only the general mission library, not an actively risk-triggered overlay/screen presentation. Per the prior decision, #6 was not reordered ahead of this Tier-1 fix — these two arms remain open for a future opportunistic run or after #6 lands.
- **Gotcha for future smoke sessions:** the OS dropped `enabled_accessibility_services` back to `null` at least twice this session after unrelated `am force-stop`/relaunch cycles (not a real service crash — `dumpsys` showed simply absent, not `Crashed`), matching the already-documented ROM inconsistency; re-verify with `a11y.window.accepted` after every relaunch, not just once at session start. The device also auto-rotated to landscape unattended mid-session (no input from the test) — screenshot-derived tap coordinates do not survive a rotation; re-screenshot before tapping if a wait step could have triggered one. `adb shell svc power stayon usb` was set for the two backgrounded-screen-on arms and unset again at the end of the session.

**`adb shell svc power stayon usb` is the correct tool for the two backgrounded-screen-on backstop arms specifically** (both need ~10 real minutes without the screen sleeping, which would otherwise manufacture a false screen-off-freeze result) — this is a deliberate exception to the general rule against it (that rule exists for freeze/*thaw* arms, which this isn't); unset it before anything that tests screen-off behavior, and note it only proves survival while on USB power, not Doze-on-battery.
- **Native frame-skip gate** (`android/.../screencapture/FrameHasher.java` + `ScreenCaptureModule.captureSingleFrame`): before any JPEG write / bridge crossing / OCR, the module computes a 64-bit perceptual difference hash (dHash) directly from the acquired `Image`'s plane-0 `ByteBuffer` (absolute reads only — no Bitmap, no buffer copy; uses `image.getWidth()/getHeight()`, not the module's `screenWidth/Height` fields) and compares it to the last **processed** frame. A frame is **processed** iff: `forceNextCapture` flag was set (one-shot, consumed via `getAndSet(false)`) **OR** no prior hash **OR** `now - lastProcessedAtMs >= HASH_STALENESS_MS` (90s) **OR** `hammingDistance > HASH_CHANGE_THRESHOLD` (3). Otherwise the frame is dropped: no JPEG, no `onScreenCaptured`, `lastCaptureEmittedAtMs` untouched — only a `frame.skipped.unchanged hamming=<n> hash=<hex>` debug log (an `onScreenCaptureLog` bridge event, string only; the `hash=` field is a **temporary** stale-token-brief diagnostic — strip it with the log line before the defence build). The hash lives in native memory **only** — never filed, never in the screen-event payload, never sent to the backend. Hash state is reset in `releaseProjectionSession()` so every new monitoring session processes its first frame. `@ReactMethod forceNextCapture()` (bridged in `native/ScreenCapture.ts`) sets the one-shot bypass flag; `useScreenshotCapture.tryCaptureNow` calls it fire-and-forget **before** `captureNow()` whenever `isForceCaptureReason(decision.reason)` is true. **Tier-0 (force-capture) reasons** — `APP_SWITCH`, `APP_SWITCH_LAUNCHER_RETURN`, `APP_SWITCH_DEFERRED`, `BROWSER_NAVIGATION` — always bypass the hash check; `isForceCaptureReason` in `captureCoordinator.ts` is the single source of that list (explicit literal, not derived from `CAPTURE_REASON_PRIORITY`), unit-tested for all 11 `CaptureReason` members. **Orphaned-flag close (A3c-1):** when `captureNow` rejects on `MIN_CAPTURE_INTERVAL_MS` it now `forceNextCapture.set(false)` itself **and** emits `onScreenCaptureRejected {reason:"interval_floor", elapsedMs}` so the JS coordinator clears its mirror too — the bypass can no longer survive to an unrelated later frame. The `isFrameInProgress` busy-reject emits `onScreenCaptureRejected {reason:"busy"}` (no `elapsedMs` key) but does **not** clear the flag (the in-flight frame consumes it via `getAndSet(false)`). `MIN_CAPTURE_INTERVAL_MS` is checked only in `captureNow(Promise)`. On-device verification scope: `onScreenCaptureRejected` + native self-clear are confirmed on-device for `interval_floor` (`elapsedMs` matches the native debounce log exactly, both observed instances); the *armed*-flag-cleared-on-reject path is unit-test-verified only — both live rejections were `appstate_background` (`forceArmedReason: null`), since the coordinator's own 5s debounce normally shields native from ever seeing a tier-0 reason inside its floor. **A3c-2:** the periodic loop calls `emitNativePeriodicTick()` instead of `captureSingleFrame()`; `captureSingleFrame()` (and its hash gate) now runs only from `captureNow()`. **Debt (not fixed here):** (1) `lastCaptureEmittedAtMs` is a plain `long`, written on the main thread (`emitScreenCaptured`) and read on the bridge thread (the `captureNow` floor check) — pre-existing cross-thread visibility gap; make it `volatile` next time this file is touched. (2) **DONE (2026-09-03).** `README.md`'s adaptive-capture section + reference tables were reworked to the single native-tick + JS-subsample model (new "Wedged-frame recovery" table covering tick-liveness / D1 per-phase deadlines / D3), and `docs/` (`architecture.md` ADR-5 + Reliability, `SRS.md` FR-CAP-7 / NFR-REL-1, `PREFINAL_REPORT.md`, `testing_strategy.md`, `scrum_artifacts.md`) were aligned in the same pass — including test counts (mobile 181→330 / 23→26 suites; totals 310/380→459). (3) `useScreenshotCapture.ts` has accumulated three briefs' worth of event-reaction logic — A3c-1 `onScreenCaptureRejected` handling, A3c-2 tick-subsample wiring, A3c-2b `onMonitoringRevoked` handling — covered **only by device smoke** (no hook test harness exists, and each brief re-cites that as the reason to skip JS tests). A3c-2c extracted the tick path's decision into the pure `decideTickAction` reducer (`adaptiveCapture.test.ts` covers it), but `onScreenCaptureRejected` and `onMonitoringRevoked` are still device-smoke-only; a hook harness or pure reducer for those two deserves its own brief before the defence. (4) **Backgrounded `processCapturedFrame` hang = silent monitoring death — self-heal shipped (A3c-2c); root cause still open.** If any `await` inside `processCapturedFrame` never settles *while SafeGuard is backgrounded*, `isProcessingRef` stays latched and the coordinator `SUPERSEDED`s every capture; the `setTimeout`/`setInterval` watchdogs and the next-frame `OCR_LOCK_TAKEOVER_MS` check are all dead (RN-frozen / no next frame). Device-observed 2026-09-01: a frame hung ~477 s (`{elapsed: 476997}`) in the vision pass, silent until foreground. **A3c-2c** makes the system self-heal — `onNativePeriodicTick` keeps firing while backgrounded (native `Handler.postDelayed` loop, Doze-resistant via the FGS), so its handler runs `decideTickAction` **before** the subsample gate and, past `OCR_LOCK_LIVENESS_MS` (60s), calls `forceReleaseProcessingLock`. The backstop is **broader than its name** — it recovers **any** wedged `await` in `processCapturedFrame` that latches the lock, not just OCR. **Device-verified 2026-09-01:** smoke #1 (staged hang, `DEBUG_SIMULATE_OCR_HANG`, since removed) force-released 3× while backgrounded, every one `cause: 'tick-liveness'` — confirming a `DeviceEventEmitter` listener runs while an `await` is parked (the four vision-pass native calls resolve from worker threads, none `isBlockingSynchronousMethod`). Smoke #2 (~13.5 min, no hook) caught a **spontaneous** wedge at `{elapsed: 60009}` — this one in the **foreground-lookup UsageStats IPC** (`await withTimeout(resolveForegroundAppWithRetry, 2500)`), *before* OCR ran — and recovered it; 13 healthy frames that session ran `Frame received → POST OK` in ~350–770 ms (≥78× below the 60 s threshold, zero false positives). **Not fixed:** whatever makes a native call never settle — see (5).
(5) **`withTimeout` is structurally unsound while backgrounded — everywhere it's used. Backgrounded blindness window MITIGATED by D1 (2026-09-02) for 2 of 3 sites; root cause (native promises that can fail to settle) still open.** `withTimeout(p, ms, fallback)` races `p` against a JS `setTimeout`; RN freezes that timer while backgrounded, so if `p` never settles the race never settles and the fallback never applies. **Three independent hangs in two days**, all backgrounded, all recovered *only* by the A3c-2c 60s native-tick liveness backstop (never by the `withTimeout` itself): the vision pass (`VISION_PIPELINE_TIMEOUT_MS` 25s, 2026-09-01 ~477 s hang), the foreground-lookup IPC (`FOREGROUND_LOOKUP_TIMEOUT_MS` 2.5s, 2026-09-01 `elapsed: 60009` — `resolveForegroundAppWithRetry` → native UsageStats IPC can wedge indefinitely), and a third 2026-09-02 spontaneous hang (`elapsed: 60045`, `cause: 'tick-liveness'`) during device smoke for the stale-token brief. `API_POST_TIMEOUT_MS` (12s) is the same shape. Three in two days is a rate, not a curiosity — each occurrence used to cost a full 60s of blind monitoring while the backstop fired (D1's per-phase deadlines now bound the `foreground_lookup` / `api_post` sites to 10s / 20s). **Severity escalation (AF#4 smoke, 2026-09-06) — the `foreground_lookup` site specifically is worse than "three in two days":** past ~2 min sustained backgrounding on this MIUI build it wedges on *nearly every frame* (`tick-phase-timeout:foreground_lookup`, `appPackage: 'unknown'`), not occasionally. D1's 10s `foreground_lookup` phase deadline means each frame **degrades** (loses ≤10s to the stuck UsageStats IPC, then force-releases) rather than dying — but at that frequency the backgrounded pipeline is materially impaired for the whole stretch, which is exactly the defence scenario (SafeGuard backgrounded well past 2 min). Source-level fix is `ForegroundAppModule`'s `Future.get(2500, MS)` (tracked as **#11**), already in the #4b native batch below; **on this evidence #11 outranks #4b within that batch** — same rebuild, so the cost is only ordering, and #4b's Arabic wedge self-disables safely after one hit whereas #11's bites frame-after-frame during the demo. **New lead (n=1, not proven causal):** the 2026-09-02 hang was immediately preceded by a burst of rapid app switches (Instagram → systemui → Instagram → miui.systemui.plugin, several within seconds) landing right as the stuck frame's `processCapturedFrame` began — the first structural correlation anyone has for what might trigger the wedge; recon (D1) examined ImageReader buffer starvation and MediaProjection/VirtualDisplay transitions as mechanisms and found neither plausible (see `adaptiveCapture.ts`'s per-phase-deadline block for the JS-side `frame.phase` instrumentation that now collects `appState` at every phase transition — the evidence needed to settle this, mechanical vs. coincidental, still wants the native-side half: bridge entry/resolve timestamps + process-importance, deferred to the next native rebuild). The hook already half-knew the general shape: the `isForegroundLookupStuck()` skip-path comment says a wedged UsageStats IPC "can't time out and would otherwise hang every frame forever" — it protects the *next* frame but not the one already stuck. **D1 (2026-09-02, JS-only, `adaptiveCapture.ts` + `useScreenshotCapture.ts`):** not a cancellation fix — `processingGenerationRef` already neutralizes a late-settling stale run (fails `isActive()`, its `finally` won't clear a newer run's lock), so the fix is a backgrounded-safe *timeout*, not real cancellation. Per-phase deadlines (`foreground_lookup` 10s, `api_post` 20s; both whole multiples of `NATIVE_TICK_INTERVAL_MS`, both strictly between their `withTimeout` budget and the 60s backstop) are evaluated from the native tick via `decideTickAction`'s new `'forceReleasePhase'` branch — checked *after* the absolute 60s liveness check (so a phase-ref bug can never bypass the backstop) and guarded by an anti-stale clause (`phaseStartedAtMs >= processingStartAtMs`, so a ref left over from a missed reset can't force-release a different, currently-active frame). `vision` (25s budget) deliberately ships with **no** phase deadline — `VISION_PIPELINE_TIMEOUT_MS` is the app's own policy for how long vision may run and no measured backgrounded duration exists anywhere in this codebase or its history to justify a tighter backgrounded number; picking one on no evidence risked clipping a legitimately slow Arabic/Derja OCR pass, which is a detection gap (worse than 60s of blindness) on exactly the content the keyword filter targets. `vision` still runs on the unchanged 60s backstop; `useScreenshotCapture`'s `setPhase()` helper now logs `frame.phase {from, to, appState, prevPhaseMs}` on every transition (logging the *outgoing* phase's duration, not just entry, is what makes this data usable) — see the debt item below for what's needed before a `vision` deadline can be set. Cause strings for smoke/defence-log triage: `tick-phase-timeout:foreground_lookup`, `tick-phase-timeout:api_post` (no `tick-phase-timeout:vision` — by design). `ForegroundAppModule` native `Future.get(2500, MS)` (fixes the foreground-lookup wedge at its source, our own code, not node_modules) and the app-switch-burst native instrumentation both remain their own follow-ups — native, need a rebuild, deliberately excluded from D1's JS-only scope.
- **MediaProjection revoke → fail loud (A3c-2b)** (`ScreenCaptureModule.java` + `useScreenshotCapture.ts` + `ScreenMonitor.tsx`): the OS (MIUI especially) can silently revoke the projection mid-session — `MediaProjection.Callback.onStop` fires but nothing else did, so pre-A3c-2b `isRunning` stayed `true`, the toggle stayed on, and no error surfaced (11 min of silent non-monitoring, device-observed). Now `releaseProjectionSession(boolean voluntary)` — `stopCapture()` / `onHostDestroy()` / both `onActivityResult` failure exits pass `true`; **only** the `onStop` callback passes `false`. `false` (and, as a stale-loop safety net, the `onActivityResult` denial + catch branches) routes to `handleInvoluntaryProjectionLoss()`, gated by `isRunning.getAndSet(false)` so it runs **once and only if a loop was live**: `cancelCaptureLoop()`, stop the FGS (kills the "Screen monitoring active" notification), `emitMonitoringRevoked()` → `onMonitoringRevoked {reason, timestamp}` bridge event. JS `handleMonitoringRevoked` (guarded by `isMonitoringRef` as a second layer) clears timers/coordinator, flips `isMonitoring`/`isPaused`/`permissionGranted` off, and sets `lastError` to a re-enable prompt (rendered as the red error card); `ScreenMonitor` has a `useEffect([isMonitoring])` that snaps the `enabled` switch off. Voluntary stop stays silent on **two** independent grounds (the `voluntary` flag + `isRunning` pre-cleared before the call). **Recovery is user-initiated** — `requestPermission()` needs a foreground Activity (`E_NO_ACTIVITY` backgrounded), so no auto re-request; the user flips the toggle back on. State semantics: monitoring-**off**, not a new "interrupted" state. **On-device verification scope:** the voluntary-stays-silent path is confirmed live 4× (incl. the re-entrant real `onStop` from our own `mediaProjection.stop()` being absorbed by `isRunning.getAndSet(false)` — no double emit). The involuntary path (native emit → `handleInvoluntaryProjectionLoss` teardown/FGS-stop/idempotency → JS state flip + red card) was exercised via a **temporary** debug `@ReactMethod` that runs the exact `onStop` callback body (`mainHandler.post(() -> releaseProjectionSession(false))`), since no external trigger drops a non-system projection on this MIUI build (`appops PROJECT_MEDIA ignore` no-ops, MIUI Screen Recorder coexists, adb can't stop another app's FGS). That a real OS revoke reaches `MediaProjection.Callback.onStop` the same way is **inferred** from the 2026-08-31 21:05 incident, whose log carried `MediaProjection stopped by system` from precisely that callback — not re-confirmed in this test. Also observed: after a revoke, **MIUI silently re-approves a recent MediaProjection grant with no visible consent dialog** (~150 ms) — re-enable can look instant; don't script a demo beat around the system re-prompting.
- **MediaProjection stale-token hardening** (`ScreenCaptureModule.java` + `useScreenshotCapture.ts` + `ScreenMonitor.tsx`): Android 14 idle-times-out a projection token that never called `createVirtualDisplay` (e.g. a `requestPermission()` that never reached a monitoring start). The `mediaProjection != null && isProjectionReady` flags are **not** a liveness signal — `onStop` is dispatched lazily, *during* the next `createVirtualDisplay`, which throws `SecurityException` inside `startCapture`. **Native:** `startCapture` now has a `catch (SecurityException | IllegalStateException)` (before the generic `Exception` catch) that calls `releaseProjectionSession(true)` and rejects with a distinct code **`E_STALE_PROJECTION`** (also catches `ensureVirtualDisplay`'s "MediaProjection is null"). **JS:** `startMonitoring` wraps the `native.startCapture(...)` call in a **one-shot retry** — on `E_STALE_PROJECTION` **or** `E_CAPTURE` only (any other code, e.g. `E_INTERVAL`, rethrows untouched: no teardown, no consent dialog), it `await native.stopCapture()` → `await requestPermission()` → `await native.startCapture(...)` once more; a second failure flows to the existing outer catch (`setLastError` / `isMonitoring=false` / returns false). The retry's `stopCapture()` cannot fire `onMonitoringRevoked` — `isRunning` was never set on the stale-token path and `stopCapture` clears it first, so A3c-2b's `isRunning.getAndSet(false)` gate returns false. **UI:** the standalone "Request MediaProjection permission" button was **removed** from `ScreenMonitor.tsx` (+ `requestPermission` dropped from its `useScreenshotCapture` destructure) — a token can now only be minted as part of a monitoring start, closing the idle-token path. No new automated tests (no native instrumented / JS hook harness); verified by device smoke (idle 5 min → toggle ON → one `E_STALE_PROJECTION` then `startCapture SUCCESS`, no red LogBox, no second toggle needed).
- **Accessibility event source** (`android/.../com/mobileapp/accessibility/` + `src/native/SafeGuardAccessibility.ts` + `src/hooks/useAccessibilityEvents.ts`): `SafeGuardAccessibilityService` (an `AccessibilityService`, `AccessibilityServiceInfo`: `eventTypes = TYPE_WINDOW_STATE_CHANGED | TYPE_WINDOWS_CHANGED | TYPE_VIEW_SCROLLED`, `feedbackType = FEEDBACK_GENERIC`, `flags = FLAG_RETRIEVE_INTERACTIVE_WINDOWS | FLAG_REPORT_VIEW_IDS`, `notificationTimeout = 100`, no `packageNames`) emits three RN `DeviceEventEmitter` events: `onAccessibilityWindowChanged {packageName, timestamp}` (on foreground package change), `onAccessibilityKeyboardChanged {visible, timestamp}` (IME window appears/disappears — checked via a single `getWindows()` scan for `TYPE_INPUT_METHOD`, run **only** on window events, never on scroll, with a 300 ms min-interval backstop), `onAccessibilityScroll {packageName, timestamp}` (throttled to ≤1/500 ms). `timestamp` is wall-clock ms; all throttling uses `SystemClock.uptimeMillis()`. `AccessibilityEventBridge` buffers events (bounded `ArrayDeque`, max 20, drop-oldest) when the JS context is dead and flushes on module `attach` / `flushPendingEvents(...)` — **correction (ALL_IS_FIXED #2, verified by reading `OverlayEventBridge.java`): this is not the same shape as `OverlayEventBridge`.** `OverlayEventBridge` holds a single-slot `pendingAction` field, overwritten (not queued) if a second event arrives before flush; see the mission-capture-session bullet below for why that matters. `SafeGuardAccessibilityModule` (`SafeGuardAccessibility`) exposes `isEnabled()` (service instance non-null AND listed in `ENABLED_ACCESSIBILITY_SERVICES`), `openAccessibilitySettings()`, `flushPendingEvents()`; registered via `AccessibilityPackage` in `MainApplication.kt`. **Manual enable required** — the user must turn on "SafeGuard" in system Settings → Accessibility. **Device-level failure mode (5th, found during A3d device testing 2026-09-01):** the OS can silently flip the service back off mid-testing with no visible toggle change and no in-app signal (`adb shell settings get secure enabled_accessibility_services` came back `null`) — while this is live, app-switch detection silently falls back to the 1s UsageStats poll (see `A11Y_STALE_MS` below), which is demo-risk if it happens during the defence. **Correction (device-confirmed 2026-09-02, then contradicted 2026-09-03): the adb recovery below is inconsistent on this ROM, not reliably reverting.** `adb shell settings put secure enabled_accessibility_services com.mobileapp/com.mobileapp.accessibility.SafeGuardAccessibilityService` then `adb shell settings put secure accessibility_enabled 1` was observed on 2026-09-02 to appear to succeed (`settings get` echoes the value back immediately) then silently revert (`accessibility_enabled` back to `0`) within minutes, service never binding — but on 2026-09-03 (ALL_IS_FIXED #1 device smoke) the identical command instead bound the service **cleanly**, with `dumpsys accessibility` showing it under Enabled services (not Crashed, not Binding) and events flowing for ~8 minutes straight. Both outcomes are real; neither is the reliable one. **The only durable fix is the manual toggle** — Settings → Accessibility → SafeGuard → ON, done by hand on the device — because it's the only method that doesn't depend on which way this inconsistency lands. Do not trust the adb command alone to have fixed it, and do not trust it to have failed either — verify by log, not by which behaviour you expected; and **any a11y-dependent test result is meaningless until `a11y.window.accepted` is actually observed in the logs** on an app switch — `isEnabled()` / `connected` can read true from a stale binding while zero events actually flow (this is exactly how one device-smoke round produced an uninterpretable raw-hash result before a second round, with the service confirmed live by log, produced a clean one). `useAccessibilityEvents(opts)` now returns `{ connected }` (via `isEnabled()`, refreshed on mount + every `AppState → active`) and is mounted **inside `useScreenshotCapture`** (removed from `ScreenMonitor.tsx` to avoid a double subscription). **`a11yHealth` (ALL_IS_FIXED #1, observability only — `A11Y_STALE_MS` / `a11yDriving` / every capture decision are byte-for-byte unchanged):** a pure evidence-based tracker (`src/capture/a11yHealth.ts`, factory + injected clock, sibling of `windowEventFilter.ts`, 19 cases in `__tests__/a11yHealth.test.ts`) surfaced by the hook as `a11yHealth: 'off' | 'unverified' | 'live' | 'degraded'` and rendered by `ScreenMonitor`'s Accessibility card (sage `Active` / neutral `Enabled` / amber `Not responding` / `Enable` button). **ALL_IS_FIXED #1b device-smoke fix:** the `degraded` row originally paired the amber pill with an `Open settings` button inside `styles.kvRight` (`flexShrink: 0`) — never rendered until #1b's `DEBUG_SIMULATE_A11Y_HEALTH` scaffold forced it, at which point it collapsed the `flex: 1` label to near-zero width and wrapped "Accessibility service" one character per line. Fixed by dropping the button; the amber pill alone renders cleanly, and the sub-label already carries the "turn it off and back on" instruction so nothing is lost. The dead `a11yDegradedRow` style was removed with it. `ScreenMonitor` **no longer runs its own `isAccessibilityServiceEnabled` check** — the hook's `a11yHealth` is the single source of truth. State rules: `getHealth()` order is `!enabled→off → missStanding→degraded → sawRealEvent→live → unverified`; `onWindowEvent()` (every raw window event, pre-filter, pre-`connected`) sets `live` and clears a miss; `onPollObservedSwitch()` in the 1s poll — only when the poll itself saw a real app switch (`launcherReturn || appChanged`) — latches `degraded` iff the service is enabled and no window event landed within `A11Y_MISS_GRACE_MS` (5s, strict `>`); a proven miss also fires one `isAccessibilityServiceEnabled()` bridge call to tell "unbound" from "bound but silent" and logs `a11y.health.degraded`. `setEnabled` clears a standing miss **only** on a false→true transition (manual re-enable); true→true keeps it (the bound-but-silent case). `reset()` (re-seeds timestamps, keeps `enabled`) runs next to **all three** `windowEventFilterRef.reset()` sites (start / stop / revoke). **No time-based decay** — silence is not evidence (a child on one app for ten minutes is silent and healthy). **`onPollTick()`** runs first thing in the poll `setInterval`, before the async body: a tick gap > `POLL_FREEZE_GAP_MS` (5s) means the JS thread was frozen (MIUI screen-off, D3's 185s) not that the feed went silent, so it re-seeds the liveness clock — without it, screen-wake would manufacture a false `degraded` on every unlock. Health only advances from poll evidence while foregrounded + monitoring on (the poll is a JS `setInterval`, RN-frozen when backgrounded); a drop that happens backgrounded is surfaced on the next foreground app switch. `onWindowEvent()` sits above the `connected`/`isMonitoringRef` guards, so `live` re-proves itself on the next window event even with monitoring off — post-stop/revoke `unverified` is a one-app-switch state, not a stuck pill. Hook + card wiring is **device-smoke-only** (no hook harness — debt item 22). **ALL_IS_FIXED #1b findings (2026-09-03):** `Active`, `Enabled`/`unverified`, and `off` are now device-confirmed from **real** events (a manual toggle-off, and a genuine OS-level a11y drop across a screen sleep — see the pre-flight bullet above), not just the `#1` smoke's fast path. `degraded` is confirmed only visually, via a temporary `DEBUG_SIMULATE_A11Y_HEALTH` scaffold (never shipped) — no real bound-but-silent drop was caught live to trigger it. **`onPollTick()`/`POLL_FREEZE_GAP_MS` analysis: the guard is very likely inert on this device, not proven necessary.** Device evidence: the 1s poll (`appPollTimerRef`) does not tick at all while SafeGuard is backgrounded — confirmed over a 4+ minute backgrounded window with a real app switch (home→Chrome) in it, zero poll activity, while the *native*-tick-driven periodic-capture path kept firing normally (JS thread was demonstrably alive, just this one JS timer wasn't running) — and the poll resumes at a clean ~1s cadence the instant SafeGuard is foregrounded again. Since `flushPendingAccessibilityEvents()` fires on that exact same `AppState → 'active'` transition, the poll's first post-foreground tick is racing a native buffer flush that fires at the same moment rather than up to `A11Y_MISS_GRACE_MS` later — structurally, not just empirically, favoring the flush. The condition `onPollTick` guards against (a poll tick observing a real stale gap) may not be reachable via foregrounding on this ROM. The guard is kept — twelve lines, three unit tests, cheap — but should be treated as **unexercised** rather than validated; a device history where it actually fires would change this assessment. **Window events drive app-switch captures when the a11y path is *driving*** — `connected` AND a window event seen within `A11Y_STALE_MS` (60s). While driving, the 1s UsageStats poll's own `triggerAppSwitchCapture` calls are suppressed (the poll keeps running for its foreground-cache writes, which OCR attribution depends on; the a11y handler mirrors those same `lastAppPackageRef` writes before it triggers). If the service is off, or connected but silent for 60s (crashed/unbound), the poll is the **unchanged fallback**. **`src/capture/windowEventFilter.ts`** (pure, injected clock, no RN imports) filters the raw window feed: `createWindowEventFilter().accept(pkg)` rejects `OWN_PACKAGE` → `IME` → `SAME_PACKAGE` → `LAUNCHER_SETTLING` (in that order; `LAUNCHER_SETTLE_MS` 1500, treats `com.google.android.googlequicksearchbox` as a launcher surface alongside `isLauncherPackage`), else accepts and records; `isImePackage` is a heuristic (substrings `inputmethod`/`latin`/`swiftkey`/`gboard`/`.ime` + exact `com.google.android.inputmethod.latin`) — the authoritative IME signal is the keyboard event. The filter is `reset()` on monitoring start **and** stop (next to `coordinatorRef.reset()`); on start `lastA11yWindowEventAtMs` is seeded to now. Keyboard events call `coordinator.setKeyboardVisible(e.visible)`; the latch is force-cleared on a11y disconnect and on `stopMonitoring` so a dead service with the keyboard open can't suppress routine captures forever (no capture is still fired on keyboard-close). **`onAccessibilityScroll` → `SCROLL_SETTLED` (A3c-3, tick-driven settle).** `handleA11yScroll` (in `useScreenshotCapture`) drops events from `APP_OWN_PACKAGE` / `isImePackage(pkg)` / while the keyboard flag is up / while not-monitoring or mission-paused, then folds the rest into `scrollSettleStateRef` via pure `recordScrollEvent` (stamps `event.timestamp`, arms). **No JS timer** — the native periodic tick (the only JS clock that survives backgrounding) evaluates pure `decideScrollSettle(state, now, periodicIntervalMs, lastPeriodicPassAtMs, cooldownMs, periodicGuardMs)` on every `noop` tick: emits `tryCaptureNow('SCROLL_SETTLED')` once the burst has been quiet for `SCROLL_SETTLE_MS` (2s), **defers (stays armed)** while a periodic frame fired within `CAPTURE_DEBOUNCE_MS` (else the emit hits the native 5s floor with the arm consumed and no retry) or while inside the cooldown, and **disarms** when `periodicIntervalMs ≤ 0` (game/launcher-home — uniform gate with periodic; the launcher Discover feed is category `default` so scroll still fires there). Cooldown = `max(SCROLL_SETTLE_COOLDOWN_MS 10s, effectivePeriodicIntervalMs)` so `education` (120s) isn't re-scanned every 10s. `scrollSettleStateRef` resets alongside `coordinatorRef.reset()` (start/stop/revoke), in `resumeCapture` (a pre-pause arm would settle-fire on the first tick after resume, duplicating `MISSION_RESUME`), and **on every accepted a11y app switch** (a stale arm must not settle-fire mislabeled against the next app). Pure logic in `src/utils/adaptiveCapture.ts`, covered by `__tests__/adaptiveCapture.test.ts` (incl. a permanent guard that a 10s tick can't realize a 15s target). `onAccessibilityScroll` is otherwise still not used for anything else. **Privacy**: no `getRootInActiveWindow()`, no `AccessibilityNodeInfo` content, no node text, no URLs, no field values — package names and booleans only (URL reading is a later task with its own privacy review). The unused `FOREGROUND_SERVICE_SPECIAL_USE` permission was removed from the manifest.
- **Phase B — adult-site blocking (branch `phase-b`, IN PROGRESS, not merged; brief `briefs/PHASE-B-01-adult-site-blocking.md`, merge gate 2026-09-27).** Detect Chrome (`com.android.chrome` only) opening an adult **domain** via the accessibility service, Back + block screen, blacklist confidently-detected domains. **Part 1 (device side, Tasks 1-9) is GREEN as of the 2026-09-22 review-round-4 quiet session (Q1-Q5 all run live and passed; see the quiet-session bullet below). Part 2, Tasks 10-12 (backend endpoints, device sync, dashboard) are implemented, test-verified AND device-verified live in the same session, including a full §6 step 5 run (unblock → sync → revisit → not blocked). Only Task 13 (finish: full rehearsal, final verify block) remains before the merge gate.** `onBrowserBlocked` now has a JS listener (Task 11) and domains detected on-device do reach the backend and the dashboard.
  > Domains named below are test targets drawn from the shipped static adult-domain list. They appear alongside their measurements so each arm can be reproduced.

  - **Where things are.** Pure Java, no `android.*`, JVM-tested, in `android/app/src/main/java/com/mobileapp/accessibility/browser/`: `HostNormalizer`, `DomainMatcher`, `NeverBlockList`, `DomainLists`, `HashedDomainSet`, `HostHistory`, `BrowserBlockController`, `BrowserUrlWatcher`, `DevStaticSwitch`. Android glue: `BrowserBlockRuntime` (loads lists off-thread, reads the address bar, presses Back/HOME, emits `onBrowserBlocked`; Task 11 added `getDynamicDomains`/`syncBlockedDomains`, thin delegations to `DomainLists`), called from `SafeGuardAccessibilityService`. JS: `src/utils/browserBlockDecision.ts` (pure), wired into `processCapturedFrame` just before the screen-event POST; bridge in `native/SafeGuardAccessibility.ts` (`addDetectedDomain`, `leaveBlockedPage`, `devAddDetectedDomainNow`, `showBrowserBlockScreen`, `onBrowserBlocked`, `getDynamicDomains`, `syncBlockedDomains`). Backend (Task 10): `routes/blockedDomains.routes.ts`, `routes/browserIncidents.routes.ts`, `services/blockedDomainsService.ts`, `validators/blockedDomains.validator.ts`, migration `016_blocked_domains.sql`. Device sync (Task 11): `src/services/blockedDomainsApi.ts` (HTTP client), `src/services/blockedDomainsSync.ts` (orchestrator — queue/backfill/reconcile), `src/utils/blockedDomainsSyncPlan.ts` (pure decision logic). Block screen + `overlayKind` (none/mission/block) in `OverlayService`/`OverlayWindowHelper`; a mission always wins over the block screen, and `hasActiveOverlayView()` (the #58 lease query) now means a MISSION overlay only. The mission overlay's adult-site warning line is driven by an Intent extra (`EXTRA_BROWSER_ADULT`) set from the R4 condition, not from whether a domain was added.
  - **Lists.** `assets/never_block_domains.json` is the single source for both Java and `scripts/build-adult-list.js`: `neverBlock` (`google.*` = label + 1-label TLD or known two-part suffix, bing, youtube, instagram, ...) and `exactHostOnly` (shared hosting: blogspot.com, github.io, netlify.app, ...; a host under one of these is stored **exactly**, the platform suffix is never stored, so blocking `x.blogspot.com` never blocks `y.blogspot.com`). Static list `assets/adult_domains.txt`: StevenBlack/hosts `alternates/porn-only` pinned at `cb5daa41eadded64f169ffe9b0cdee7000540d7f` (MIT, `NOTICE`), 76,774 domains, deterministic build. Dynamic list: `filesDir/dynamic_blacklist.txt` (never `cacheDir`), registrable domains. **Change from the brief:** the dynamic file is a full **snapshot rewrite** (temp file then rename, ordered under the writer lock) rather than append-only, so parent removals (Task 11) and detected adds can both be reflected and the file always equals memory. The static list is a `HashedDomainSet` (sorted 64-bit FNV-1a+avalanche hashes, binary search) instead of a `HashSet<String>`: measured on the device, ~0.8 MB and ~80-115 ms vs ~3.9 MB and ~170 ms (470-560 ms during app start), i.e. over the brief's 300 ms rule. A hash collision with a different host at 77k entries in 64 bits is ~77k/2^64 per lookup, negligible, deliberately untested.
  - **Rules (each mutation-tested).** Adds from a screenshot detection are Back-only; only a URL-watcher match may show the block screen. A frame taken after Chrome left the foreground is never blamed on the last Chrome host. An add needs a real capture time <= 30 s old (native `onScreenCaptured` timestamp, else the `screen_<ms>.jpg` epoch, never "now") with no host change since. One enforcement sequence at a time, 3 s ceiling. A focused address bar is never read as a host (Chrome shows partial text while typing). `<none>` is not a host. The dedupe key resets on leaving Chrome so a reopened blocked tab blocks again.
  - **F1 (review round 4): a re-check on list-load, so a restart doesn't buy a free pass.** Without this, a child who force-stops/relaunches SafeGuard while Chrome already sits on a blocked page keeps that page until its content next changes — nothing re-checks the page Chrome is already showing. `BrowserBlockRuntime.checkCurrentChromePage()` runs on the main thread once the static list finishes loading (and, defensively, if the lists are somehow already loaded when the runtime is built — not reachable today, since a fresh `DomainLists` is created and starts unloaded on every `startBrowserBlocker()` call, but kept for a future change that might reuse lists across restarts). It first checks whether Chrome is the **active** window (`isChromeTheActiveWindow()`, reading only the active window's package name unless it's Chrome) and, only then, makes the exact same forced url_bar read + match a Chrome window-state change already makes (`watcher.onWindowStateChanged`). JVM test (`BrowserBlockControllerTest`): lists become ready while the history's current host is still blocked → exactly one sequence, and a second forced re-check landing inside that sequence (an ordinary window event racing the F1 check) starts nothing twice. **Device arm Q3 (2026-09-22): outcome confirmed, mechanism only partly isolated.** Force-stopped SafeGuard while Chrome sat on a listed static-list page (`redtube.com`), confirmed via `dumpsys accessibility` that force-stop wipes `enabled_accessibility_services` on this ROM (the documented gotcha), re-enabled via the manual Settings toggle (the only reliable route here), and confirmed the lists reloaded (`lists loaded: static=76774 ... loadMs=331`) **while Chrome was not the active window** (Settings still had focus) — `checkCurrentChromePage()` correctly found `isChromeTheActiveWindow()` false and did nothing, a genuine negative-path confirmation of the guard. The actual block (125 ms after Chrome regained focus, ~6 min later) came from the pre-existing ordinary Chrome window-state-changed re-read, not from F1's list-load hook firing live — the Settings-toggle recovery route this ROM requires makes it hard to time "Chrome already foregrounded at the exact list-load instant" by hand. End-to-end guarantee holds (no free pass on relaunch); F1's own positive case (catches a page already showing, no new window event needed) remains JVM-only.
  - **F2 (review round 4, 2026-09-22 device finding): leaving the page is separated from blacklisting it, so an OCR-only false positive can't loop the demo.** Device finding during the quiet session's Q1 arm: xhamster.com's OCR text ("porn", "sex", "nude") pushed `finalCategory` to `'adult'` while the raw image check stayed at 0.016–0.505 (well under the 0.7 blacklist floor), so `addDetectedDomain` correctly never qualified (R4) — but nothing else pressed Back either (add-only enforcement is the sole trigger for BACK_ONLY), so the resurfaced pending mission was shown, completed, and — since Chrome was still sitting on the same page — the very next capture started **another** mission. Confirmed from the read-only log stream: three consecutive missions on the same xhamster.com session, zero `browser.add` calls, raw NSFW 0.06 / 0.016 / 0.505 on their three triggering frames respectively — none ever cleared 0.7. The mission-capture lease was not at fault (capture correctly paused for the full overlay duration each time and resumed within ~10-35 ms of completion); this was a missing "leave the page" action, not a timing bug. **Fix: `shouldLeaveBlockedPage` (`browserBlockDecision.ts`) is deliberately broader than `shouldAddDetectedDomain` — same package rule (Chrome or `'unknown'`), but no `adultScore` check at all: `finalCategory === 'adult'` in Chrome is enough.** `leaveBlockedPageForFrame` mirrors `addDetectedDomainForFrame`'s shape (same B3 capture-timestamp resolution, same 500 ms A7 timeout) and calls the new native bridge method `leaveBlockedPage(captureTimestampMs)`. **Sequencing (`useScreenshotCapture.ts`):** `leaveBlockedPageForFrame` is only called when `addDetectedDomainForFrame` did **not** qualify for the same frame — `shouldAddDetectedDomain`'s conditions are a strict subset of `shouldLeaveBlockedPage`'s, so a qualifying add's own Back-only sequence already covers the leave case; nothing ever runs both for one frame. **Native (`BrowserBlockController.leaveBlockedPage`):** resolves the Chrome host at capture time from `HostHistory` using the *identical* A2/A4 attribution rules as `addDetectedDomain` (30 s max age, no host-change-since-capture, refuses `left_chrome`/`no_host`), shares the A5 `sequenceActive` re-entrancy guard (a leave landing inside a running sequence is absorbed, reported `already_in_progress`, and does not reset the sequence clock), and starts `Enforcement.BACK_ONLY` with **no** list write and **no** incident (`Decision(BACK_ONLY, null, null, false)` — no domain, no source, no `emitIncident`). Reuses the existing re-read machinery unchanged: since the host was never blacklisted, `lists.match(hostNow)` is null on the very next re-read, so the sequence naturally ends after exactly one Back in the ordinary case (worst case, the child goes back one page) — no new escalation logic needed. **Violent and every other non-adult category never trigger Back** (both `shouldAddDetectedDomain` and `shouldLeaveBlockedPage` gate on `finalCategory === 'adult'` first). **Accepted tradeoff, written down per the reviewer's instruction:** an OCR-only adult false positive (an article or page that merely contains a word like "porn") sends the child back one page. That's accepted as reversible; blacklisting a wrong domain is not, so it still requires the raw image check. **Device-verified twice, 2026-09-22.** (1) The Q1 re-run: `xhamster.com` fully rendered → raw NSFW 0.847 (≥0.7) → `browser.add listed=true added=true reason=added` → `browser.block ... enforcement=BACK_ONLY` — the add path qualified so leave correctly never ran, no double sequence; domain added, mission resurfaced with the browser-adult warning, no separate block screen (mission wins, A1/B9). (2) A dedicated leave-only arm: static list off, `youporn.com` (fresh, on neither list), raw NSFW 0.028 with OCR keywords `adult`/`youporn`/`porn` pushing `finalCategory` to `'adult'` → `browser.leave left=true reason=left host=youporn.com` → `browser.block domain=null list=null enforcement=BACK_ONLY back=true incident=false` — confirmed no `browser.add` line, dynamic list unchanged, mission completed and Chrome **not** returned to the page (next capture cycle showed SafeGuard's own screen, no second mission). Also exercised live under Q5 (Google search results with SafeSearch off, raw vision stayed `neutral`, OCR alone triggered `'adult'`) — same `browser.leave`/no-add outcome, `google.com` never touched (never-block is additionally guaranteed there since the add path — the only path that ever writes to `lists` — would refuse it with `reason=never_block` if it ever did qualify).
  - **Detection condition (R4).** `finalCategory === 'adult' && adultScore >= 0.7` (raw vision score, not the OCR-boosted one). Recon: real adult frames 0.728-0.953, OCR-only adult frames 0.007/0.033, so **0.728 is close to the cut**. **Package rule:** Chrome, **or 'unknown'** when the UsageStats foreground lookup failed (device finding: a clear adult frame, NSFW 0.976, had `Foreground app package: 'unknown'` and was vetoed); native attribution still refuses a capture that was not on a Chrome host, so 'unknown' cannot blacklist another app's page, and a KNOWN non-Chrome package still vetoes. The mission overlay's adult warning for an 'unknown' package needs native to have listed a host. The block screen is requested from JS only when the domain is listed and no mission was presented.
  - **Block sequence (device findings, 2026-09-21).** Back at ~120-160 ms after the address-bar event; the block screen is shown only **after** the Back -> 400 ms re-read -> Back -> HOME sequence settles, because a covering overlay makes Chrome's window unreadable (`chrome-window-unreadable`) and the escalation went blind, leaving OK to re-block in a loop. The re-read reads Chrome's own window via `getWindows()`. `FLAG_SECURE` incognito frames are black (no first-visit detection there).
  - **Privacy note (address-bar text).** The only foreign node ever read is Chrome's `com.android.chrome:id/url_bar` (text and focus flag), only for events whose package is Chrome (the watcher's first statement), never page content, form fields or passwords. Only the normalised **host** is kept, logged or sent anywhere; the raw text, path and query are never logged and never leave the device. Event types added: `TYPE_WINDOW_CONTENT_CHANGED` (fires in every app; routed **only** to `BrowserBlockRuntime`, returns before the window/keyboard/scroll handlers, so it can never reach the capture triggers or the #58 backstop - device-confirmed: 20 non-Chrome content events in 61 s, only native periodic ticks captured, no backstop lines) with a 250 ms read throttle. `TYPE_VIEW_TEXT_CHANGED` is not subscribed. `onBrowserBlocked {host, listSource, timestamp}` is buffered drop-oldest (20) like the other bridge events.
  - **Stated limits.** Chrome only (other browsers, VPN, private DNS bypass it). The static list and the accessibility service must both be running; the lists load on a background thread after the service connects, so for **under a second (measured 34-600 ms)** nothing is blocked and it is logged (fail-open), and a service the OS unbinds blocks nothing. A first visit to an unlisted adult site is seen for a few seconds before capture-based detection. Incognito first-visit detection cannot work (black frames); listed domains still block there. Drawn/animated adult content is a vision blind spot (hanime.tv raw 0.007-0.067) covered by the static list only. Detection depends on vision, so a page whose images are blurred (Google SafeSearch) is not detected.
  - **JVM tests are a separate line from Jest:** `cd MobileApp/android; .\gradlew.bat app:testDebugUnitTest --project-cache-dir C:/gradlecache/mobileapp` - **151 tests** (`junit:4.13.2`, +1 for F1, +8 for F2, +1 for the Q2 dev-trigger gating test; unchanged through Task 11 — its two new native methods are thin `DomainLists` delegations). Mobile Jest **517 / 37 suites** (was 493/35 through F2/Q2; +2 suites / +24 tests for Task 11's `blockedDomainsSyncPlan`/`blockedDomainsSync` suites), `tsc` exactly the 2 known errors. Backend **247 / 24 suites** (was 212/21; +3 suites / +35 tests for Task 10).
  - **Debug-only static-list switch (Task 9).** A marker file `filesDir/debug_disable_static_list` turns the static list off (`adb shell run-as com.mobileapp touch files/debug_disable_static_list`; remove it to re-enable). It is read only when `BuildConfig.DEBUG` (a compile-time false in release), and `DevStaticSwitch`'s tests pin both that a release build ignores the marker and that the runtime has exactly one call site passing `BuildConfig.DEBUG`. No JS/UI/bridge path can flip it.
  - **Q2 dev-trigger (review round 4, 2026-09-22): `devAddDetectedDomainNow()`, gated exactly like the static-list switch.** Q2 (A2's mandatory device arm: a screenshot-detected adult frame captured just after Chrome left the foreground must refuse `left_chrome`, no add) needs a genuine adult image to appear in a *second* app within the capture window — too risky/uncontrollable to stage by driving Chrome alone. Instead: `SafeGuardAccessibilityModule.devAddDetectedDomainNow()` (debug-build-only, `if (!BuildConfig.DEBUG)` early-reject, no marker file needed) calls `BrowserBlockRuntime.addDetectedDomain(System.currentTimeMillis())` directly — the real production method, with a genuinely-now capture timestamp, so native attribution runs against the real `HostHistory` ring with no screenshot/vision pipeline involved. JS wrapper `devAddDetectedDomainNow()` (`native/SafeGuardAccessibility.ts`) is additionally a no-op outside `__DEV__`, so it's inert on both sides regardless of which guard a caller might skip. A temporary `__DEV__`-only button on the Monitor screen (`ScreenMonitor.tsx`, "Dev · Phase B Q2 trigger") fires it — **this button is dev tooling, not a one-off scaffold, and was deliberately left in place** (unlike prior sessions' `DEBUG_SIMULATE_*` scaffolds), matching the static-list switch's own permanence. JVM test (`DevStaticSwitchTest`): text-scans `SafeGuardAccessibilityModule.java` for the method and its `BuildConfig.DEBUG` guard, exactly one call site — release-unreachability proven the same way as the static-list switch, without needing the RN/Android classpath this test module doesn't have. **Device-verified 2026-09-22 (Q2 arm):** Chrome on `wikipedia.org` → switched to Settings (a real foreground change, recorded by `HostHistory` as "left Chrome") → 2 s wait → fired the trigger → `browser.add listed=false added=false reason=left_chrome`; dynamic list confirmed unchanged (still just `pornhub.com`/`xhamster.com`, no `wikipedia.org`).
  - **Quiet session (review round 4, 2026-09-22): all five arms run live, Part 1 GREEN.** Full results, in run order: **Q1** (pending mission exists, first-visit detection) — first attempt hit F2's OCR-only case (raw 0.06, filed as the F2 defect and fixed same-session); re-run after the fix scored raw NSFW 0.847 → domain added, mission resurfaced with the warning line, no separate block screen (mission wins). **Q3** (force-stop/relaunch on a listed domain) — outcome confirmed (blocked ~125 ms after Chrome regained focus, no free pass); F1's own list-load hook exercised only its negative case live, see the F1 bullet above. **Q2** (adult frame after leaving Chrome, mandatory per C1) — via the dev-trigger, refused `left_chrome`, no add. **F2 live check** (a fresh grid-style page, `youporn.com`, static list off) — exactly one Back, no add, no incident, mission completed, not returned to the page. **Q4** (incognito → static-list domain, `redtube.com`) — static-list match blocked it (Back + block screen), no mission (incognito frames are black). **Q5** (SafeSearch off + an explicit Google search) — raw vision stayed `neutral` the whole time, OCR alone triggered the leave path (not add): domain never touched, mission ran, `google.com` never blocked (both from this specific frame's low raw score, and independently guaranteed by `never_block` in the add path if it ever did qualify). **Read-only DB reconciliation, same session:** the "penalised and completed" fingerprint from 2026-09-21 was re-examined and found to be `getResurfaceableRiskyMission` working as designed, not a race — see the correction bullet below; no fix filed. #60 (`briefs/ALL_IS_FIXED-60-nsfw-misses-thumbnail-grids.md`) filed as a measure-only spike for after Part 2, no pipeline change now. **Second in-the-wild instance, 2026-09-23 (#61 rider):** a live adult domain not on the static list, thumbnail-grid landing page (picked specifically because it was absent from the static list), measured **raw NSFW 0.271** on the first-loaded viewport — well under the 0.7 add floor, so `shouldLeaveBlockedPage`'s OCR-only path fired twice instead of `shouldAddDetectedDomain`, and neither run ever added a domain. This is why triggering a genuine first-detection add on such a page needs a tap into a full-screen item (a single video/photo), not just loading the listing page — the grid alone won't cross the floor. Also noted while hunting for a test domain: of ~20 live-looking candidates absent from the static list, most were dead, parked, or redirected through an already-listed host — static-list coverage is broad enough that staging a genuine first-detection against an unlisted domain is itself the hard part of this kind of test.
  - **Task 10 (backend, 2026-09-22): `blocked_domains` + `browser_block_incidents`, no parent-facing DELETE.** Migration `016_blocked_domains.sql` adds both tables. `blocked_domains` is one row per `(child_id, domain)` (a real `UNIQUE` constraint, not the partial-index-per-active-row shape used elsewhere) — a row is reactivated in place (`removed_at` cleared) rather than re-inserted, so `detected_at`/`removed_at` history survives a re-detection after an unblock. **A10 rule** (`services/blockedDomainsService.ts`'s `recordDetectedDomain`): a POST whose `detectedAt` predates that domain's `removed_at` is ignored (`200 {outcome:'ignored'}`, row untouched) rather than silently re-blocking a domain the parent just cleared via the dev endpoint; a POST at or after `removed_at` reactivates. `browser_block_incidents` is append-only, one row per `onBrowserBlocked` firing. **Routes mirror `screen-events`'s shape:** `POST /api/blocked-domains` and `POST /api/browser-incidents` are `child:token` (the device posts with its own JWT `childId`, never a body field — matches `POST /api/screen-events`); `GET /api/blocked-domains/:childId` is `child:param` with **no `requireParentRole`** (deliberately, matching `GET /api/badges/child/:childId`'s precedent) so the child's own device token can read it back for Task 11's reconcile, not just the parent dashboard; `GET /api/browser-incidents/:childId` stays parent-only (`requireParentRole` + `requireChildAccess`) since the device never needs to read its own incidents back. **No parent DELETE anywhere** — the only unblock path is `POST /api/blocked-domains/dev/unblock`, gated by the same `devOnlyRoute` pattern as `/api/missions/dev/force` (flat 404 in production before any role/body/ownership branch, the `#8-SF4` reasoning), then `requireParentRole` + `requireChildAccess('body:childId')`. **Domain validation is server-side** (`validators/blockedDomains.validator.ts`): a Joi pattern restricted to the DNS host charset (`[a-z0-9-]` labels, dot-separated, 63-char label cap, 253-char total cap, case-normalised via `.lowercase()`) rejects a path, query string, space, userinfo, or scheme prefix by construction — no separate blacklist-of-characters check needed. All 5 routes added to `ROUTE_INVENTORY`. **New live-HTTP test file** `tests/blockedDomains.routes.test.ts` (no supertest dependency — `createApp()` on an ephemeral `http.listen(0)` port + real `jsonwebtoken`-signed tokens + Node's global `fetch`) proves, not just wires: another parent gets 403 on GET, the dev/unblock route is a flat 404 in production before any DB call, a child token 403s dev/unblock outside production, and each disallowed domain shape 400s. `tests/blockedDomainsService.test.ts` covers the full A10 outcome matrix (`added`/`already_active`/`ignored`/`reactivated`) against a mocked `db/pool`. **Migration-tooling gotcha hit live this session:** running `npm run db:migrate` against a dev DB whose `schema_migrations` ledger table didn't exist yet (but whose *structures* were already at head through migration 015) triggered `decideStartupMode`'s **baseline** path — which stamps **every** file in `migrations/` as applied without running any of them, including the brand-new 016, because the landmark-probe set (`probeSchema` in `db/migrate.ts`) is hardcoded per historical migration and has no entry for a migration added in the same session. Net effect: the ledger said 016 was applied, but `blocked_domains`/`browser_block_incidents` did not exist (confirmed via `to_regclass`). Fixed by hand: `DELETE FROM schema_migrations WHERE filename='016_blocked_domains.sql'` then re-running `db:migrate`, which took the normal (non-baseline) path since the ledger was no longer empty, and actually ran the file. **Not a regression in migration tooling to fix** — baseline mode is deliberately scoped to "this DB already has the *known* landmarks" and 016 simply isn't one yet; the lesson is to verify a freshly-added migration's tables directly (`to_regclass`) after any `db:migrate` run on a DB whose ledger state is uncertain, not to trust the runner's own "up to date" claim blindly the first time a new migration lands on such a DB. Backend: **247 tests / 24 suites** (was 212/21: +3 suites, +35 tests), `tsc` clean.
  - **Task 11 (device sync, 2026-09-22): backfill, offline queue, incident POST, full reconcile — `replaceDynamic` (built in Task 4, zero callers until now) is finally wired up.** New pure module `src/utils/blockedDomainsSyncPlan.ts` (no React/AsyncStorage/native imports): `enqueueDetection`/`dequeueFlushed` (dedupe-by-domain queue ops, keyed on the exact `{domain, detectedAt}` pair so a re-queued newer detection for an already-in-flight domain is never dropped by an older POST's success) and `planBlockedDomainsSync` (decides the one-time backfill batch and whether to touch native at all — `applyToNative` is `null`, never `[]`, when the server GET failed/was skipped, so a network blip can never be misread as "the server says block nothing" and wipe the device's real list). New orchestrator `src/services/blockedDomainsSync.ts` (AsyncStorage-backed queue + backfill-done flag under `@pfe/phaseB/blockedDomains/*`) exposes `queueBlockedDomainDetection` (real-time, called right after a qualifying `addDetectedDomain` succeeds in `useScreenshotCapture.ts`, using the same capture timestamp already resolved for attribution), `reportBrowserIncident` (best-effort, not queued — see below), and `runBlockedDomainsSync` (fire-and-forget from `startMonitoring`): flush the offline queue → one-time backfill (uploads whatever `getDynamicDomains()` already lists — `pornhub.com`/`xhamster.com` from earlier testing — so the dashboard isn't empty) → flush again → **only then** `GET /blocked-domains/:childId` → `syncBlockedDomains(serverList)` into native. **The GET runs after the backfill flush on purpose:** reading it first would let the same round's `replaceDynamic` (a full snapshot swap, not a diff) wipe domains the device just uploaded but the server's stale snapshot doesn't reflect yet. **Reconciliation itself needed no new native logic** — `DomainLists.replaceDynamic`/`dynamicSnapshot` (Task 4, `BrowserBlockRuntime.syncBlockedDomains`/`getDynamicDomains` glue, two new `@ReactMethod`s on `SafeGuardAccessibilityModule`) already handles additions AND parent removals correctly in one call, JVM-tested since Task 4 (`replaceDynamicDropsRowsTheParentRemoved`, `replaceDynamicNeverIntroducesANeverBlockDomain`) — Task 11 is the first thing that actually calls it. **`onBrowserBlocked` has its first JS consumer:** a fourth optional callback on `useAccessibilityEvents` (`onBrowserBlocked`), wired in `useScreenshotCapture.ts` to `reportBrowserIncident`. **Incidents are deliberately not queued on failure** (unlike domain detections) — a missed incident row is a lesser loss than a second offline-queue mechanism, and the domain itself (which IS queued) remains the durable signal either way. **F2 confirmed still silent here, by design:** the F2 leave-only path never emits `onBrowserBlocked` (no list write, no incident — see the F2 bullet above), so an OCR-only adult detection creates no incident row; the mission's own screen event already reaches the parent for that case. Pure-logic suite `__tests__/blockedDomainsSyncPlan.test.ts` (9 cases) plus an orchestrator suite `__tests__/blockedDomainsSync.test.ts` (15 cases, mocking `blockedDomainsApi`/`native/SafeGuardAccessibility`/`auth/tokenStorage` but using the *real* in-memory `AsyncStorage` jest mock already wired in `jest.setup.js`) cover the full backfill/reconcile/queue-survival matrix, including a parent-unblock-narrows-the-server-list case and a "GET fails → native untouched" case. Mobile: **Jest 517/37** (was 493/35: +2 suites, +24 tests), `tsc` exactly the 2 known errors (unchanged), JVM **151/151 unchanged** (the two new native glue methods are thin delegations to already-tested `DomainLists` methods, no new JVM cases needed). `assembleDebug`/`testDebugUnitTest` both `BUILD SUCCESSFUL`. **Device-verified live, same session** — see the Task 12/§6 step 5 bullet below.
  - **Task 12 (dashboard, 2026-09-22) + §6 step 5, both device-verified live in one session.** `demo_dashboard.html` gained two read-only sections on the Parent Dashboard tab — "Blocked sites" (domain, detected at) and "Browser incidents" (domain, list source, time) — via `fetchBlockedDomains`/`fetchBrowserIncidents`/`renderBlockedDomains`/`renderBrowserIncidents`/`refreshBlockedDomainsPanel`, following the exact `fetch*`/`render*` pattern the rest of the dashboard already uses, folded into the existing `refreshGamification()` orchestrator. No parent-facing unblock control — the panel text says so; verified by `adb reverse tcp:3000` + loading `demo.html` in Chrome on `2508CRN2BE`: empty state renders cleanly, then a `curl` POST directly against the running backend confirmed both rows populate on refresh. **§6 step 5 (unblock → sync → revisit → not blocked), run twice, the first run a deliberately-kept false start:** attempt 1 used `pornhub.com` — unblocked server-side, synced (`replaceDynamic` correctly dropped it from the device's dynamic file, confirmed via `cat dynamic_blacklist.txt`), but revisiting it in Chrome still blocked (`browser.block ... list=STATIC`). **Not a bug** — `pornhub.com` is also a real entry in the shipped StevenBlack `adult_domains.txt` static list, so unblocking its `detected`-source row (Task 10's `blocked_domains` table is scoped to detected/dynamic entries only, by design) can never make it reachable; the static list is independent, parent-invisible, and not what Task 10/11's unblock flow touches. Attempt 2 used a synthetic domain confirmed absent from both lists (`qa-detected-only.example`, added straight via `POST /api/blocked-domains` to simulate a detection): synced onto the device (`browser.block ... list=DETECTED` confirmed the block came from the dynamic list, not static), unblocked via the dev endpoint, synced again (`dynamic_blacklist.txt` dropped it), revisited — Chrome showed its own `DNS_PROBE_FINISHED_NXDOMAIN` page, not SafeGuard's block screen, and the browser log had zero `browser.*` lines for that navigation. Control check: `xhamster.com` (never unblocked, also independently on the static list) still blocked correctly after the whole sequence — the targeted unblock did not over-remove. **Sync trigger used in this session:** toggling monitoring off then on (`stopMonitoring`/`startMonitoring`) — the Monitor card's Accessibility row now shows a **"Last sync" timestamp** (pre-existing UI, first seen populated this session), a one-glance confirmation the fire-and-forget `runBlockedDomainsSync()` actually ran. Backfill was also incidentally re-verified live in this same install: the device's `dynamic_blacklist.txt` (`pornhub.com`, `xhamster.com` from earlier Task 9 testing) both appeared in the backend's active list after the first post-install monitoring start, before any manual POST.
  - **Device-testing gotchas learned (2026-09-21/22).** (1) **`uiautomator dump` unbinds this app's accessibility service** (UiAutomation replaces other a11y services) - never use it during an a11y test; read state from the log stream. (2) The **logcat ring buffer wraps in ~20 s** on this ROM: stream filtered logs to a file (`adb logcat -s <tags> > file` in the background, and `adb shell log -t STEP "..."` markers) rather than dumping afterwards; the file is block-buffered, so wait a few seconds before reading it. (3) Blind taps hit re-surfaced mission overlays (a pending risky mission re-surfaces on every risky capture) and can press *Later* (-10 points): confirm overlay state from `overlay attached/detached` lines before tapping. (4) An adult page left in Chrome keeps generating missions; get Chrome back to the new-tab page before a test. (5) **`installDebug` (a plain reinstall, not a force-stop) does not wipe `enabled_accessibility_services`** on this ROM — the service rebinds on its own once the process restarts, no Settings toggle needed; only `am force-stop` wipes it (see the F1/Q3 bullet above). (6) A reinstall always resets RN JS state (monitoring off, `isRunning:false`) even though the accessibility service and its `HostHistory`/list state survive — re-enable monitoring from the app UI before any arm that needs the screenshot/vision pipeline (Q1, F2, Q5); the Q2 dev-trigger arm needs only the accessibility service, not monitoring. (7) MIUI can silently re-approve a recent MediaProjection grant with no visible consent dialog on toggle-on (~150 ms) — don't assume a stuck toggle if the "Protecting" state appears almost instantly.
  - **Correction (2026-09-22): the "penalised and completed" fingerprint from the 2026-09-21 session is NOT a race — it's `getResurfaceableRiskyMission` working as designed.** An earlier pass over the same reconciliation misread `status='completed'` + `penalty_applied=10` on `88e0b99d`/`9e23da1f` as a lost-update race between `/abandon` and `/complete`'s unguarded final `UPDATE`s. A read-only recheck of `missionHelpers.ts` settled it: **"Later" (`/abandon`) is not globally terminal.** It sets `status='failed'`, `penalty_applied=10`, `escaped_at=NOW()` in one UPDATE — terminal only until the *next* risky capture. `getResurfaceableRiskyMission` (`missionHelpers.ts:128-175`, called from `POST /api/screen-events` on every risky capture, `screenEvents.routes.ts:148`) explicitly reopens a recent `'failed'` mission: `UPDATE missions SET status='pending', escaped_at=NULL, expires_at=NOW()+24h`, then `bumpResurfacedMission` adds +5 points and increments `metadata.resurfaceCount`. This is why both rows show `escaped_at IS NULL` today despite `penalty_applied=10` — the reopen clears `escaped_at` but nothing ever clears `penalty_applied` (only `/abandon` writes that column, and only once per mission in practice). The saved log stream confirms the sequence for both, sequential and ~4-5 minutes apart, not concurrent: `88e0b99d` abandoned 18:58:43 → reopened + re-presented 19:03:09 → completed 19:03:38 (DB `completed_at` matches); `9e23da1f` abandoned 19:04:01 → reopened + re-presented 19:08:37 → completed 19:09:37 (DB `completed_at` matches). `/complete`'s final `UPDATE` genuinely has no `WHERE status='pending'` re-check (same is true of `/abandon`'s), so a *true* concurrent double-request race is still theoretically possible, but it did not occur in this session and is not what produced this fingerprint — no fix is filed. Net effect on the child, as designed: the `-10` escape penalty from the first attempt stands, and the reopened mission can still be completed for points afterward — the same "penalty then completion" shape the 2026-09-14 rehearsal's running point total already reflected, just via the same mission id being reopened rather than a new one being generated.
### #61 — fresh Chrome tab after the block screen (shipped 2026-09-23)

On the block screen's OK, the app launches `ACTION_VIEW` on `about:blank`
with `setPackage("com.android.chrome")`, `FLAG_ACTIVITY_NEW_TASK` and
`Browser.EXTRA_CREATE_NEW_TAB=true`, so Chrome comes forward on a blank new
tab instead of the adult page.

**Load-bearing ordering.** The launch fires BEFORE the overlay teardown, in
`OverlayWindowHelper.BlockDismissGate`. Android grants the background-activity
start under `BAL_ALLOW_NON_APP_VISIBLE_WINDOW` — the exemption for a visible
*non-app* window, i.e. our overlay. Remove the overlay first and the exemption
goes with it; the refusal would be swallowed by the try/catch and the child
would be left on the adult page with nothing visibly failing. Device-confirmed
twice in recon and once on the shipped path (SDK 36, 2508CRN2BE).

**The adult tab stays in Chrome's tab list.** Android gives no way to close
another app's tab. Switching back to it triggers the normal block again,
which is correct.

**Scope: block screen only.** The mission-overlay path was examined and
excluded — `useMissionOverlayListener.handleOverlayAction` has five exits that
each call `hideMissionOverlay()`, inside mission-completion state, so wiring
the relaunch there means editing that state machine in four places. Not worth
it before the freeze. Pinned by a JVM case asserting zero relaunches on a
mission-overlay dismissal.

**Whether Back moves the page depends on how fast it fires, not on tab history — measured both ways on 2026-09-23, unresolved on purpose.** Two runs, same zero-history tab shape (a fresh Chrome tab, no prior navigation, one typed domain) but different enforcement paths gave opposite outcomes. **Fast path (static-list match, redtube.com, `enforcement=BACK_AND_BLOCK_SCREEN`):** Back fires ~130ms after the address-bar event; both Back presses returned `ok=true` while every re-read still reported the same host — a no-op. **Slow path (first-detection via vision, `enforcement=BACK_ONLY`, a live adult domain not on the static list, two independent runs):** Back fires seconds after the page has fully rendered; the immediate 400ms re-read was inconclusive both times (`chrome-window-unreadable` — our own mission overlay raced in and covered Chrome's window, the same interference pattern as the block screen's re-read), but the end state was conclusive — after the mission was completed (not abandoned) and dismissed, Chrome showed `about:blank`, not the adult page, both times. Mission-overlay dismissal never touches Chrome (see Scope above), so that end state is exactly what the Back press itself left behind: **it moved the page.**

Two candidate explanations, both consistent with a zero-history tab in both runs: (a) BACK_ONLY and BACK_AND_BLOCK_SCREEN genuinely behave differently, or (b) the fast path presses Back before the navigation has committed to Chrome's stack, while the slow path presses it well after commit — same tab shape, different outcome because of timing, not history. (b) is the likelier read (it explains both observations with one mechanism), but this is unresolved on purpose — a proposed fix (escalate to `GLOBAL_ACTION_HOME` when a BACK_ONLY re-read shows the host unchanged) was considered and rejected: the trigger condition it targets was never observed, both measured paths already end with the child off the page, and changing enforcement behaviour on the demo path with no failing case to validate against is not a trade worth making three days from the freeze. **Do not rely on Back alone, and do not describe either outcome as the general case** — say what path was measured.

Baseline change: JVM 151 → **155** (4 new `OverlayWindowHelperDismissGateTest`
cases). Jest, backend and tsc unchanged.

Device-verified 2026-09-23 on the shipped code path via a real static-list
trigger (redtube.com typed into Chrome), not the dev probe.
- **D1 follow-up debt (2026-09-02):** five items (4 from D1, item 5 found during D3 device smoke). **Item 3 fixed in D3 (2026-09-03); item 2's JS half shipped as ALL_IS_FIXED #4 and its native half as #4b (both 2026-09-06, #4b device-verified with a real `TessBaseAPI.init()==false`); items 1, 4, 5 remain.** AF#4's device smoke also seeded item 1 (first 5 backgrounded `vision` samples — but the 30s floor still decides that deadline, see item 1) and escalated the `foreground_lookup` finding under the frame-skip-gate debt (item (5): nearly every frame past ~2 min backgrounded → #11's `Future.get` now leads the #4b batch).
  1. **Set the `vision` phase deadline — at the 30s floor; the measurement only confirms nothing comes near it.** `PHASE_DEADLINE_MS.vision` is `null`. AF#4's measurement arm (2026-09-06) landed the first 5 backgrounded `vision`-phase durations (`frame.phase` `vision → api_post` `prevPhaseMs`, `appState ≠ 'active'`, Arabic/Tesseract path in ≥2): **906 / 932 / 1070 / 1235 / 1665 ms, p50 ≈ 1.07s / p95 ≈ 1.7s.** **Provenance — do not let the doc imply a measurement-derived number:** the recipe is ≈1.5× observed p95 floored at 30s, and 1.5 × 1.7s ≈ 2.6s, so the **30s floor decides the deadline outright** — the samples establish only that no backgrounded `vision` pass came within ~35× of the ceiling, they do not set the number. Write it as "floor rule, floored 30s (below `OCR_LOCK_LIVENESS_MS`, a whole multiple of `NATIVE_TICK_INTERVAL_MS`)", never "1.5× measured p95". Second caveat: all 5 samples are n=5 from inside AF#4's deliberate ~100s healthy window (kept short to dodge the `foreground_lookup` wedge / dHash gate — see item 2's smoke caveat), i.e. the **favourable** regime — a lower bound on how slow `vision` gets, not a characterisation of the sustained-backgrounding tail. Both facts point the same way (30s is safe *because the floor dominates*); a representative tail sample still wants a sustained-backgrounding run once #4b + #11 land.
  2. **`TesseractOcr.recognize` abandons its promise on any throw, poisoning the single-flight for the rest of the session.** `TesseractocrModule.java` (node_modules `@devinikhiya/react-native-tesseractocr`) runs recognition in `new Thread(){ run(){ … promise.resolve() } }` with **no try/catch inside `run()`** — the outer try/catch covers only thread creation and `getBitmap`. Any throw from `createTesseractAPI` / `TessBaseAPI.init` (returns `false`, unchecked) / `setImage` / `getUTF8Text` / native leaves the promise permanently unsettled. Consequence: `activeRecognition` in `mobileArabicOcr.ts` clears in a `finally` that only runs if the promise settles — it never does, so **every subsequent Arabic OCR call for the rest of the session returns the same dead promise**; Arabic/Derja OCR is dead until app restart. Backgrounded-only (foregrounded, `withTimeout(…, '')` fires and the `finally` clears cleanly) — a consequence of the same unsoundness, not an independent bug. D1's phase deadlines cannot fix it: `forceReleaseProcessingLock` touches hook-scoped refs only, while `activeRecognition` is module state in `mobileArabicOcr.ts`. Since the keyword filter targets Tunisian Derja specifically, this is a detection gap, not a robustness nicety.
  **JS half SHIPPED as ALL_IS_FIXED #4 (2026-09-06) — degrade, not retry.** Recon reshaped the brief's fix: (a) the proposed "clear `activeRecognition` on the `withTimeout` fallback path" is **inert** — that path *is* the frozen `setTimeout`, and in the foreground the `finally` at `:98` already clears it; (b) **`tesseract` is a shared instance field** (`TesseractocrModule.java:40`), reassigned by every `recognize()`/`recognizeTokens()` `run()`. The current wedge accidentally serialises all native access; abandon-then-start-fresh would race a possibly-still-live wedged worker thread against that field (its `recycle()` at line 97 reads the field at exec time → recycles the *new* run's live API → native crash / garbage risk classification). `stop()` calls `recycle()` on the same field → same race, and can't unstick an exception-killed thread. So #4 **degrades**: an entry-time staleness check (`isRecognitionStale`, `RECOGNITION_STALE_MS` 45s, mirrors `ForegroundApp.isForegroundLookupStuck`) — when a fresh frame re-enters `extractArabicTextOnDevice` and finds `activeRecognition` still bound and older than the threshold, it `scWarn`s (`"recognition did not settle within Nms"`), sets `arabicOcrDisabledForMonitoringSession`, and returns `null` (ML Kit path only) for the rest of that monitoring session — **never touching the wedged thread or the shared field**. A monotonic `recognitionGeneration` token guards the `finally` clear so a late-settling abandoned promise can't null the slot out from under a post-reset run. `resetActiveArabicRecognition()` (exported, wired into `useScreenshotCapture.ts`'s start/stop/revoke reset blocks — the **first `reset*()` flowing from the hook into a `src/services/` module**; `resetForegroundLookup` from `native/` is the closest prior art) re-arms it on the next monitoring toggle and also re-arms the (native-call-free, cannot-park) lazy-init flags for future-proofing. **Recovery clock is `OCR_LOCK_LIVENESS_MS` (60s), not 45s** — while a frame is parked in `vision` the OCR lock is latched and the coordinator SUPERSEDES every capture, so the next `extractArabicTextOnDevice` only runs after A3c-2c's tick-liveness force-release; `RECOGNITION_STALE_MS`'s exact value is not load-bearing as long as it stays below `OCR_LOCK_LIVENESS_MS` (pinned by a test). **Earned claim (narrow):** a wedged native OCR call no longer stalls the capture pipeline; Arabic OCR disables itself for the remainder of that monitoring session and a monitoring toggle restores it — **not** "Arabic OCR is resilient." Verified: 371 mobile tests / 29 suites (new `__tests__/mobileArabicOcr.test.ts`, +8), `tsc` 8/8, lint delta **−16 / zero new** (an `eslint --fix` pass on the small `mobileArabicOcr.ts` cleared 8 pre-existing `prettier/prettier` + 4 `curly` + 4 dead `no-console` disables; the one new `useScreenshotCapture.ts` import written `{x}`-style; all LF, 0 CR — measured LF-normalized HEAD-blob vs working, the CRLF-trap-proof method).
  **Device smoke — PASSED (2508CRN2BE / MIUI, 2026-09-06, app-backgrounded + screen-on via `svc power stayon usb`).** Wedge staged with a JS stand-in — `new Promise<string>(() => {})` substituted for `TesseractOcr.recognize` on the first backgrounded recognition, armed off the first `AppState → 'background'` (module-load arming would let the monitoring-start capture consume it foregrounded, where the 25s `withTimeout` timer fires and clears cleanly — proving nothing). **NOT a genuine `TessBaseAPI` throw** (that's #4b, where the corrupt-`ara.traineddata` repro belongs); the backgrounded-hang half was real. Void checks both held: poisoned `[ArabicOCR] starting` (04:25:44) landed after the `background` transition (04:25:23); **zero `[ArabicOCR] timed out` during the 65s wedge window** (04:25:44→04:26:49) — the `withTimeout` timer stayed genuinely frozen, so recovery was the fix's degrade branch, not the timer. Sequence observed: poison → `activeRecognition` latched, frame parks in `vision`, `isProcessingRef` held, captures SUPERSEDED → `Processing lock force-released { cause: 'tick-liveness' }` at exactly ~60s → **`[ArabicOCR] recognition did not settle within 65080ms — disabling Arabic OCR for this monitoring session; ML Kit path continues`** → pipeline resumes on `[OCR] ML Kit + Arabizi normalization` at normal ~15s cadence, no further `[ArabicOCR] starting` (disabled flag latched), no 60s stalls. Toggle monitoring OFF→ON → `stopMonitoring()`/`startMonitoring()` → next Arabic frame `[ArabicOCR] done in 1339ms, chars=181` — **real Tesseract recognition, re-enabled** (5 real recognitions afterwards: 662–1339ms). **Generation guard — device-exercised, not merely unit-tested** (a stronger position than either #1's `onPollTick` or #2's owed-resume debt reached — CLAUDE.md had to mark both unexercised / lightly-exercised because the ROM would not stage their conditions; this one occurred on its own). The abandoned poisoned promise settled naturally ~150s after it started — `[ArabicOCR] timed out after 150266ms`, once foregrounding for the toggle unfroze its `withTimeout` timer — and ran its `finally` against a slot that `resetActiveArabicRecognition()` had already re-armed and refilled with a live run. `recognitionGeneration !== myGeneration` held, so the stale `finally` was a no-op and the re-enabled run was untouched. This is exactly the round-1 late-settling-orphan clobber scenario the token exists for, observed unstaged. **Measurement arm:** 5 backgrounded Arabic-path `vision → api_post` `prevPhaseMs` = **906, 932, 1070, 1235, 1665 ms** (Tesseract-only 662–1339 ms) — max ~1.7s, **~36× below `OCR_LOCK_LIVENESS_MS`** and ~27× below `RECOGNITION_STALE_MS`; the 45s threshold has enormous margin, no adjustment. (Also seeds D1 debt item 1: backgrounded `vision` p50 ≈ 1.07s / p95 ≈ 1.7s with a real Arabic pass.) **Device caveat:** on this MIUI build, sustained backgrounding (>~2 min) wedges the `foreground_lookup` UsageStats IPC on nearly every frame (`tick-phase-timeout:foreground_lookup`, `appPackage: 'unknown'` — D1 debt #11; AF#4's smoke escalated this site's frequency — see the frame-skip-gate debt item (5)) and the dHash gate correctly treats low-detail near-static backgrounded content as unchanged. **The dHash drops are working as designed, NOT real content being lost, and are NOT new evidence for the closed-negative dead/stale-capture-*source* hypothesis:** a small ticking counter on an otherwise-static page lands on ≤2 of the 64 dHash sample-pair bits, so consecutive frames are `hamming ≤ HASH_CHANGE_THRESHOLD (3)` and correctly judged perceptually identical (the 90s `HASH_STALENESS_MS` backstop still forces one through). The frames were byte-distinct (the counter advanced) — the source was delivering fresh frames, which is what the 2026-09-02 close-out already established; the coarse perceptual gate filtering them is the gate doing its job, not the source going stale. Consequence for **smoke staging only**: a sustained-backgrounding repro needs genuinely high-detail changing content (or a wait past `HASH_STALENESS_MS`). The passing run kept total backgrounded time to ~100s (fresh relaunch → bg → poison at bg≈20s → tick-liveness at bg≈80s → disable at bg≈85s) to stay inside the window where the backgrounded pipeline still delivers frames to OCR. Both blockers are pre-existing and unrelated to #4. **Scaffold spec for a re-run** (reverted via `git checkout` before the commit; confirmed gone by a clean post-revert relaunch — monitoring healthy, no `POISON`/`DEBUG` in the fresh bundle): module-level `debugPoisonArmed` set `true` on the first `AppState → 'background'`; the next `runRecognition` substitutes `new Promise<string>(() => {})` for the native call and increments `debugPoisonCount` (so it fires once); a content page that trips `shouldAttemptOnDeviceArabicOcr` (the literal words "arabizi"/"arabic" via `ARABIC_PAGE_HINT_RE`, plus Derja Arabizi) served locally so it survives OCR at 720p. **Round-0 reversal:** #4 was scoped JS-only believing the JS guard makes the native fix non-demo-blocking; the shared-field finding invalidates that — the `patch-package` half is now **#4b, required before the defence, sequenced ahead of #5+#6**: patch both `run()` bodies with try/catch/finally + `promise.reject` **and localise the `TessBaseAPI` off the shared field**, check `init`'s boolean return. Batch #4b's gradle rebuild + device session with #15 / #11-`Future.get` / D1 app-switch-burst instrumentation (all native, all parked on that one rebuild). **Within the batch, do #11 (`Future.get`) before #4b** — AF#4's smoke escalated the `foreground_lookup` wedge to nearly-every-frame past ~2 min backgrounding (frame-skip-gate debt item 5), which impairs the whole demo, whereas #4b's Arabic wedge self-disables safely after a single hit.
  **NATIVE HALF SHIPPED as ALL_IS_FIXED #4b (2026-09-06) — patch-package; device-verified with a genuine native failure.** `TesseractocrModule.java`'s `recognize()` **and** `recognizeTokens()` `run()` bodies now use a **method-local** `TessBaseAPI` (not the shared `tesseract` instance field), the whole body wrapped `try { … } catch (Throwable t) { try { promise.reject("E_TESSERACT", …) } catch (Throwable ignored) {} } finally { local.recycle() }` — `Throwable` (catches `UnsatisfiedLinkError`/OOM, not just `Exception`), and the inner `try` around `promise.reject` because `promise.resolve` sits inside the outer `try` and can itself throw "Illegal callback invocation" on a dead RN catalyst (root remount), which would otherwise re-invoke a spent callback uncaught out of the worker thread. `createTesseractAPI` now checks `init()`'s boolean and `recycle()`+`throw IllegalStateException` on `false` (the corrupt/absent-traineddata path — previously a live-but-uninitialised API that threw two calls later). A worker-thread failure now **rejects** instead of orphaning the promise → `withTimeout` rethrows → `runRecognition` catch → `null` → `extractArabicTextOnDevice` `finally` clears the slot (generation-guarded): **no disable, `activeRecognition` never latches**. **Scope of the claim:** only the `TessBaseAPI.init()==false` path was *exercised on device* (see smoke below). `catch (Throwable t)` also covers `UnsatisfiedLinkError` / `OutOfMemoryError` / any `RuntimeException` from `setImage`/`getUTF8Text` — those reject **by construction, not by demonstration**; the mechanism is identical (a `Throwable` out of the worker `try` → guarded `promise.reject`) but they were not staged. `recognizeTokens` patched identically but is **unused by our JS (`mobileArabicOcr.ts` only calls `recognize`) — code-reviewed only, not device-verified.** **#4's degrade path stays** (`arabicOcrDisabledForMonitoringSession` / `RECOGNITION_STALE_MS`) as the rare backstop for any *future* non-settling native promise — now rare, not primary; do not delete it. **Delivery — new tooling:** `patch-package` devDependency, chained into `postinstall` **after** `clean-gradle-plugin-eclipse.js` (`"node scripts/clean-gradle-plugin-eclipse.js && patch-package"`); patch is `MobileApp/patches/@devinikhiya+react-native-tesseractocr+0.1.10.patch`. **Regenerating this patch (footgun — read before any `patch-package` regen, e.g. after a dependency bump):** `patch-package` diffs the *entire* package dir, and `@devinikhiya/react-native-tesseractocr/android/` accumulates Buildship/Gradle junk (`.gradle/`, `.project`, `bin/` — a Gradle 7.4.2 *and* 9.2.0 cache were both present) that `clean-gradle-plugin-eclipse.js` does **not** clean (its `TARGET_PROJECT_DIRS` list has no entry for this package). A bare `npx patch-package @devinikhiya/react-native-tesseractocr` silently captures ~250 lines of that junk into the `.patch`. Always: (1) `rm -rf node_modules/@devinikhiya/react-native-tesseractocr/android/{.gradle,.project,bin}` first, then (2) `npx patch-package @devinikhiya/react-native-tesseractocr --include 'TesseractocrModule'` — the `--include` is the durable guard (matches only `.../TesseractocrModule.java`; a loose `src/main/.../TesseractocrModule\.java$` did **not** match — use the bare name). Expected clean patch is ~147 lines, `.java`-only. New `MobileApp/.gitattributes` with the single rule `patches/**/*.patch -text` so the committed `.patch` stays byte-stable under `core.autocrlf=true` (a fresh clone would otherwise re-smudge it to CRLF and `patch-package` would fail to re-apply) — scoped to net-new files, zero working-tree churn, does **not** pre-empt the deferred repo-wide `.gitattributes` (Tier-4 #30). **Recon Q4 (shapes #11, recorded so its author doesn't rediscover it):** `ForegroundAppModule.getCurrentForegroundApp` runs the UsageStats query **synchronously on the RN bridge thread** — no executor — so #11's `Future.get(2500, MS)` is an **executor restructure, not a timeout add**; the JS side already swallows a native rejection from that path (`resolveForegroundAppOnce` catch → `UNKNOWN_FOREGROUND`), so a `Future.get` timeout that rejects cannot surface as an unhandled rejection. **Struck** the #4b brief's "build once, verify each arm separately" line — batch the **device session** (one a11y/adb/screen-on setup), separate build + verify per batch item. **Capability removal:** `stop()` (`TesseractocrModule.java`) is now dead — `run()` no longer assigns the `tesseract` field, so `stop()` would NPE into its own `catch`; nothing in `MobileApp/` calls it. **Filed, not fixed:** in **`recognize()` only**, `getHOCRText(0)`'s result is discarded and `getUTF8Text()` re-runs OCR — looks like a redundant second pass; the identical call in `recognizeTokens()` is load-bearing (populates `getResultIterator()`) and must stay. **Verification:** patch CR-byte count 0, no BOM, pure ASCII; working-tree clean-install AND post-commit smudge-path (`git checkout --` re-materialise → `npm install`) both re-apply the patch cleanly; mobile tests **372 / 29** (+1 rejection-path regression guard in `__tests__/mobileArabicOcr.test.ts`), `tsc` 8/8; **lint — zero committed delta** (measured properly: full `npm run lint` -f json rule-histogram on `main` vs the branch tip. Raw output showed `prettier/prettier` 7228→7059, −169 — **a working-tree CRLF-smudge artifact**, not a real change: `git cat-file -p main:MobileApp/__tests__/mobileArabicOcr.test.ts` and the branch-tip blob are **both 0-CR / LF-clean**; the −169 is `git checkout` re-smudging that file to CRLF on `main` under `autocrlf=true`, the same false-delta class the #2 CRLF saga documents. Blob-level = 0 new, 0 removed.); gradle `app:assembleDebug` BUILD SUCCESSFUL (the patched `:devinikhiya_react-native-tesseractocr:compileDebugJavaWithJavac` ran, no encoding warnings). **Device smoke — PASSED (2508CRN2BE / HyperOS, 2026-09-06, SafeGuard foreground-then-Chrome, no `svc power stayon usb`).** `ara.traineddata` header corrupted **in place** on device (`adb shell dd … bs=1024 count=64 conv=notrunc` — worked, no HyperOS shell-write denial; the plan's push-fallback was unneeded) → real `TessBaseAPI.init` returns `false`, **not a scaffold** (#4's smoke used a non-settling JS stand-in — that disclosure is fully dropped for the rejection path). **AC1:** 3 consecutive Arabic captures, one monitoring session (PID 3654), each `[ArabicOCR] starting` → `[ArabicOCR] recognition failed [Error: java.lang.IllegalStateException: TessBaseAPI.init failed for 'ara' - missing or corrupt traineddata under /storage/emulated/0/Android/data/com.mobileapp/files]` → `runRecognition` returns `null` → `[OCR] … using ML Kit path` → `POST /api/screen-events OK`. **Zero `recognition did not settle`, `arabicOcrDisabledForMonitoringSession` never latched** — each next frame re-enters `[ArabicOCR] starting` (no state accumulates across failures — the property #4b claims, proven with 3 frames not 1). **AC2:** `adb push` restored the model (md5 `a9e85b5a…` byte-identical to the pre-corruption backup), **no monitoring toggle** (same PID 3654, no `stop/startMonitoring`) → next 3 Arabic frames `[ArabicOCR] done in 1124/1145/1340ms, chars=220/229/221, conf=0.55–0.65` — real Tesseract recognition recovered automatically. **These three figures are the `TesseractOcr.recognize` call duration, NOT `frame.phase vision→api_post prevPhaseMs` (which spans ML Kit + Tesseract + the classifier) — they are NOT D1 debt-#1 samples and must not be filed as such.** They sit inside #4's 906–1665 ms range, so: reasonable evidence the patch did not slow recognition despite localising the API off the shared field and wrapping the body in try/catch. No crash, no RedBox; the LogBox "4" badge is just the `console.warn` count from the 3 expected `recognition failed` diagnostics. **Device caveat (not a #4b issue, confirms the #11-before-#4b ordering):** past ~2 min sustained backgrounding the `foreground_lookup` UsageStats IPC wedged nearly every frame (`tick-phase-timeout:foreground_lookup`, `appPackage: 'unknown'`, `prevPhaseMs` up to 74365) — both arms were run inside fresh ~2-min windows. **Also:** HyperOS wiped `enabled_accessibility_services` to `null` on every `am force-stop`; the adb re-enable (`settings put secure enabled_accessibility_services …; accessibility_enabled 1`) bound the service cleanly here (`dumpsys` Bound+Enabled), though the in-app a11y card stayed `Enabled`/`unverified` (hook hadn't re-detected) — a11y is not in the OCR-rejection path so this did not affect the arm.
  3. **FIXED in D3 (2026-09-03) — `processCapturedFrame`'s `finally` no longer zeroes `processingStartTimeRef` outside the generation guard.** The `processingStartTimeRef.current = 0;` line moved from *above* `if (processingGenerationRef.current === generation) { … }` to *inside* it, next to the already-guarded `isProcessingRef.current = false` and D1's `setPhase('idle')` — the three `finally` resets are now uniformly generation-guarded. Recon (`D3` plan) established this was **one line corrupting four readers**, three harmed on every late stale `finally`: (a) `shouldForceReleaseProcessingLock` early-returns on `<= 0` → the A3c-2c 60s backstop is silently disabled for the *active* successor frame (worst on `vision`, whose phase deadline is `null` — no other backstop); (b) the takeover check at `processCapturedFrame`'s top computes `Date.now() - 0` → spurious immediate OCR-lock takeover of a healthy frame; (c) the heartbeat's `processingStartTimeRef.current === 0` self-clear terminates the active frame's own liveness interval early; (d) the heartbeat's `elapsed` is computed against `0`. Sites A (`forceReleaseProcessingLock` — no `generation` in scope, bumps the generation itself) and B (takeover branch — synchronously overwritten by the acquire write three lines later) are correct for **structural** reasons and were deliberately left alone; the watchdog reset was already guarded. Verified: 330 mobile tests (2 new pure `decideTickAction` invariant guards in `adaptiveCapture.test.ts` — real stamp → backstop fires, zeroed stamp → `noop`), `tsc` 8/8 (the "9" predates #2's `scWarn`-import fix), lint delta zero. **Two-arm device smoke (2026-09-03, 2508CRN2BE, app-backgrounded + screen-on, temp `DEBUG_SIMULATE_FINALLY_RACE` scaffold since removed):** identical staged interleaving both arms — frame 1 parked in `vision`, `tick-liveness` force-released it at `elapsed≈64827` (gen 1→2), frame 2 acquired gen 3 and parked, frame 1 then resumed and ran its `finally` stale (`staleGeneration:1, activeGeneration:3`). **Arm 1** (`willZeroActiveStamp:true`, pre-D3 behaviour re-injected): frame 2's stamp zeroed → **no further `tick-liveness` ever fired** (6+ periodic ticks over 2 min, all `capture.skipped`, monitoring silently dead). **Arm 2** (D3 fix, `willZeroActiveStamp:false`): frame 2's stamp intact → `tick-liveness` force-released it at `elapsed:60050` ≈ 60 s after *it* parked, and the healthy ~60 s recovery cycle continued (frame 3, `tick-phase-timeout:foreground_lookup`, …). `processCapturedFrame` has no test harness, so the moved line itself is device-smoke-verified only.
  4. **FIXED as ALL_IS_FIXED #12, C1 (2026-09-10) — `pendingFrameRef` read/null is no longer unconditional.** Was: `const deferredFrame = pendingFrameRef.current; pendingFrameRef.current = null;` ran regardless of generation, so a superseded frame's late `finally` could steal the pending frame the *active* frame had just enqueued, or steal the coordinator's coalesced `pendingReason` via the unconditional `takePendingReason()` on the following `else if`. See the ALL_IS_FIXED #12 bullet below for the fix, its round-1 wiring mistake and correction, and what the resulting test suite does and does not prove.
  5. **The A3c-2c native-tick liveness backstop does NOT survive *screen-off* — only *app-backgrounded, screen-on*.** D3's device smoke measured this directly: with the screen slept (`KEYCODE_SLEEP`), JS froze completely for the full 185 s hold and `tick-liveness` fired at `elapsed: 195233` — i.e. **only on screen-on thaw**, not at 60 s. With the app backgrounded but the screen kept on (`stay_on_while_plugged_in`), it fired at `elapsed≈60000` every cycle, matching the three recorded incidents (all of which, re-examined, were screen-on). So `NATIVE_TICK_INTERVAL_MS`'s "Doze-resistant … keeps the process unfrozen for the whole session" is true for the *native* `Handler.postDelayed` loop but **not** for the JS `DeviceEventEmitter` listener it drives — MIUI freezes the RN JS thread on screen-off despite the `mediaProjection` FGS. Consequence: a frame that wedges while the phone is locked is unrecovered until the user wakes the screen (then recovered promptly). Not a D3 regression (D3's fix is correct in both states); it bounds what *any* JS-side backstop — A3c-2c or D1 — can promise. A real fix would need the force-release to run in native (e.g. the tick loop itself checking a last-progress timestamp the bridge updates), which is a native change deferred with the rest of the `withTimeout` root-cause work.
- **ALL_IS_FIXED #58 — the mission capture lease backstop could reclaim a live mission, and (round 1's own design flaw, caught in review) an unbounded query could have made it never reclaim a leaked one (2026-09-18).** Filed as a code-trace finding (`briefs/ALL_IS_FIXED-58-lease-backstop-under-live-mission_1.md`): `checkMissionCaptureSessionBackstop()` measured staleness as `Date.now() - session.startedAtMs`, so any mission (quiz/minigame) open past `MISSION_SESSION_MAX_MS` (10 min) force-ended regardless of whether the mission UI was still genuinely mounted — a false reclaim, not a leak reclaim. **Downstream risk was already double-gated on both surfaces** (resolved during planning, not device-verified this session): the in-app `MissionScreen` chrome and the native overlay's own rendered text (`OverlayQuizHelper.java`'s `"Question " + N + " / " + M` progress `TextView`, still set on every question — **doc correction**: ALL_IS_FIXED #46's progress dots were added *alongside* that text, not instead of it, contrary to this file's prior description of them as a replacement) both independently match the OCR self-skip predicate (`shouldSkipScreenEventReporting`/`isMissionGameUiContext`, `benignRiskContext.ts`), so a resumed capture of either surface gets OCR-skipped before scoring. The remaining defect was the lifecycle/architecture gap itself, not an active scoring-risk hole.
  - **Fix has two halves, `missionCaptureSession.ts`.** (1) `lastTouchedAtMs`, distinct from `startedAtMs` — the soft threshold is now measured from the last *proof of life*, not session start. For `MissionScreen` (sources `'screen'`/`'fallback'`) proof of life is a new JS `setInterval` heartbeat (`startMissionCaptureSessionHeartbeat`, `MISSION_TOUCH_INTERVAL_MS` 60s) — safe **only** because `MissionScreen`'s own pre-existing `AppState` listener auto-abandons the mission after >3s out of foreground (`MissionScreen.tsx`'s mount effect now has an inline comment recording this dependency), so the interval can never go un-ticked longer than that without the mission self-ending first. A plain JS timer is **not** safe for the overlay — confirmed both by this repo's own prior finding (the 1s `appPollTimerRef` UsageStats poll was device-confirmed to stop ticking entirely while backgrounded, screen-on) and by this session's own device observation (below) — because the overlay is drawn over *another* app, so the RN host is backgrounded for the overlay's entire display duration. Proof of life there is instead an **on-demand native query** (`deps.isOverlayShowing`, wired to a new `OverlayMissionModule.isOverlayShowing()` / `OverlayService.hasActiveOverlayView()`, an in-process immediately-resolved static-field read — no IPC, no `Future`/timeout hardening needed), asked only when the soft threshold trips inside `checkBackstop()` (now `async`); a `true` answer re-touches the lease, `false`/rejected falls through to force-end. `OverlayService.overlayView` is now `volatile` (same cross-thread-read justification as #53's `pendingQuizCancel`/`pendingTttCancel`) — confirmed by direct read that all four teardown paths (`removeOverlayNow()` ×2 branches, `removeOverlay()` ×2 branches, `onDestroy()`, the `ACTION_HIDE` branch) null it right after `OverlayWindowHelper.detach(...)`, and the only place it's set non-null is the successful-attach line in `showOverlayFromIntent`. (2) **`MISSION_SESSION_HARD_MAX_MS`** (1 hour, a policy number not measured/derived, applied to every source uniformly) — an absolute ceiling measured from `startedAtMs`, checked first and unconditionally before the query logic, added in review (round 2 caught that an unbounded "defer whenever still-showing" query would let an orphaned-but-attached overlay — `OverlayService` runs `START_STICKY` — defer forever, deleting the backstop's one job). Past it, force-end regardless of what the query says, logged as a `scWarn` distinct from the ordinary deferral `scLog`.
  - **Disclosed behavior change:** `checkMissionCaptureSessionBackstop()` is now `async`; on the `'overlay'` path it returns control at its first `await` (the native query), so its two call sites (`useScreenshotCapture.ts`'s `handleA11yWindowChanged`, `useMissionOverlayListener.ts`'s `AppState` listener — both now call it as `void checkMissionCaptureSessionBackstop()`, commented at each site) continue running before the query resolves or any resulting `forceEnd()` lands: a capture that would previously have been allowed immediately after a reclaim is now skipped for that one triggering event, picked up by the next one. `'screen'`/`'fallback'` paths never hit an `await`, so they're genuinely unchanged (byte-for-byte synchronous), which is what keeps the 14 pre-existing tests passing unmodified. The quiz-retry-without-a-fresh-`begin()` path (`useMissionOverlayListener.ts`'s `isQuizNotPassedError` branch) is now covered for free — however long a retry loop runs, the next backstop check just asks the window again.
  - **Verified:** mobile **460 tests / 34 suites** (was 440/34, +20 in `missionCaptureSession.test.ts` — touch/heartbeat, the overlay query's every branch including a rejected-promise case and a manual-resolver race test proving a session mutated mid-`await` isn't resurrected, and the hard-ceiling guard proving `forceEnd()` fires even when the query still resolves `true`). `tsc` exactly 2 pre-existing (`useScreenshotCapture.ts`, unrelated), 0 new. Lint: 0 new after `eslint --fix` (which also cleared pre-existing prettier debt in `OverlayMission.ts`, net −2) — measured by blob-level before/after per touched file, not raw count; the one genuinely new category per file is the expected `+1 no-void` warning at each of the two new `void checkMissionCaptureSessionBackstop()` call sites. Native: `app:assembleDebug`/`installDebug` both `BUILD SUCCESSFUL`, no new javac warnings beyond a pre-existing unrelated deprecation note.
  - **Device smoke — partial, and the gap is real, not glossed over.** App launched cleanly with the new code (confirms the `native/OverlayMission.ts` import inside `missionCaptureSession.ts`'s module-level singleton doesn't crash on load). At temporarily lowered constants (`MISSION_SESSION_MAX_MS` 15s / `MISSION_SESSION_HARD_MAX_MS` 90s / `MISSION_TOUCH_INTERVAL_MS` 5s, `DEBUG_SMOKE_LOWERED`-tagged, fully reverted before commit — confirmed by `grep DEBUG_SMOKE` coming back empty), a real forced Tic-Tac-Toe mission opened via `MissionScreen` (the `'screen'` source, from the Missions list "Start" button, **not** the overlay) survived ~3 minutes foregrounded with heartbeat ticks logged every 5s — **but this observation turned out to prove something narrower than intended**: neither of `checkBackstop()`'s two trigger signals (a11y window-changed events, `AppState → 'active'`) ever fired during that window (the device's SafeGuard accessibility service showed as both `Enabled` and `Crashed` in `dumpsys accessibility` — the documented ROM inconsistency — and the app never left foreground), so `checkBackstop()` itself was never invoked at all, soft or hard threshold. What this *does* confirm, honestly: a `MissionScreen` session sitting quietly foregrounded is harmless regardless of the backstop, because nothing calls the backstop in that state — which is itself the real-world reason `MissionScreen`'s hard ceiling "costs nothing under normal operation," but it is **not** a device confirmation of the heartbeat deferring an actual backstop check, nor of the hard ceiling firing on a healthy heartbeat. **The overlay-path query and the hard-ceiling force-end were not exercised live at all** — `OverlayService` is `android:exported="false"` (no adb-triggerable start), there is no dev route that forces the overlay to actually display (only `/missions/dev/force`, which forces the DB row), and building a temporary trigger scaffold or staging a full risky-capture pipeline (MediaProjection consent + a flagged keyword page) was out of this session's scope. **Do not claim the overlay query or the hard ceiling were device-verified** — they're unit-tested (including the exact race and hard-ceiling-with-healthy-query scenarios) and native-build-verified, not live-verified. A follow-up device session with either a temporary forced-overlay JS scaffold or a real risky-capture stage, and with the a11y service confirmed live via `a11y.window.accepted`, is needed to close this.
- **ALL_IS_FIXED #11 — the `foreground_lookup` wedge (2026-09-08).** Three commits, in order: **F1** (JS, `5846cfb`), **F3** (native one-liner, `04dd013`), **F2** (native executor restructure — commit pending as this entry is written, see below). **R0 (discriminator) — null result, not a confirmation of either mechanism.** A fresh 5-minute sustained-backgrounding device window (2508CRN2BE, screen on via `svc power stayon usb`, a11y verified live via `a11y.window.accepted` at both the start **and** end of the window — the AF#4-era prose never records this) produced **zero** `tick-phase-timeout:foreground_lookup` across ~19 full backgrounded capture cycles (72 of 75 `frame.phase` transitions were `appState: 'background'`, so the pipeline was genuinely exercised, not idle) and **zero** `Foreground lookup wedged` skip-warns; every `foreground_lookup→vision` transition completed in single/double-digit ms, matching `adaptiveCapture.ts:113-117`'s documented sub-100ms healthy figure. The AF#4/#4b "nearly every frame" wedge did **not** reproduce on this run. Per the brief's own null-result rule, F1's device arm (A2, a before/after comparison) was **not run** — there was no nonzero "before" to compare against — and neither F1 nor F2 should be read as "confirmed to fix the AF#4 symptom"; both ship because each is independently a real, code-read-confirmed defect (see below), not because R0 proved which one caused AF#4's wedge.
  - **F1 (mechanism B — the frozen retry-sleep):** `resolveForegroundAppWithRetry` (`MobileApp/src/native/ForegroundApp.ts`) slept via a raw `setTimeout` between attempts; RN freezes that timer while backgrounded, so a first attempt returning `unknown` (a normal, healthy outcome, not a failure) wedged the retry loop forever. Now takes a required 3rd parameter `backgrounded: boolean` (no default — both call sites, `useScreenshotCapture.ts:687` and `:1732`, must pass it explicitly, so `tsc` catches any future caller that forgets it); backgrounded makes exactly one attempt and never sleeps, foregrounded is byte-for-byte unchanged (3 attempts, 200ms apart). New suite `__tests__/foregroundApp.test.ts` (5 tests, incl. a generation-guard regression test using `jest.setSystemTime` to move `Date.now()` past the native timeout **without** firing the pending `setTimeout` — the actual backgrounded scenario — rather than `jest.advanceTimersByTime`, which would fire it and prove nothing). **377 mobile tests / 30 suites** (was 372/29).
  - **F3 (unrelated, bundled for commit-ordering reasons — see below):** `ScreenCaptureModule.java`'s `lastCaptureEmittedAtMs` (write on main thread, read on bridge thread, no barrier) → `volatile`. **Code-review only — no device arm exists or can be staged** for a missed-visibility-window race on demand; do not describe this as "verified."
  - **F2 (mechanism A — the unbounded native Binder block):** `ForegroundAppModule.getCurrentForegroundApp` ran `UsageStatsManager` queries **synchronously on the shared NativeModulesThread** (legacy bridge, RN 0.74.5, `newArchEnabled=false`) with no timeout — a wedged Binder call (not client-interruptible; Binder blocks in-kernel via `ioctl`) could hang forever, as it once did for `elapsed: 60009` (2026-09-01). The resolve body is extracted into `resolveForegroundAppBlocking(String ownPackage)`, run inside a `Callable` on a **persistent, bounded, fail-fast, self-recovering** single-thread executor (`new ThreadPoolExecutor(1, 1, 0L, MS, new SynchronousQueue<>())` — zero-capacity queue, default `AbortPolicy`, so a second concurrent call fails fast instead of queuing behind a possibly-wedged worker), bounded by `future.get(2_500, MS)`. **Only the calling (bridge) thread ever touches `promise`** — `resolve` is guarded separately from the `reject` paths (`try { promise.resolve(x) } catch (Throwable t) { Log.e(...) }`) because `resolve()` can itself throw ("Illegal callback invocation" on a torn-down catalyst — the same hazard `TesseractocrModule.java` was patched for in #4b); every `reject()` is similarly wrapped in `try/catch(Throwable ignored)`. **Recovery:** on `RejectedExecutionException`, if the current task has been outstanding `< EXECUTOR_WEDGE_RECOVERY_MS` (60s — justified standalone, not by citing an unrelated constant: a healthy call is sub-100ms per `adaptiveCapture.ts`, so 60s has enormous margin, and waiting it out costs nothing since a call this deep into busy-rejections already reads as `unknown` to JS anyway, same as a genuine wedge would) it rejects `E_FOREGROUND_BUSY`; past that threshold it presumes a genuine wedge, **`shutdown()`s (not `shutdownNow()`) the old executor** (lets a merely-very-slow call's worker exit once it actually finishes, rather than lingering — an earlier draft of this fix used `shutdownNow()`/discarded the old executor outright, which would have made a slow-not-wedged call leak its thread forever; caught in review before shipping) and submits fresh. A `destroyed` flag (set in a newly-added `invalidate()`, which this module previously lacked — its absence would have leaked the persistent executor's thread on every RN catalyst teardown, independent of the wedge case) stops a teardown from ever being misread as a recoverable wedge; a `currentTaskSubmittedAtMs == 0` guard stops the very first rejection from computing an astronomical `now - 0` elapsed time. `ownPackage` is read on the bridge thread and passed into the executor, not read via `getReactApplicationContext()` inside the worker — an abandoned worker outliving a torn-down catalyst is exactly the thread that must not touch it. **Device-verified: healthy path only** (multiple app switches under monitoring, `resolveForegroundApp` resolving correctly every time, `frame.phase foreground_lookup→vision` transitions fast, zero `E_FOREGROUND_BUSY`/`E_FOREGROUND_TIMEOUT` under normal — even fairly rapid — switching). **A genuine multi-minute Binder wedge was not staged or reproduced this session** — the 60s recovery/`E_FOREGROUND_BUSY` path is code-reviewed and unit-reasoned, not device-verified. Do not claim "the foreground lookup can no longer wedge" — this bounds the JS-visible wait and self-recovers from a *replaceable* wedge; a truly deadlocked Binder thread still cannot be cancelled, only abandoned.
  - **Commit ordering (F1 → F3 → F2, not F1 → F2 → F3):** F3 is committed *before* F2 specifically so the one native rebuild (built once, after both F3's and F2's code exist in the tree) corresponds exactly to F2's own commit tree — building F2 and F3 together then committing F2 alone would leave the device-tested binary matching neither commit. F3's tree, as committed, was never independently installed/run on device in isolation — verified by `assembleDebug` (compile-only) only; the device-tested binary is F2's commit, which includes it.
  - **Gotcha found this session, not previously documented — `git stash push`/`pop` is a CRLF trap distinct from the already-documented ones.** Using `git stash` to snapshot a clean HEAD baseline for a lint comparison **corrupted two touched files to full CRLF** (every line): `git stash push` checks out HEAD (autocrlf smudges LF blobs to CRLF in the working tree), and `git stash pop`'s patch-reapplication does not undo that smudge — the restored file ends up CRLF even though the pre-stash working copy and the git blob were both LF. Do not use `git stash` as a baseline-snapshot mechanism for this kind of measurement; use `git show HEAD:path` (reads the blob directly, bypasses the smudge filter entirely) into a scratch file instead, exactly as this repo's other CRLF write-ups already recommend for verification — stash just hadn't been flagged as sharing the same failure mode until now. **Also:** this session's `grep -c $'\r' file` (git-bash) silently reported `0` on files later confirmed via a Node byte-count script to be **100% CRLF** — do not trust bash `grep` for CR-byte verification in this environment; use a small Node script (`for (const b of buf) if (b===13) cr++`) instead, every time.
- **ALL_IS_FIXED #12 — the `pendingFrameRef`/pending-reason steal, + #9 Option A (2026-09-10).** Two commits: `ec2bc21` (C2, `NsfwTflite`) then `3ce9421` (C1, the steal). **C1 went through two review rounds before landing — both findings are worth knowing before touching this code again.**
  - **Round 1 finding (the mistake, not just the fix):** the first draft called the new pure decision function `decideDeferredFrameHandoff` **from inside** the same `if (processingGenerationRef.current === generation)` block that already guarded the D3 lock-reset fields — meaning `isOwnGeneration` was **trivially `true`** at the only call site, so the function's `!isOwnGeneration → 'none'` branch was dead code in production even though it unit-tested clean in isolation. A companion test case mislabeled an unreachable input (`isOwnGeneration: true, isProcessing: true` — impossible, since the same block sets `isProcessingRef.current = false` before it's read) as "pre-existing behavior, must not change," when the *real* second half of the defect is `isOwnGeneration: false, isProcessing: true`: a stale run whose deferred frame can't dispatch (live successor busy) falling through and stealing the coordinator's `pendingReason` instead, via the old code's unconditional `takePendingReason()` on the left of `&&`. **Lesson: a generation guard added around a decision call must make the guarded parameter genuinely reach both branches — nesting the call inside the very `if` it's meant to test makes the negative case unreachable and the test suite decorative.**
  - **The fix (post-correction):** `isOwnGeneration` is computed once, used for the existing D3 lock-reset guard, and passed to `decideDeferredFrameHandoff` (new export, `adaptiveCapture.ts`) **outside** any nested `if` — the call is unconditional, so a stale generation genuinely reaches it and genuinely evaluates `false`. The `pendingFrameRef` clear is gated directly on `isOwnGeneration` (kept separate from the returned action, not derived from it — the old code cleared it unconditionally for the own-generation case regardless of which branch fired downstream). `captureCoordinator.ts` gained a non-mutating `peekPendingReason()` (mirrors `isForceArmed()`) so the decision can see `hasPendingReason` without consuming it — the mutating `takePendingReason()` stays exactly where the old code called it, inside the branch actually taken. `pendingFrameRef.current = null` was also added to the two generation-bump sites that didn't already do it (`forceReleaseProcessingLock`, the `processingWatchdog` `setTimeout`; the OCR-lock-takeover branch already cleared it) — F1 (the guard) and F2 (these clears) ship together because F1 alone would leave `pendingFrameRef` orphaned after either bump, dispatchable later by an unrelated future generation's `finally` as if fresh.
  - **Disclosed behavior delta:** the old code's `else if (takePendingReason() != null && isMonitoringRef.current)` put the mutating call on the left of `&&`, so it always consumed the coalesced reason even while `isMonitoring` was `false` — only the *dispatch* was conditional, not the *consumption*. A coalesced request arriving right before monitoring turned off was silently discarded, never used. Now it survives unconsumed until monitoring resumes (case 8 of the test table below).
  - **Round 2 finding:** the first correction's red/green protocol for the new pure function ran the 8-case table against a **do-nothing stub**, not the real function with one branch deleted — a stub returns `'none'` for everything, so it passes the three discriminating cases (which all expect `'none'`) for the wrong reason, proving nothing. Corrected protocol: implement the function fully, delete branch 1, run (expected split worked out by hand: exactly 3 failed — the defect cases — 6 passed), restore branch 1, run again (9/9 green). Round 2 also required reinstating a `peekPendingReason`/`takePendingReason` regression case in `captureCoordinator.test.ts` (a new exported method with zero coverage is exactly the shape that rots silently) that an earlier pass had dropped without argument.
  - **Verified (measured, not assumed):** RED run — 3 failed (cases 2, 3, 4, exactly as predicted), 6 passed. GREEN run — 9/9. Full suite **390 mobile tests / 30 suites** (was 377/30 pre-session: +3 from C2, +9 `adaptiveCapture.test.ts` + 1 `captureCoordinator.test.ts` from C1). `tsc` unchanged at 8 pre-existing errors (0 new). Lint: net **negative** delta on every touched file — an `eslint --fix` pass on the touched files incidentally cleared a batch of pre-existing prettier nits (same pattern as prior sessions' "eslint --fix on a small file clears N pre-existing issues"); zero new issues anywhere, confirmed via the blob-level clean-baseline method (`git cat-file -p HEAD:path` written directly, never `git checkout --` or `git stash`, both proven CRLF traps in this repo).
  - **Gotcha reconfirmed this session:** the working tree for 4 of the 5 C1-touched files (`captureCoordinator.ts`, `captureCoordinator.test.ts`, `adaptiveCapture.ts`, `adaptiveCapture.test.ts`) had drifted to full CRLF on disk **before any edit this session touched them** — pre-existing working-tree drift, not something these edits introduced (confirmed: the stored HEAD blobs for all 5 files were already 0-CR/LF-clean, and the corruption was present in the very first backup taken immediately after editing, before any git operation in this session could have caused it). This is the same ambient, `.gitattributes`-less, `autocrlf=true` condition already documented elsewhere in this file — `dos2unix`'d the 4 files (confirmed via `git diff --stat` unchanged before/after: same insertion counts, proving zero logical content changed) before measuring lint, otherwise the raw prettier count would have been inflated by hundreds of phantom per-line CRLF violations that vanish the moment the file is committed (`autocrlf`'s clean filter normalizes on `git add`).
  - **C2 (#9 Option A, `ec2bc21`, unrelated to the above, approved without a review round):** `NsfwTflite.initNsfwModel()` declared `initModel(): Promise<boolean>` but discarded the resolved boolean — a native "not actually loaded" `false` result was indistinguishable from success, `nsfwClassifier.ts`'s `modelReady` latched `true` on it, and every subsequent `classifyImage()` call skipped re-init forever, silently running inference against an unloaded model. Now throws on a falsy resolve; `nsfwClassifier.ts`'s existing `initPromise` catch (nulls it, rethrows) and `classifyImage`'s catch (`unavailableResult()`, never sets `modelReady`) already handled this correctly once the signal wasn't swallowed — no other file needed changing. **Contract hardening for a branch prior recon (`#9`) established is unreachable against the current native code** (which resolves literal `true` or rejects, never `false`) — not a fix for an observed production defect. New `describe` block in `nsfwClassifier.test.ts` is the **first test in the repo to mock `NsfwTflite`** (none existed before) — installs the mock before a `jest.resetModules()`-forced fresh `require()` of `nsfwClassifier.ts`, since it captures its native module reference at import time and `modelReady`/`initPromise` are otherwise-unresettable module singletons.
  - **Not fixed, out of scope for #12:** the underlying wedge that produces a stale run in the first place (the still-open, unexplained vision-pass hang, Tier-3 #11's remainder) — C1 closes the *consequence* (a stale run stealing a live run's state), not the cause. No hook-level test exists or was added (no `@testing-library/react-native` in this repo) — the 9 pure-function cases pin `decideDeferredFrameHandoff`'s branch logic; they cannot prove the call-site wiring (`isOwnGeneration` computed outside any nested `if`) holds, which remains a code-review fact. Device arms confirming the fix live (real wedge, real steal, real recovery) were not run this session.
- **ALL_IS_FIXED #40 — the game grids wrapped a column off the board (2026-09-11).** First device play of any mission mini-game (`tictactoe`, forced via the dev route) found the third column untappable; `uiautomator` confirmed zero accessibility bounds anywhere past the second column pre-fix (only 8 of 9 `Pressable` cells present, two with degenerate/inverted bounds). **Mechanism:** React Native lays out border-box (confirmed for this exact pin — `react-native@0.74.5` has no `boxSizing` style property anywhere in `Libraries/StyleSheet/`, so `width`/`height` always include `borderWidth`), and both `TicTacToeGame.tsx`'s and `SudokuGame.tsx`'s `grid` container combined a `flexWrap: 'wrap'` row with `borderWidth: 1` — shrinking the content box by 2dp (300→298 for TicTacToe's 3×100dp cells, 256→254 for Sudoku's 4×64dp cells) — so the last column silently wrapped onto a row clipped away by the container's `overflow: 'hidden'`. A repo-wide `flexWrap` sweep (rooted at `MobileApp/`) found exactly these two defective files; `ScreenMonitor.tsx`'s `liveRow` (no fixed width) and `BadgesScreen.tsx`'s percentage-width `grid` are a different, unaffected shape. **Fix:** removed `borderWidth`/`borderColor` from both `grid` styles — each cell already carries its own border, so the board keeps its internal grid lines and loses only the 1dp outer outline. Both containers use the same `borderWidth` on every edge with `width === height` on container and cell, so the fix resolves both axes in one edit (worked by hand, not just asserted). **Regression test** (`__tests__/gameGridLayout.test.ts`, new file): rather than the more obvious hand-maintained constants object, each screen re-exports `gridLayout = {grid: styles.grid, cell: styles.cell}` — `StyleSheet.create` is a proven identity function at this RN pin (`StyleSheet.js:344-356`, `return obj`), so the test asserts on the *real* style numbers with zero drift risk. Per-game arithmetic assertions (`perRow * cell.width <= grid.width - 2*borderWidth`, and the height equivalent) went genuinely red against the pre-fix code for both games before the fix landed; a second guard per game asserts `padding`/`paddingHorizontal`/`paddingVertical` on `grid` and `margin`/`marginHorizontal`/`marginVertical` on `cell` are all `undefined` — the arithmetic only folds in `borderWidth`, so unguarded padding or margin could silently reintroduce the exact same wrap bug under a still-green suite. **Device verification is asymmetric, by design:** TicTacToe (C1) got the full arm — post-fix `uiautomator` showing nine cells three-per-row with valid bounds spanning the grid's full width, a real tap committing a mark in the third column, and a game played to completion using a third-column cell. Sudoku (C2) got a ~2-minute layout-only check (sixteen cells four-per-row, valid bounds, one screenshot) since the staging apparatus was already up — not played to completion. **No render harness exists in this repo** (`@testing-library/react-native` is not a dependency, Tier-4 #23) — the 13 passing `tttTurn.test.ts` unit tests were testing the correct thing (the pure game-state reducer) and could never have caught a stylesheet-only defect in the surrounding component; a green pure-logic suite says nothing about the screen the child actually touches.
- **ALL_IS_FIXED #41 — a failed mission-completion request trapped the child on a dead screen (2026-09-12).** Helmi hit a raw "Error" alert finishing a mission on device on 2026-09-11 (mission row deleted by #40's own DB restore, a 404) and was stuck: board finished, screen wouldn't close, hardware back disabled. `MissionScreen.submitCompletion`'s catch block only recovered from one error — `ApiHttpError` 409 ("already finished") — via a dedicated branch that settles, force-ends the capture lease, and navigates back; every other failure fell to a bare `Alert.alert('Error', err.message)` with a single OK that did nothing: no `navigation.goBack()`, no `forceEndMissionCaptureSession()` (screen monitoring stayed paused), hardware back a permanent no-op. The only escape was Quit → `handleAbandon`, a −10 penalty for a mission the child had actually finished. **Recon correction to the brief: expiry is 410, not 400** (`missions.routes.ts:509-521`, `{"error":"Mission expired"}`, opportunistically flips a stale row first) — 400 is a separate "wrong state"/validation-failure condition (`:523-526`, `:537-543`); both were unhandled before this fix, and conflating their text would have mislabeled one. Repo-wide greps confirmed `MissionScreen` is the **only** screen with this shape (sole `fullScreenModal`, hardware back forced `true`) — every game component, including `QuizScreen`, funnels through its `onComplete` callback with no error handling of its own, so all inherit the fix. The native overlay's `handleOverlayAction` (`useMissionOverlayListener.ts`) already had the right shape — a catch-all that still hides the overlay, force-ends the lease, and toasts a message — so this fix brings `MissionScreen` to parity with an already-correct sibling, not a new pattern. **Fix:** new pure `decideCompletionFailure(err)` (`src/missions/completionFailure.ts`, mirrors #12's `decideDeferredFrameHandoff` — pull the decision out of the component, unit-test the pure function) classifies every failure into a curated, child-readable `{action, title, message}` — `'alreadyFinished'` (409, unchanged UX) or `'recoverable'` (404/410/400/500/`ApiAuthError`/network/non-Error, now **Retry** + **Close**). No branch ever reads `err.message` — the raw wire text (`HTTP 404: {"error":...}`) never reaches the child for *any* failure shape, not just the one the original report named. Close never abandons or penalizes — it just releases the lease and navigates back, since the mission legitimately stays `pending` server-side; a lost-but-actually-successful response is harmless either way, since a second attempt (via Retry, or a later re-open) comes back 409 and is handled gracefully. Retry re-dispatches through a `submitCompletionRef` kept current by an effect (mirrors `useScreenshotCapture.ts`'s `processCapturedFrameRef`) rather than `submitCompletion` calling itself from inside its own `useCallback` body — the direct self-reference would have worked only by accident of stale-closure capture. **A worse pre-existing trap, found and mitigated while fixing this one:** the 409 branch's `settledRef.current = true` runs *before* the alert, not inside its OK handler — if that alert ends without `onPress` firing (tapped outside, or a device rotation, which is known to kill an RN `Alert` without firing `onPress` — Tier-3 #42), there's no release, no `goBack()`, the AppState-abandon listener is suppressed (`!settledRef.current` now false), **and `handleAbandon` itself early-returns on `if (settledRef.current) return`** — Quit does nothing either. Every exit gone, worse than the bug this brief targets (which at least had a working −10 escape hatch). Mitigated by passing `{cancelable: false}` on both new `Alert.alert` calls in the catch block — closes tap-outside/back-button dismissal, **does not** survive a rotation destroying the dialog, so **Tier-3 #42's severity is now "a rotation at the wrong moment can leave the child with no working exit at all,"** not just "an unexplained 66-second release." **Same shape still open, not fixed here (out of scope, name it so it isn't rediscovered as new):** the success-path `Alert.alert('Mission', message, [...])` a few lines above the catch block sets `settledRef.current = true` before its own alert too, with no `cancelable: false` — identical vulnerability, untouched because the approved plan scoped this fix to the catch block only. **Device-verified end to end, twice** (red arm before any code existed, green arm after): red — forced a mission, staged a risk-flagged capture via a local page loaded in Chrome, killed the backend by PID, played to a draw; confirmed the bare alert, the screen never closing, hardware back inert, and **no** `Mission capture session force end` line in logcat while trapped (lease still held). Quit's own `abandonMission` call *also* failed against the down backend (no `finally`-independent penalty landed server-side — the mission stayed `pending`); restarting the backend and calling the same abandon endpoint directly confirmed the real −10 (240→230). Green — Retry after backend restart: `+25 points! Total: 255`, clean `goBack`. Close on a second forced failure: screen closed immediately, points **unchanged** (255 before and after), and logcat showed the plain `Mission capture session force end` line with **no** preceding `exceeded max age — force-releasing` warning — confirming the immediate Close-triggered release, not the 10-minute backstop (the two paths are log-distinguishable exactly this way, `missionCaptureSession.ts:107-140`). **Not fixed/out of scope:** `TicTacToeGame.tsx`'s `void recordGameResult(...)` (fire-and-forget AsyncStorage write, unawaited before `onComplete` fires) — a rejection there is an unhandled promise rejection outside this catch block entirely; in RN that surfaces as a **LogBox banner**, the same artifact class that cost real diagnosis time investigating a stale `[Usage] Backend unreachable` banner on 2026-09-11 (a self-resolved, unrelated warning misread as "the error" at the time). A 401/`ApiAuthError` gets an honest "Sign-in expired" message, but `handleUnauthorized`'s token refresh is debounced 2 seconds, so an immediate Retry can need a second press. Mobile: **405 tests / 32 suites** (was 396/31, +9: 8 case-shape assertions incl. the 409/404/410/400/500/`ApiAuthError`/network/non-Error split, +1 message allow-list regression guard). `tsc` 8 pre-existing, 0 new. Lint 0 new (ambient CRLF drift on `MissionScreen.tsx`, pre-existing per the standard protocol — `git diff --stat` unchanged before/after `dos2unix`).
- **ALL_IS_FIXED #45 — the success-path completion alert had the same unguarded-`onPress` shape #41 left unfixed (2026-09-12).** `MissionScreen.tsx`'s success alert (`+points! Total: …`) set `settledRef.current = true` before showing, then released the capture lease and navigated back only inside the OK button's `onPress` — with no `{cancelable: false}`, unlike the two branches #41 had already hardened (`:151` alreadyFinished, `:173` Retry/Close). This is the path that runs on **every** normal completion, more reachable than #41's error branches. **Fix:** added `{cancelable: false}` as the alert's 4th argument, matching #41's precedent exactly — no other change (the early `settledRef` latch stays where it is; `confirmQuit` stays cancelable, since "Keep playing" is a real choice). **Device arms, all three run against a real forced Tic-Tac-Toe mission (2508CRN2BE-class device, `Y9WCSCZHTKEUKRZD` this session):** **Arm 1 (red, pre-fix)** was a clean **null result** — on the unmodified build, the alert already resisted an outside tap, the hardware back button, and a tap on the underlying "Quit" button; only OK worked, and worked correctly (`Mission capture session force end`, no preceding backstop warning). **Arm 2 (green, post-fix)** reproduced the identical resistant behavior — no regression, no behavior change from the fix on this ROM. **Arm 3 (rotation, the free arm that also probes Tier-3 #42):** with rotation unlocked, a live portrait→landscape rotation was forced via `settings put system user_rotation` while the alert was showing. **The alert survived intact** — re-laid out cleanly in landscape with the same text and button, no dismissal, no orphaned lease (confirmed no `Mission capture session force end` line appeared until OK was actually tapped) — and `onPress` fired normally afterward with a clean deterministic release. **This does not resolve #42** (a different 2026-09-11 session observed a rotation kill an `Alert` without firing `onPress`) — it measures that a straightforward portrait/landscape flip does *not* reproduce that failure on this build, leaving #42 narrower (something else about that session's rotation path, not simple orientation, remains unexplained) rather than closed. **No automated guard added, deliberately** — there is no render harness (`@testing-library/react-native` is not a dependency, Tier-4 #23) and a one-line `Alert.alert` argument has no pure-function shape to extract; a test asserting the literal argument exists would not guard the actual failure mode (dialog dismissal behavior) and was rejected as manufactured coverage. Verified: 405/32 tests (unchanged — no new tests), `tsc` 8 pre-existing/0 new, lint 0 new on the touched file (0 errors, 5 pre-existing unrelated `no-void` warnings). **Recon note that also updates #41's writeup above:** every `Alert.alert` call site under `MobileApp/src/` was re-enumerated (15 total, not 13) — the two `handleAbandon` alerts (`MissionScreen.tsx:64,69`) are **not** vulnerable to this shape despite having no `cancelable` option, because their release already runs unconditionally in the surrounding `finally` block, not inside the alert's `onPress`.
- **Design system** ("Calm Guardian" — warm sand + deep teal, coral for alerts). Tokens live in `src/theme/index.ts` (`colors`, `spacing`, `radius`, `type`, `shadow`, plus a dark `focus` palette for the immersive mission/game screens). Shared primitives are in `src/components/ui/index.tsx` (`Screen`, `Card`, `Button`, `Pill`, `SectionLabel`, `Meter`, `StatTile`, `PulseDot`, `EmptyState`). **Never hardcode hex in screens — import from the theme.** The parent dashboard (`demo_dashboard.html`) carries the same identity via a `tailwind.config` palette remap in its `<head>`; it is served directly at `/demo.html` from the repo-root file — no copy, no sync step (ALL_IS_FIXED #5). Edit `demo_dashboard.html` and reload. A `backend/public/demo.html` copy still works as a fallback (via `express.static`), and a missing file 404s rather than 500s.

Tests (`MobileApp/__tests__/`) heavily cover the risk logic (keyword filter, OCR normalization, adaptive capture, risk combination/mapping) and the capture coordinator/subsample logic as pure TS — no device needed. **460 mobile unit tests / 34 suites** as of ALL_IS_FIXED #58 (440/34 baseline carried in from #20/#53/#54, not individually re-narrated below; #58 added +20 to the existing `missionCaptureSession.test.ts` suite — no new suite — covering the touch/heartbeat mechanism, every branch of the overlay's on-demand liveness query including a rejected-promise case and a manual-resolver race test, and the hard-ceiling guard; see the ALL_IS_FIXED #58 bullet above). Progression up to #51 for historical reference: 251 pre-A3c → 258 A3c-1 → 267 A3c-2 → 281 A3c-2c → 303 A3c-3 → 308 A3d → 328 D1 → 330 D3 → 349 AF#1 → 363 AF#2 → 371 AF#4 → 372 AF#4b → 377 #11 → 380 #12-C2 → 390 #12-C1 → 396 #40 → 405 #41 → 405 #45 (no new tests) → 417 #51, +1 suite `missionCompletion.test.ts` at #51 (8 cases: quiz/cognitive/real_world/default regression guards plus the tictactoe minigame branch — evidence-present forwarding, evidence-absent `{}` not `{won:true}`, sudoku's unchanged `{won:true}`), `tttTurn.test.ts` +4 cases at #51 for `moveSequence` recording/replay. +1 suite `a11yHealth.test.ts` at AF#1, +1 suite `missionCaptureSession.test.ts` at AF#2, +1 suite `mobileArabicOcr.test.ts` at AF#4 (+8: pure `isRecognitionStale`, the `RECOGNITION_STALE_MS < OCR_LOCK_LIVENESS_MS` invariant, and degrade / re-enable+generation-guard / single-flight-preserved integration cases), +14 tests; AF#4b added +1 to that suite (a native rejection settles the slot cleanly — no `did not settle`, next frame re-armed); D1 added two `describe` blocks to `adaptiveCapture.test.ts` — `shouldForceReleasePhase` (incl. the anti-stale guard and a permanent regression guard that `vision` never fires) and `decideTickAction`'s phase-timeout composition (incl. two regression guards proving the 60s absolute backstop still fires when the phase ref is stale or unset) — and updated a pre-D1 test comment that had been the only artifact in the repo resembling "evidence" for a `vision` deadline number, to state plainly that the figure was unmeasured. D3 added two sibling cases to the phase-timeout-composition block: a real successor start stamp still force-releases via the 60s backstop, and a zeroed stamp returns `noop` — pinning the reducer contract on either side of the one-line `finally` fix, which itself has no reachable harness). #11 added +1 suite `foregroundApp.test.ts` (5 tests) — see the #11 bullet above. #12 added no new suites: C2 added a first-ever `NsfwTflite` mock + 3 cases to `nsfwClassifier.test.ts`; C1 added a `decideDeferredFrameHandoff` `describe` block (9 cases) to `adaptiveCapture.test.ts` and one `peekPendingReason`/`takePendingReason` regression case to `captureCoordinator.test.ts` — see the ALL_IS_FIXED #12 bullet above. #40 added +1 suite `gameGridLayout.test.ts` (6 cases: two per game for the width/height arithmetic, one per game for the padding/margin absence guard) — see the ALL_IS_FIXED #40 bullet above. #41 added +1 suite `completionFailure.test.ts` (9 cases: one per failure shape — 409/404/410/400/500/`ApiAuthError`/network `TypeError`/non-Error throw — plus one message allow-list regression guard) — see the ALL_IS_FIXED #41 bullet above.

## Working across both apps

- API contract changes touch **both** sides: a backend route/shape change needs the matching `MobileApp/src/services/*Api.ts` update. There is no shared/generated type package — types are duplicated by hand.
- The mobile app assumes the backend is reachable at the configured LAN IP on port 3000. "JWT error / start backend" on the app's loading screen means the backend isn't up or the IP is wrong.

## Docs worth reading before large changes

`docs/` holds the formal project docs: `architecture.md`, `SRS.md`, `scoring_formulas.md` (the exact score weights/formulas), `testing_strategy.md`, `deployment.md`. `backend/DATABASE.md` and `MobileApp/PROJECT_SETUP.md` cover environment setup. The root `README.md` is the fullest architecture reference (with mermaid diagrams and the adaptive-capture tables); its adaptive-capture / wedged-frame-recovery sections and the `docs/` reliability + test-count references were brought current with the A3c→D3 model on 2026-09-03.
