<?xml version="1.0" encoding="utf-8"?>
<!--
  Config for SafeGuardAccessibilityService. Mirrors the AccessibilityServiceInfo built in
  onServiceConnected(). canRetrieveWindowContent is required for getWindows() /
  flagRetrieveInteractiveWindows to return anything — only the window *type* is read
  (to detect the IME window), never window content, node text, or URLs.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowsChanged|typeViewScrolled"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />
