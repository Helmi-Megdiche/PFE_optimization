package com.mobileapp.accessibility.browser;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

/** Proves the JVM unit-test task runs. Deleted once real tests exist. */
public class JvmInfraTest {
    @Test
    public void jvmTestsRun() {
        assertTrue(true);
    }
}
